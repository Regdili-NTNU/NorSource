
      abandonere_v-k := predsort.
      abbreviere_v-k := predsort.
      abdisere_v-k := predsort.
      abonnere_v-k := predsort.
      abortere_v-k := predsort.
      absentere_v-k := predsort.
      absolvere_v-k := predsort.
      absorbere_v-k := predsort.
      abstrahere_v-k := predsort.
      adaptere_v-k := predsort.
      addere_v-k := predsort.
      adherere_v-k := predsort.
      adjektivere_v-k := predsort.
      adle_v-k := predsort.
      adlyde_v-k := predsort.
      administrere_v-k := predsort.
      adoptere_v-k := predsort.
      adressere_v-k := predsort.
      adsorbere_v-k := predsort.
      adstringere_v-k := predsort.
      advare_v-k := predsort.
      advisere_v-k := predsort.
      ære_v-k := predsort.
      ærekrenke_v-k := predsort.
      æresforplikte_v-k := predsort.
      æreskjelle_v-k := predsort.
      æreskjenne_v-k := predsort.
      ætte_v-k := predsort.
      ættlede_v-k := predsort.
      affektere_v-k := predsort.
      affisere_v-k := predsort.
      afrikanisere_v-k := predsort.
      age_v-k := predsort.
      agere_v-k := predsort.
      agglutinere_v-k := predsort.
      agitere_v-k := predsort.
      agne_v-k := predsort.
      ajourføre_v-k := predsort.
      ake_v-k := predsort.
      akkedere_v-k := predsort.
      akke_v-k := predsort.
      akklimatisere_v-k := predsort.
      akkommodere_v-k := predsort.
      akkompagnere_v-k := predsort.
      akkordere_v-k := predsort.
      akkreditere_v-k := predsort.
      akkumulere_v-k := predsort.
      akkvirere_v-k := predsort.
      akselerere_v-k := predsort.
      aksentuere_v-k := predsort.
      akseptere_v-k := predsort.
      aksjonere_v-k := predsort.
      aksle_v-k := predsort.
      akte_v-k := predsort.
      akterutsegle_v-k := predsort.
      akterutseile_v-k := predsort.
      aktivere_v-k := predsort.
      aktivisere_v-k := predsort.
      aktualisere_v-k := predsort.
      alarmere_v-k := predsort.
      alboge_v-k := predsort.
      albue_v-k := predsort.
      aldersbestemme_v-k := predsort.
      aldre_v-k := predsort.
      aldres_v-k := predsort.
      ale_v-k := predsort.
      alfabetisere_v-k := predsort.
      alienere_v-k := predsort.
      alkoholisere_v-k := predsort.
      alliere_v-k := predsort.
      allitterere_v-k := predsort.
      allokere_v-k := predsort.
      alludere_v-k := predsort.
      alterere_v-k := predsort.
      alternere_v-k := predsort.
      aluminere_v-k := predsort.
      amalgamere_v-k := predsort.
      ambulere_v-k := predsort.
      amerikanisere_v-k := predsort.
      amme_v-k := predsort.
      amortisere_v-k := predsort.
      amputere_v-k := predsort.
      analysere_v-k := predsort.
      anbefale_v-k := predsort.
      anbringe_v-k := predsort.
      ande_v-k := predsort.
      ane_v-k := predsort.
      anerkjenne_v-k := predsort.
      anestesere_v-k := predsort.
      anfalle_v-k := predsort.
      anfekte_v-k := predsort.
      anføre_v-k := predsort.
      angå_v-k := predsort.
      ange_v-k := predsort.
      angi_v-k := predsort.
      angle_v-k := predsort.
      anglifisere_v-k := predsort.
      anglisere_v-k := predsort.
      angre_v-k := predsort.
      angripe_v-k := predsort.
      anholde_v-k := predsort.
      animalisere_v-k := predsort.
      animere_v-k := predsort.
      anke_v-k := predsort.
      anklage_v-k := predsort.
      ankomme_v-k := predsort.
      ankre_v-k := predsort.
      anlegge_v-k := predsort.
      anløpe_v-k := predsort.
      anmasse_v-k := predsort.
      anmelde_v-k := predsort.
      anmerke_v-k := predsort.
      anmode_v-k := predsort.
      annamme_v-k := predsort.
      annektere_v-k := predsort.
      annonsere_v-k := predsort.
      annullere_v-k := predsort.
      anodisere_v-k := predsort.
      anonymisere_v-k := predsort.
      anordne_v-k := predsort.
      anrette_v-k := predsort.
      anrike_v-k := predsort.
      anrope_v-k := predsort.
      anse_v-k := predsort.
      ansette_v-k := predsort.
      anskaffe_v-k := predsort.
      anskue_v-k := predsort.
      anskueliggjøre_v-k := predsort.
      anslå_v-k := predsort.
      ansøke_v-k := predsort.
      anspenne_v-k := predsort.
      anspore_v-k := predsort.
      anstå_v-k := predsort.
      anstifte_v-k := predsort.
      anstille_v-k := predsort.
      anstrenge_v-k := predsort.
      anta_v-k := predsort.
      antaste_v-k := predsort.
      antedatere_v-k := predsort.
      antegne_v-k := predsort.
      antenne_v-k := predsort.
      antesipere_v-k := predsort.
      antikisere_v-k := predsort.
      antikrøllbehandle_v-k := predsort.
      antikrympbehandle_v-k := predsort.
      antirefleksbehandle_v-k := predsort.
      antirustbehandle_v-k := predsort.
      antistatbehandle_v-k := predsort.
      antyde_v-k := predsort.
      anvende_v-k := predsort.
      anvise_v-k := predsort.
      ape_v-k := predsort.
      åpenbare_v-k := predsort.
      åpne_v-k := predsort.
      apokopere_v-k := predsort.
      apostrofere_v-k := predsort.
      appellere_v-k := predsort.
      applaudere_v-k := predsort.
      applikere_v-k := predsort.
      applisere_v-k := predsort.
      apportere_v-k := predsort.
      appretere_v-k := predsort.
      approbere_v-k := predsort.
      appropriere_v-k := predsort.
      aptere_v-k := predsort.
      arbeide_v-k := predsort.
      arde_v-k := predsort.
      arge_v-k := predsort.
      argumentere_v-k := predsort.
      arkaisere_v-k := predsort.
      arkebusere_v-k := predsort.
      arkivere_v-k := predsort.
      arme_v-k := predsort.
      armere_v-k := predsort.
      aromatisere_v-k := predsort.
      arrangere_v-k := predsort.
      arrestere_v-k := predsort.
      arretere_v-k := predsort.
      arrivere_v-k := predsort.
      arrondere_v-k := predsort.
      arte_v-k := predsort.
      artikulere_v-k := predsort.
      artsbestemme_v-k := predsort.
      arve_v-k := predsort.
      asfaltere_v-k := predsort.
      aspirere_v-k := predsort.
      assimilere_v-k := predsort.
      assistere_v-k := predsort.
      assortere_v-k := predsort.
      assosiere_v-k := predsort.
      assurere_v-k := predsort.
      astringere_v-k := predsort.
      atomisere_v-k := predsort.
      atrofiere_v-k := predsort.
      atskille_v-k := predsort.
      atsprede_v-k := predsort.
      atspre_v-k := predsort.
      attachere_v-k := predsort.
      attakkere_v-k := predsort.
      åttedoble_v-k := predsort.
      attestere_v-k := predsort.
      attrahere_v-k := predsort.
      attrå_v-k := predsort.
      auke_v-k := predsort.
      auksjonere_v-k := predsort.
      aure_v-k := predsort.
      ause_v-k := predsort.
      autoklavere_v-k := predsort.
      automatisere_v-k := predsort.
      autorisere_v-k := predsort.
      avalere_v-k := predsort.
      avansere_v-k := predsort.
      avbalansere_v-k := predsort.
      avbestille_v-k := predsort.
      avbetale_v-k := predsort.
      avbilde_v-k := predsort.
      avbinde_v-k := predsort.
      avblåse_v-k := predsort.
      avbøte_v-k := predsort.
      avbøye_v-k := predsort.
      avbryte_v-k := predsort.
      avdampe_v-k := predsort.
      avdekke_v-k := predsort.
      avdele_v-k := predsort.
      avdempe_v-k := predsort.
      avdra_v-k := predsort.
      avdramatisere_v-k := predsort.
      avduke_v-k := predsort.
      avertere_v-k := predsort.
      avfase_v-k := predsort.
      avfatte_v-k := predsort.
      avfeie_v-k := predsort.
      avferdige_v-k := predsort.
      avfette_v-k := predsort.
      avfinne_v-k := predsort.
      avføde_v-k := predsort.
      avfolke_v-k := predsort.
      avføre_v-k := predsort.
      avfyre_v-k := predsort.
      avgå_v-k := predsort.
      avgi_v-k := predsort.
      avgjøre_v-k := predsort.
      avgrense_v-k := predsort.
      avheimle_v-k := predsort.
      avhende_v-k := predsort.
      avhenge_v-k := predsort.
      avhente_v-k := predsort.
      avherde_v-k := predsort.
      avhjelpe_v-k := predsort.
      avhjemle_v-k := predsort.
      avholde_v-k := predsort.
      avhøre_v-k := predsort.
      avise_v-k := predsort.
      avkante_v-k := predsort.
      avkjøle_v-k := predsort.
      avkjønne_v-k := predsort.
      avklare_v-k := predsort.
      avkle_v-k := predsort.
      avkode_v-k := predsort.
      avkoke_v-k := predsort.
      avkolonisere_v-k := predsort.
      avkorte_v-k := predsort.
      avkrefte_v-k := predsort.
      avkreve_v-k := predsort.
      avkriminalisere_v-k := predsort.
      avkristne_v-k := predsort.
      avlåse_v-k := predsort.
      avlaste_v-k := predsort.
      avlede_v-k := predsort.
      avlegge_v-k := predsort.
      avleie_v-k := predsort.
      avleire_v-k := predsort.
      avle_v-k := predsort.
      avlese_v-k := predsort.
      avlevere_v-k := predsort.
      avlive_v-k := predsort.
      avlokke_v-k := predsort.
      avlønne_v-k := predsort.
      avløse_v-k := predsort.
      avløyse_v-k := predsort.
      avlure_v-k := predsort.
      avluse_v-k := predsort.
      avlyse_v-k := predsort.
      avlytte_v-k := predsort.
      avmagnetisere_v-k := predsort.
      avmagre_v-k := predsort.
      avmåle_v-k := predsort.
      avmerke_v-k := predsort.
      avmilitarisere_v-k := predsort.
      avmønstre_v-k := predsort.
      avmytologisere_v-k := predsort.
      avnasjonalisere_v-k := predsort.
      avnorske_v-k := predsort.
      avpasse_v-k := predsort.
      avpatruljere_v-k := predsort.
      avpolitisere_v-k := predsort.
      avreagere_v-k := predsort.
      avreise_v-k := predsort.
      avrette_v-k := predsort.
      avrime_v-k := predsort.
      avrunde_v-k := predsort.
      avruse_v-k := predsort.
      avruste_v-k := predsort.
      avsanne_v-k := predsort.
      avse_v-k := predsort.
      avsende_v-k := predsort.
      avsette_v-k := predsort.
      avsi_v-k := predsort.
      avsile_v-k := predsort.
      avskaffe_v-k := predsort.
      avskalle_v-k := predsort.
      avskilte_v-k := predsort.
      avskipe_v-k := predsort.
      avskjære_v-k := predsort.
      avskjedige_v-k := predsort.
      avskjerme_v-k := predsort.
      avskoge_v-k := predsort.
      avskrape_v-k := predsort.
      avskrekke_v-k := predsort.
      avskrive_v-k := predsort.
      avsky_v-k := predsort.
      avslå_v-k := predsort.
      avsløre_v-k := predsort.
      avslutte_v-k := predsort.
      avsondre_v-k := predsort.
      avsone_v-k := predsort.
      avspalte_v-k := predsort.
      avspasere_v-k := predsort.
      avspeile_v-k := predsort.
      avsperre_v-k := predsort.
      avspille_v-k := predsort.
      avspise_v-k := predsort.
      avspore_v-k := predsort.
      avstå_v-k := predsort.
      avstedkomme_v-k := predsort.
      avstemme_v-k := predsort.
      avstive_v-k := predsort.
      avstøpe_v-k := predsort.
      avstraffe_v-k := predsort.
      avstresse_v-k := predsort.
      avsvekke_v-k := predsort.
      avsverge_v-k := predsort.
      avsverje_v-k := predsort.
      avsvi_v-k := predsort.
      avsynge_v-k := predsort.
      avta_v-k := predsort.
      avtalefeste_v-k := predsort.
      avtale_v-k := predsort.
      avtegne_v-k := predsort.
      avtjene_v-k := predsort.
      avtrappe_v-k := predsort.
      avtvinge_v-k := predsort.
      avvæpne_v-k := predsort.
      avveie_v-k := predsort.
      avvende_v-k := predsort.
      avvenne_v-k := predsort.
      avvente_v-k := predsort.
      avverge_v-k := predsort.
      avverje_v-k := predsort.
      avvike_v-k := predsort.
      avvikle_v-k := predsort.
      avvirke_v-k := predsort.
      avvise_v-k := predsort.
      bable_v-k := predsort.
      bade_v-k := predsort.
      bære_v-k := predsort.
      bæsje_v-k := predsort.
      bagatellisere_v-k := predsort.
      baisse_v-k := predsort.
      bakbinde_v-k := predsort.
      bake_v-k := predsort.
      bakke_v-k := predsort.
      bakse_v-k := predsort.
      baksnakke_v-k := predsort.
      baktale_v-k := predsort.
      bakvaske_v-k := predsort.
      balansere_v-k := predsort.
      bale_v-k := predsort.
      balkanisere_v-k := predsort.
      ballaste_v-k := predsort.
      ballottere_v-k := predsort.
      balsamere_v-k := predsort.
      banalisere_v-k := predsort.
      bandasjere_v-k := predsort.
      bande_v-k := predsort.
      bandlegge_v-k := predsort.
      båndlegge_v-k := predsort.
      bane_v-k := predsort.
      banke_v-k := predsort.
      banne_v-k := predsort.
      bannlyse_v-k := predsort.
      bannsette_v-k := predsort.
      barbere_v-k := predsort.
      bardunere_v-k := predsort.
      bare_v-k := predsort.
      båre_v-k := predsort.
      barke_v-k := predsort.
      barrikadere_v-k := predsort.
      barske_v-k := predsort.
      base_v-k := predsort.
      basere_v-k := predsort.
      baske_v-k := predsort.
      baste_v-k := predsort.
      basunere_v-k := predsort.
      baute_v-k := predsort.
      beære_v-k := predsort.
      beånde_v-k := predsort.
      bearbeide_v-k := predsort.
      bebo_v-k := predsort.
      bebreide_v-k := predsort.
      bebude_v-k := predsort.
      bebygge_v-k := predsort.
      bebyrde_v-k := predsort.
      bedage_v-k := predsort.
      bedåre_v-k := predsort.
      bedekke_v-k := predsort.
      bedømme_v-k := predsort.
      bedøve_v-k := predsort.
      bedre_v-k := predsort.
      bedrive_v-k := predsort.
      bedrøve_v-k := predsort.
      bedyre_v-k := predsort.
      beedige_v-k := predsort.
      befale_v-k := predsort.
      befare_v-k := predsort.
      befatte_v-k := predsort.
      befeste_v-k := predsort.
      befinne_v-k := predsort.
      beflitte_v-k := predsort.
      beføle_v-k := predsort.
      befolke_v-k := predsort.
      befordre_v-k := predsort.
      befrakte_v-k := predsort.
      befri_v-k := predsort.
      befrukte_v-k := predsort.
      begå_v-k := predsort.
      begeistre_v-k := predsort.
      begi_v-k := predsort.
      begjære_v-k := predsort.
      beglo_v-k := predsort.
      begrave_v-k := predsort.
      begrense_v-k := predsort.
      begrepsbestemme_v-k := predsort.
      begripe_v-k := predsort.
      begrunne_v-k := predsort.
      begunstige_v-k := predsort.
      begynne_v-k := predsort.
      behage_v-k := predsort.
      behandle_v-k := predsort.
      behefte_v-k := predsort.
      beherske_v-k := predsort.
      beholde_v-k := predsort.
      behøve_v-k := predsort.
      beile_v-k := predsort.
      beine_v-k := predsort.
      beinflyge_v-k := predsort.
      beinke_v-k := predsort.
      beise_v-k := predsort.
      beiskne_v-k := predsort.
      beite_v-k := predsort.
      bejae_v-k := predsort.
      beke_v-k := predsort.
      bekjempe_v-k := predsort.
      bekjenne_v-k := predsort.
      bekjentgjøre_v-k := predsort.
      beklage_v-k := predsort.
      bekle_v-k := predsort.
      beklippe_v-k := predsort.
      bekomme_v-k := predsort.
      bekoste_v-k := predsort.
      be_v-k := predsort.
      bekranse_v-k := predsort.
      bekrefte_v-k := predsort.
      beksle_v-k := predsort.
      bekvemme_v-k := predsort.
      bekymre_v-k := predsort.
      belære_v-k := predsort.
      belage_v-k := predsort.
      belåne_v-k := predsort.
      belaste_v-k := predsort.
      belegge_v-k := predsort.
      beleire_v-k := predsort.
      belemre_v-k := predsort.
      belesse_v-k := predsort.
      belje_v-k := predsort.
      belme_v-k := predsort.
      belønne_v-k := predsort.
      beløpe_v-k := predsort.
      belyse_v-k := predsort.
      bemanne_v-k := predsort.
      bemektige_v-k := predsort.
      bemerke_v-k := predsort.
      bemøye_v-k := predsort.
      bemyndige_v-k := predsort.
      benåde_v-k := predsort.
      bende_v-k := predsort.
      bene_v-k := predsort.
      benekte_v-k := predsort.
      benevne_v-k := predsort.
      benke_v-k := predsort.
      benytte_v-k := predsort.
      beordre_v-k := predsort.
      beplante_v-k := predsort.
      beramme_v-k := predsort.
      berede_v-k := predsort.
      beregne_v-k := predsort.
      berekne_v-k := predsort.
      berette_v-k := predsort.
      berettige_v-k := predsort.
      berge_v-k := predsort.
      bergta_v-k := predsort.
      berike_v-k := predsort.
      beriktige_v-k := predsort.
      bero_v-k := predsort.
      berolige_v-k := predsort.
      berømme_v-k := predsort.
      berøre_v-k := predsort.
      berøve_v-k := predsort.
      berte_v-k := predsort.
      beruse_v-k := predsort.
      besegle_v-k := predsort.
      beseire_v-k := predsort.
      bese_v-k := predsort.
      besette_v-k := predsort.
      besifre_v-k := predsort.
      besikte_v-k := predsort.
      besiktige_v-k := predsort.
      besinne_v-k := predsort.
      besitte_v-k := predsort.
      besjele_v-k := predsort.
      beskadige_v-k := predsort.
      beskatte_v-k := predsort.
      beskikke_v-k := predsort.
      beskjære_v-k := predsort.
      beskjeftige_v-k := predsort.
      beskne_v-k := predsort.
      beskrive_v-k := predsort.
      beskue_v-k := predsort.
      beskylde_v-k := predsort.
      beskyte_v-k := predsort.
      beskytte_v-k := predsort.
      beslaglegge_v-k := predsort.
      beslå_v-k := predsort.
      beslutte_v-k := predsort.
      besmitte_v-k := predsort.
      besnære_v-k := predsort.
      besnakke_v-k := predsort.
      besøke_v-k := predsort.
      besørge_v-k := predsort.
      bespise_v-k := predsort.
      bespotte_v-k := predsort.
      bestå_v-k := predsort.
      bestemme_v-k := predsort.
      bestige_v-k := predsort.
      bestikke_v-k := predsort.
      bestille_v-k := predsort.
      bestjele_v-k := predsort.
      bestorme_v-k := predsort.
      bestøve_v-k := predsort.
      bestråle_v-k := predsort.
      bestrebe_v-k := predsort.
      bestride_v-k := predsort.
      bestri_v-k := predsort.
      bestryke_v-k := predsort.
      bestykke_v-k := predsort.
      bestyre_v-k := predsort.
      bestyrke_v-k := predsort.
      besudle_v-k := predsort.
      besvære_v-k := predsort.
      besvangre_v-k := predsort.
      besvare_v-k := predsort.
      besverge_v-k := predsort.
      besverje_v-k := predsort.
      besvime_v-k := predsort.
      besynge_v-k := predsort.
      betakke_v-k := predsort.
      beta_v-k := predsort.
      betale_v-k := predsort.
      betegne_v-k := predsort.
      bete_v-k := predsort.
      betenke_v-k := predsort.
      betinge_v-k := predsort.
      betjene_v-k := predsort.
      betle_v-k := predsort.
      betone_v-k := predsort.
      betrakte_v-k := predsort.
      betrede_v-k := predsort.
      betre_v-k := predsort.
      betro_v-k := predsort.
      betvile_v-k := predsort.
      betvinge_v-k := predsort.
      bety_v-k := predsort.
      beundre_v-k := predsort.
      bevæpne_v-k := predsort.
      bevare_v-k := predsort.
      bevege_v-k := predsort.
      beve_v-k := predsort.
      beverte_v-k := predsort.
      bevilge_v-k := predsort.
      bevirke_v-k := predsort.
      bevise_v-k := predsort.
      bevisstgjøre_v-k := predsort.
      bevitne_v-k := predsort.
      bevokte_v-k := predsort.
      bevre_v-k := predsort.
      bibeholde_v-k := predsort.
      bibringe_v-k := predsort.
      bidra_v-k := predsort.
      bie_v-k := predsort.
      bifalle_v-k := predsort.
      bikke_v-k := predsort.
      bikte_v-k := predsort.
      bilegge_v-k := predsort.
      bile_v-k := predsort.
      billettere_v-k := predsort.
      billige_v-k := predsort.
      binde_v-k := predsort.
      bisette_v-k := predsort.
      bistå_v-k := predsort.
      bite_v-k := predsort.
      bivåne_v-k := predsort.
      bivuakkere_v-k := predsort.
      bjeffe_v-k := predsort.
      blære_v-k := predsort.
      blaffe_v-k := predsort.
      blafre_v-k := predsort.
      blåholde_v-k := predsort.
      blakke_v-k := predsort.
      blakne_v-k := predsort.
      bla_v-k := predsort.
      blakre_v-k := predsort.
      blamere_v-k := predsort.
      blande_v-k := predsort.
      blåne_v-k := predsort.
      blånekte_v-k := predsort.
      blanke_v-k := predsort.
      blankpolere_v-k := predsort.
      blankpusse_v-k := predsort.
      blankslite_v-k := predsort.
      blåse_v-k := predsort.
      blasonere_v-k := predsort.
      blautgjøre_v-k := predsort.
      blautkoke_v-k := predsort.
      bleige_v-k := predsort.
      bleike_v-k := predsort.
      bleikne_v-k := predsort.
      bleke_v-k := predsort.
      blekke_v-k := predsort.
      blekne_v-k := predsort.
      blende_v-k := predsort.
      blidgjøre_v-k := predsort.
      bli_v-k := predsort.
      blinde_v-k := predsort.
      blingse_v-k := predsort.
      blinke_v-k := predsort.
      blitse_v-k := predsort.
      blive_v-k := predsort.
      bløde_v-k := predsort.
      bløffe_v-k := predsort.
      blogge_v-k := predsort.
      bløgge_v-k := predsort.
      blokke_v-k := predsort.
      blokkere_v-k := predsort.
      blø_v-k := predsort.
      blomstre_v-k := predsort.
      blote_v-k := predsort.
      bløte_v-k := predsort.
      bløtgjøre_v-k := predsort.
      bløtkoke_v-k := predsort.
      bløtlegge_v-k := predsort.
      blotte_v-k := predsort.
      blottlegge_v-k := predsort.
      blottstille_v-k := predsort.
      bløyte_v-k := predsort.
      bløytlegge_v-k := predsort.
      blues_v-k := predsort.
      blunde_v-k := predsort.
      blunke_v-k := predsort.
      blusse_v-k := predsort.
      blyges_v-k := predsort.
      boble_v-k := predsort.
      boge_v-k := predsort.
      bogne_v-k := predsort.
      boikotte_v-k := predsort.
      bokføre_v-k := predsort.
      bokne_v-k := predsort.
      bo_v-k := predsort.
      bokse_v-k := predsort.
      bokstavere_v-k := predsort.
      bole_v-k := predsort.
      bølge_v-k := predsort.
      bølgje_v-k := predsort.
      bolte_v-k := predsort.
      boltre_v-k := predsort.
      bombardere_v-k := predsort.
      bombe_v-k := predsort.
      bomme_v-k := predsort.
      bone_v-k := predsort.
      bonge_v-k := predsort.
      bonitere_v-k := predsort.
      bønnfalle_v-k := predsort.
      bønnhøre_v-k := predsort.
      booke_v-k := predsort.
      bordkle_v-k := predsort.
      bore_v-k := predsort.
      borge_v-k := predsort.
      børse_v-k := predsort.
      børsnotere_v-k := predsort.
      børste_v-k := predsort.
      bortbygsle_v-k := predsort.
      bortfalle_v-k := predsort.
      bortfeste_v-k := predsort.
      bortføre_v-k := predsort.
      bortforklare_v-k := predsort.
      bortfrakte_v-k := predsort.
      bortlede_v-k := predsort.
      bortløpe_v-k := predsort.
      bortødsle_v-k := predsort.
      bortse_v-k := predsort.
      bortsette_v-k := predsort.
      bortvise_v-k := predsort.
      bosette_v-k := predsort.
      botanisere_v-k := predsort.
      bøte_v-k := predsort.
      bøtelegge_v-k := predsort.
      botlegge_v-k := predsort.
      botne_v-k := predsort.
      botnfelle_v-k := predsort.
      botnfryse_v-k := predsort.
      botnskrape_v-k := predsort.
      bowle_v-k := predsort.
      bøye_v-k := predsort.
      bråbremse_v-k := predsort.
      brake_v-k := predsort.
      bråke_v-k := predsort.
      brakke_v-k := predsort.
      bramme_v-k := predsort.
      bråne_v-k := predsort.
      brase_v-k := predsort.
      braske_v-k := predsort.
      bråstanse_v-k := predsort.
      bratne_v-k := predsort.
      braute_v-k := predsort.
      bravere_v-k := predsort.
      breie_v-k := predsort.
      breke_v-k := predsort.
      brekke_v-k := predsort.
      bre_v-k := predsort.
      bremme_v-k := predsort.
      bremse_v-k := predsort.
      brenne_v-k := predsort.
      brennemerke_v-k := predsort.
      bresere_v-k := predsort.
      breste_v-k := predsort.
      brette_v-k := predsort.
      brettsegle_v-k := predsort.
      brettseile_v-k := predsort.
      brife_v-k := predsort.
      brikettere_v-k := predsort.
      briljere_v-k := predsort.
      bringe_v-k := predsort.
      brise_v-k := predsort.
      briske_v-k := predsort.
      briste_v-k := predsort.
      brodde_v-k := predsort.
      brodere_v-k := predsort.
      brødfø_v-k := predsort.
      brolegge_v-k := predsort.
      brøle_v-k := predsort.
      bronse_v-k := predsort.
      bronsere_v-k := predsort.
      brosjere_v-k := predsort.
      brotne_v-k := predsort.
      brotsje_v-k := predsort.
      brøyte_v-k := predsort.
      bruke_v-k := predsort.
      brulegge_v-k := predsort.
      brumle_v-k := predsort.
      brumme_v-k := predsort.
      brune_v-k := predsort.
      brunsteike_v-k := predsort.
      brunsteke_v-k := predsort.
      bruse_v-k := predsort.
      brutalisere_v-k := predsort.
      brydde_v-k := predsort.
      brygge_v-k := predsort.
      bry_v-k := predsort.
      bryne_v-k := predsort.
      bryske_v-k := predsort.
      bryste_v-k := predsort.
      bryte_v-k := predsort.
      budsende_v-k := predsort.
      budsjettere_v-k := predsort.
      bue_v-k := predsort.
      bugne_v-k := predsort.
      bukke_v-k := predsort.
      buklande_v-k := predsort.
      bu_v-k := predsort.
      buksere_v-k := predsort.
      bukte_v-k := predsort.
      buldre_v-k := predsort.
      bule_v-k := predsort.
      bulke_v-k := predsort.
      bulne_v-k := predsort.
      bumpe_v-k := predsort.
      bunke_v-k := predsort.
      bunkre_v-k := predsort.
      bunne_v-k := predsort.
      bunnfelle_v-k := predsort.
      bunnfryse_v-k := predsort.
      bunnskrape_v-k := predsort.
      bunte_v-k := predsort.
      burde_v-k := predsort.
      bure_v-k := predsort.
      buse_v-k := predsort.
      busette_v-k := predsort.
      buste_v-k := predsort.
      butte_v-k := predsort.
      byde_v-k := predsort.
      bygge_v-k := predsort.
      bygsle_v-k := predsort.
      by_v-k := predsort.
      bykse_v-k := predsort.
      byråkratisere_v-k := predsort.
      bysse_v-k := predsort.
      bytte_v-k := predsort.
      byttelåne_v-k := predsort.
      campe_v-k := predsort.
      cashe_v-k := predsort.
      celebrere_v-k := predsort.
      changere_v-k := predsort.
      chartre_v-k := predsort.
      chevrotere_v-k := predsort.
      chiffrere_v-k := predsort.
      choke_v-k := predsort.
      clinche_v-k := predsort.
      clutche_v-k := predsort.
      cruise_v-k := predsort.
      cyanidisere_v-k := predsort.
      dabbe_v-k := predsort.
      dadle_v-k := predsort.
      daffe_v-k := predsort.
      dagdrømme_v-k := predsort.
      dage_v-k := predsort.
      dages_v-k := predsort.
      dale_v-k := predsort.
      dalke_v-k := predsort.
      damascere_v-k := predsort.
      dampe_v-k := predsort.
      dampkoke_v-k := predsort.
      dandere_v-k := predsort.
      dåne_v-k := predsort.
      dangle_v-k := predsort.
      danke_v-k := predsort.
      danne_v-k := predsort.
      danse_v-k := predsort.
      danske_v-k := predsort.
      dåre_v-k := predsort.
      darre_v-k := predsort.
      daske_v-k := predsort.
      databehandle_v-k := predsort.
      dataføre_v-k := predsort.
      datastyre_v-k := predsort.
      datere_v-k := predsort.
      datostemple_v-k := predsort.
      daude_v-k := predsort.
      daugjødsle_v-k := predsort.
      dauhogge_v-k := predsort.
      dauhugge_v-k := predsort.
      daukjøre_v-k := predsort.
      deaktivere_v-k := predsort.
      debattere_v-k := predsort.
      debitere_v-k := predsort.
      debutere_v-k := predsort.
      dechiffrere_v-k := predsort.
      dedisere_v-k := predsort.
      dedusere_v-k := predsort.
      defilere_v-k := predsort.
      definere_v-k := predsort.
      deflatere_v-k := predsort.
      deflorere_v-k := predsort.
      deformere_v-k := predsort.
      degenerere_v-k := predsort.
      degge_v-k := predsort.
      degradere_v-k := predsort.
      dehydrere_v-k := predsort.
      deise_v-k := predsort.
      dekantere_v-k := predsort.
      dekatere_v-k := predsort.
      dekke_v-k := predsort.
      deklamere_v-k := predsort.
      deklarere_v-k := predsort.
      deklassere_v-k := predsort.
      deklinere_v-k := predsort.
      dekode_v-k := predsort.
      dekomponere_v-k := predsort.
      dekorere_v-k := predsort.
      dekortere_v-k := predsort.
      dekretere_v-k := predsort.
      dekupere_v-k := predsort.
      delabialisere_v-k := predsort.
      delegere_v-k := predsort.
      dele_v-k := predsort.
      delere_v-k := predsort.
      delikatere_v-k := predsort.
      delirere_v-k := predsort.
      delje_v-k := predsort.
      delta_v-k := predsort.
      demaskere_v-k := predsort.
      dementere_v-k := predsort.
      demilitarisere_v-k := predsort.
      demisjonere_v-k := predsort.
      demme_v-k := predsort.
      demobilisere_v-k := predsort.
      demoe_v-k := predsort.
      demokratisere_v-k := predsort.
      demolere_v-k := predsort.
      demonstrere_v-k := predsort.
      demontere_v-k := predsort.
      demoralisere_v-k := predsort.
      dempe_v-k := predsort.
      demre_v-k := predsort.
      denasjonalisere_v-k := predsort.
      denaturalisere_v-k := predsort.
      denaturere_v-k := predsort.
      denge_v-k := predsort.
      denotere_v-k := predsort.
      denudere_v-k := predsort.
      deoksidere_v-k := predsort.
      deponere_v-k := predsort.
      deportere_v-k := predsort.
      deppe_v-k := predsort.
      depravere_v-k := predsort.
      deprimere_v-k := predsort.
      deprivatisere_v-k := predsort.
      derangere_v-k := predsort.
      derelinkvere_v-k := predsort.
      derivere_v-k := predsort.
      desarmere_v-k := predsort.
      desavuere_v-k := predsort.
      desentralisere_v-k := predsort.
      desertere_v-k := predsort.
      desidere_v-k := predsort.
      designe_v-k := predsort.
      desillusjonere_v-k := predsort.
      desimere_v-k := predsort.
      desinfisere_v-k := predsort.
      desinformere_v-k := predsort.
      desintegrere_v-k := predsort.
      desoksidere_v-k := predsort.
      desorganisere_v-k := predsort.
      desorientere_v-k := predsort.
      destillere_v-k := predsort.
      destruere_v-k := predsort.
      detaljbehandle_v-k := predsort.
      detaljere_v-k := predsort.
      detasjere_v-k := predsort.
      determinere_v-k := predsort.
      detonere_v-k := predsort.
      detronisere_v-k := predsort.
      dette_v-k := predsort.
      devaluere_v-k := predsort.
      deviere_v-k := predsort.
      diagnostisere_v-k := predsort.
      dialysere_v-k := predsort.
      die_v-k := predsort.
      differensiere_v-k := predsort.
      differere_v-k := predsort.
      diffundere_v-k := predsort.
      diftongere_v-k := predsort.
      digge_v-k := predsort.
      digitalisere_v-k := predsort.
      dikke_v-k := predsort.
      dikte_v-k := predsort.
      diktere_v-k := predsort.
      dille_v-k := predsort.
      dilte_v-k := predsort.
      dimensjonere_v-k := predsort.
      dimittere_v-k := predsort.
      dimme_v-k := predsort.
      dinere_v-k := predsort.
      dingle_v-k := predsort.
      direktesende_v-k := predsort.
      dirigere_v-k := predsort.
      dirke_v-k := predsort.
      dirre_v-k := predsort.
      disiplinere_v-k := predsort.
      diske_v-k := predsort.
      diskontere_v-k := predsort.
      diskreditere_v-k := predsort.
      diskriminere_v-k := predsort.
      diskutere_v-k := predsort.
      diskvalifisere_v-k := predsort.
      dispasjere_v-k := predsort.
      dispensere_v-k := predsort.
      disponere_v-k := predsort.
      disputere_v-k := predsort.
      dissekere_v-k := predsort.
      disse_v-k := predsort.
      dissentere_v-k := predsort.
      dissimilere_v-k := predsort.
      dissonere_v-k := predsort.
      dissosiere_v-k := predsort.
      distansere_v-k := predsort.
      distingvere_v-k := predsort.
      distrahere_v-k := predsort.
      distribuere_v-k := predsort.
      divergere_v-k := predsort.
      divertere_v-k := predsort.
      dividere_v-k := predsort.
      djupetse_v-k := predsort.
      djupfryse_v-k := predsort.
      djupne_v-k := predsort.
      doble_v-k := predsort.
      dogge_v-k := predsort.
      dokke_v-k := predsort.
      dokksette_v-k := predsort.
      dø_v-k := predsort.
      doktorere_v-k := predsort.
      dokumentere_v-k := predsort.
      dølge_v-k := predsort.
      dolke_v-k := predsort.
      domfelle_v-k := predsort.
      dominere_v-k := predsort.
      domisiliere_v-k := predsort.
      dømme_v-k := predsort.
      donere_v-k := predsort.
      dønne_v-k := predsort.
      dope_v-k := predsort.
      døpe_v-k := predsort.
      dopingavsløre_v-k := predsort.
      dopingta_v-k := predsort.
      dore_v-k := predsort.
      dorge_v-k := predsort.
      dorme_v-k := predsort.
      døse_v-k := predsort.
      dosere_v-k := predsort.
      dossere_v-k := predsort.
      dotere_v-k := predsort.
      dovne_v-k := predsort.
      døye_v-k := predsort.
      døyve_v-k := predsort.
      drage_v-k := predsort.
      drages_v-k := predsort.
      dra_v-k := predsort.
      dramatisere_v-k := predsort.
      drapere_v-k := predsort.
      drapsdømme_v-k := predsort.
      drapstrue_v-k := predsort.
      drasjere_v-k := predsort.
      dras_v-k := predsort.
      drasse_v-k := predsort.
      dratte_v-k := predsort.
      dregge_v-k := predsort.
      dreie_v-k := predsort.
      drene_v-k := predsort.
      drenere_v-k := predsort.
      drepe_v-k := predsort.
      dresse_v-k := predsort.
      dressere_v-k := predsort.
      drette_v-k := predsort.
      drible_v-k := predsort.
      drifte_v-k := predsort.
      drikke_v-k := predsort.
      drille_v-k := predsort.
      driste_v-k := predsort.
      drite_v-k := predsort.
      drive_v-k := predsort.
      drodle_v-k := predsort.
      drøfte_v-k := predsort.
      drømme_v-k := predsort.
      drønne_v-k := predsort.
      droppe_v-k := predsort.
      drøse_v-k := predsort.
      drøvtygge_v-k := predsort.
      drøye_v-k := predsort.
      drukne_v-k := predsort.
      dryge_v-k := predsort.
      dryppe_v-k := predsort.
      drypptørke_v-k := predsort.
      drypptørre_v-k := predsort.
      drysse_v-k := predsort.
      dubbe_v-k := predsort.
      dublere_v-k := predsort.
      duellere_v-k := predsort.
      dufte_v-k := predsort.
      duge_v-k := predsort.
      dugge_v-k := predsort.
      duke_v-k := predsort.
      dukke_v-k := predsort.
      dulle_v-k := predsort.
      dulme_v-k := predsort.
      dulte_v-k := predsort.
      dumme_v-k := predsort.
      dumpe_v-k := predsort.
      dundre_v-k := predsort.
      dunge_v-k := predsort.
      dunke_v-k := predsort.
      dunste_v-k := predsort.
      dupere_v-k := predsort.
      duplisere_v-k := predsort.
      duppe_v-k := predsort.
      dure_v-k := predsort.
      durksy_v-k := predsort.
      duse_v-k := predsort.
      dusje_v-k := predsort.
      duskregne_v-k := predsort.
      dutte_v-k := predsort.
      duve_v-k := predsort.
      dvele_v-k := predsort.
      dykke_v-k := predsort.
      dy_v-k := predsort.
      dyktiggjøre_v-k := predsort.
      dynge_v-k := predsort.
      dynke_v-k := predsort.
      dypetse_v-k := predsort.
      dypfryse_v-k := predsort.
      dyppe_v-k := predsort.
      dyrke_v-k := predsort.
      dysse_v-k := predsort.
      dytte_v-k := predsort.
      ebbe_v-k := predsort.
      edfeste_v-k := predsort.
      effektivisere_v-k := predsort.
      effektuere_v-k := predsort.
      egalisere_v-k := predsort.
      egenkomponere_v-k := predsort.
      egge_v-k := predsort.
      egle_v-k := predsort.
      egne_v-k := predsort.
      eie_v-k := predsort.
      eime_v-k := predsort.
      eire_v-k := predsort.
      ejakulere_v-k := predsort.
      eksaminere_v-k := predsort.
      eksegere_v-k := predsort.
      eksekvere_v-k := predsort.
      eksellere_v-k := predsort.
      eksemplifisere_v-k := predsort.
      ekserpere_v-k := predsort.
      ekshalere_v-k := predsort.
      eksistere_v-k := predsort.
      eksklamere_v-k := predsort.
      ekskludere_v-k := predsort.
      ekskommunisere_v-k := predsort.
      ekspandere_v-k := predsort.
      ekspatriere_v-k := predsort.
      ekspedere_v-k := predsort.
      eksperimentere_v-k := predsort.
      ekspirere_v-k := predsort.
      eksplisittere_v-k := predsort.
      eksploatere_v-k := predsort.
      eksplodere_v-k := predsort.
      eksponere_v-k := predsort.
      eksportere_v-k := predsort.
      ekspropriere_v-k := predsort.
      ekstemporere_v-k := predsort.
      ekstensivere_v-k := predsort.
      ekstrahere_v-k := predsort.
      ekstrapolere_v-k := predsort.
      ekte_v-k := predsort.
      ektevie_v-k := predsort.
      ekvipere_v-k := predsort.
      ekvivalere_v-k := predsort.
      elde_v-k := predsort.
      eldes_v-k := predsort.
      elektrifisere_v-k := predsort.
      elektrisere_v-k := predsort.
      elevere_v-k := predsort.
      eligere_v-k := predsort.
      eliminere_v-k := predsort.
      eloksere_v-k := predsort.
      elske_v-k := predsort.
      elte_v-k := predsort.
      eludere_v-k := predsort.
      emaljere_v-k := predsort.
      emansipere_v-k := predsort.
      emballere_v-k := predsort.
      embarkere_v-k := predsort.
      emigrere_v-k := predsort.
      emittere_v-k := predsort.
      employere_v-k := predsort.
      emulgere_v-k := predsort.
      ende_v-k := predsort.
      endevende_v-k := predsort.
      endossere_v-k := predsort.
      endre_v-k := predsort.
      enervere_v-k := predsort.
      enes_v-k := predsort.
      engasjere_v-k := predsort.
      engste_v-k := predsort.
      ense_v-k := predsort.
      ensilere_v-k := predsort.
      ensrette_v-k := predsort.
      entre_v-k := predsort.
      envegsregulere_v-k := predsort.
      enveisregulere_v-k := predsort.
      erfare_v-k := predsort.
      ergre_v-k := predsort.
      erholde_v-k := predsort.
      erigere_v-k := predsort.
      erindre_v-k := predsort.
      erkjenne_v-k := predsort.
      erklære_v-k := predsort.
      erlegge_v-k := predsort.
      ernære_v-k := predsort.
      erobre_v-k := predsort.
      erodere_v-k := predsort.
      erotisere_v-k := predsort.
      erstatte_v-k := predsort.
      erte_v-k := predsort.
      erverve_v-k := predsort.
      ese_v-k := predsort.
      eskalere_v-k := predsort.
      eskamotere_v-k := predsort.
      eskapere_v-k := predsort.
      eskortere_v-k := predsort.
      esle_v-k := predsort.
      estetisere_v-k := predsort.
      estimere_v-k := predsort.
      etablere_v-k := predsort.
      etikettere_v-k := predsort.
      etle_v-k := predsort.
      etse_v-k := predsort.
      etteranmelde_v-k := predsort.
      etterape_v-k := predsort.
      etterbehandle_v-k := predsort.
      etterbetale_v-k := predsort.
      etterfølge_v-k := predsort.
      etterforske_v-k := predsort.
      etterfylle_v-k := predsort.
      ettergi_v-k := predsort.
      ettergjøre_v-k := predsort.
      etterisolere_v-k := predsort.
      etterkomme_v-k := predsort.
      etterlate_v-k := predsort.
      etterleve_v-k := predsort.
      etterligne_v-k := predsort.
      etterlikne_v-k := predsort.
      etterlyse_v-k := predsort.
      etterplapre_v-k := predsort.
      etterprøve_v-k := predsort.
      etterse_v-k := predsort.
      ettersende_v-k := predsort.
      etterslokke_v-k := predsort.
      etterslukke_v-k := predsort.
      ettersøke_v-k := predsort.
      etterspore_v-k := predsort.
      etterstrebe_v-k := predsort.
      ettersynkronisere_v-k := predsort.
      ettertrakte_v-k := predsort.
      europeisere_v-k := predsort.
      evakuere_v-k := predsort.
      evaluere_v-k := predsort.
      evangelisere_v-k := predsort.
      evaporere_v-k := predsort.
      eve_v-k := predsort.
      evne_v-k := predsort.
      exe_v-k := predsort.
      fable_v-k := predsort.
      fabrikkere_v-k := predsort.
      fabulere_v-k := predsort.
      fæle_v-k := predsort.
      fagne_v-k := predsort.
      fakke_v-k := predsort.
      få_v-k := predsort.
      fakse_v-k := predsort.
      faksimilere_v-k := predsort.
      faktorisere_v-k := predsort.
      fakturere_v-k := predsort.
      falby_v-k := predsort.
      falde_v-k := predsort.
      falle_v-k := predsort.
      fallere_v-k := predsort.
      falme_v-k := predsort.
      false_v-k := predsort.
      falsifisere_v-k := predsort.
      famle_v-k := predsort.
      famne_v-k := predsort.
      fange_v-k := predsort.
      fangste_v-k := predsort.
      fantasere_v-k := predsort.
      fantegå_v-k := predsort.
      fare-v_v-k := predsort.
      farge_v-k := predsort.
      farsere_v-k := predsort.
      farte_v-k := predsort.
      fascinere_v-k := predsort.
      fase_v-k := predsort.
      fasettere_v-k := predsort.
      fasle_v-k := predsort.
      fasongere_v-k := predsort.
      faste_v-k := predsort.
      fastholde_v-k := predsort.
      fastlåse_v-k := predsort.
      fastlegge_v-k := predsort.
      fastne_v-k := predsort.
      fastsette_v-k := predsort.
      fastslå_v-k := predsort.
      fastspenne_v-k := predsort.
      fate_v-k := predsort.
      fatle_v-k := predsort.
      fatte_v-k := predsort.
      fattes_v-k := predsort.
      favne_v-k := predsort.
      favorisere_v-k := predsort.
      feide_v-k := predsort.
      feie_v-k := predsort.
      feilbedømme_v-k := predsort.
      feilbehandle_v-k := predsort.
      feile_v-k := predsort.
      feilkoble_v-k := predsort.
      feilvurdere_v-k := predsort.
      feire_v-k := predsort.
      feite_v-k := predsort.
      feitne_v-k := predsort.
      fekte_v-k := predsort.
      felle_v-k := predsort.
      femdoble_v-k := predsort.
      feminisere_v-k := predsort.
      fenge_v-k := predsort.
      fengsle_v-k := predsort.
      ferde_v-k := predsort.
      ferdes_v-k := predsort.
      ferdigbehandle_v-k := predsort.
      ferdigspille_v-k := predsort.
      ferdigstille_v-k := predsort.
      ferge_v-k := predsort.
      feriere_v-k := predsort.
      ferje-v_v-k := predsort.
      ferme_v-k := predsort.
      fermentere_v-k := predsort.
      fernissere_v-k := predsort.
      ferske_v-k := predsort.
      feste_v-k := predsort.
      festne_v-k := predsort.
      fete_v-k := predsort.
      fetere_v-k := predsort.
      fetne_v-k := predsort.
      fiffe_v-k := predsort.
      fighte_v-k := predsort.
      figurere_v-k := predsort.
      fike_v-k := predsort.
      fikle_v-k := predsort.
      fikse_v-k := predsort.
      fiksere_v-k := predsort.
      file_v-k := predsort.
      filere_v-k := predsort.
      filetere_v-k := predsort.
      filmatisere_v-k := predsort.
      filme_v-k := predsort.
      filosofere_v-k := predsort.
      filtre_v-k := predsort.
      filtrere_v-k := predsort.
      finansiere_v-k := predsort.
      finere_v-k := predsort.
      finfordele_v-k := predsort.
      fingere_v-k := predsort.
      fingre_v-k := predsort.
      finhakke_v-k := predsort.
      finhøvle_v-k := predsort.
      fininnstille_v-k := predsort.
      finkjemme_v-k := predsort.
      finmale_v-k := predsort.
      finne_v-k := predsort.
      finnes_v-k := predsort.
      finpusse_v-k := predsort.
      finregne_v-k := predsort.
      finrekne_v-k := predsort.
      finsikte_v-k := predsort.
      finte_v-k := predsort.
      fintelle_v-k := predsort.
      firdoble_v-k := predsort.
      firedoble_v-k := predsort.
      fire_v-k := predsort.
      fise_v-k := predsort.
      fisjonere_v-k := predsort.
      fiske_v-k := predsort.
      fisle_v-k := predsort.
      fjære_v-k := predsort.
      fjamse_v-k := predsort.
      fjase_v-k := predsort.
      fjelge_v-k := predsort.
      fjerne_v-k := predsort.
      fjernstyre_v-k := predsort.
      fjerte_v-k := predsort.
      fjeske_v-k := predsort.
      fjetre_v-k := predsort.
      fjolle_v-k := predsort.
      fjøre_v-k := predsort.
      fjuske_v-k := predsort.
      flæ_v-k := predsort.
      flære_v-k := predsort.
      flagge_v-k := predsort.
      flagre_v-k := predsort.
      flåhakke_v-k := predsort.
      flake_v-k := predsort.
      flakke_v-k := predsort.
      flakne_v-k := predsort.
      flå_v-k := predsort.
      flakse_v-k := predsort.
      flambere_v-k := predsort.
      flamme_v-k := predsort.
      flane_v-k := predsort.
      flanere_v-k := predsort.
      flankere_v-k := predsort.
      flare_v-k := predsort.
      flåse_v-k := predsort.
      flaske_v-k := predsort.
      flasse_v-k := predsort.
      flate_v-k := predsort.
      flatne_v-k := predsort.
      flattere_v-k := predsort.
      flaue_v-k := predsort.
      fleipe_v-k := predsort.
      flekke_v-k := predsort.
      flekse_v-k := predsort.
      flektere_v-k := predsort.
      flenge_v-k := predsort.
      flense_v-k := predsort.
      flerdoble_v-k := predsort.
      flerre_v-k := predsort.
      flette_v-k := predsort.
      flikke_v-k := predsort.
      fli_v-k := predsort.
      flimre_v-k := predsort.
      flippe_v-k := predsort.
      flire_v-k := predsort.
      flise_v-k := predsort.
      fliselegge_v-k := predsort.
      flislegge_v-k := predsort.
      floke_v-k := predsort.
      flokke_v-k := predsort.
      flø_v-k := predsort.
      flokse_v-k := predsort.
      flomme_v-k := predsort.
      flømme_v-k := predsort.
      floppe_v-k := predsort.
      floragrafere_v-k := predsort.
      florere_v-k := predsort.
      flørte_v-k := predsort.
      fløte_v-k := predsort.
      flotte_v-k := predsort.
      fløyte_v-k := predsort.
      flukte_v-k := predsort.
      fluktuere_v-k := predsort.
      fluorescere_v-k := predsort.
      fluoridere_v-k := predsort.
      fluorisere_v-k := predsort.
      flyge_v-k := predsort.
      fly-v_v-k := predsort.
      flykte_v-k := predsort.
      flyte_v-k := predsort.
      flytte_v-k := predsort.
      fnise_v-k := predsort.
      fnyse_v-k := predsort.
      føde_v-k := predsort.
      fø_v-k := predsort.
      fokusere_v-k := predsort.
      folde_v-k := predsort.
      fole_v-k := predsort.
      føle_v-k := predsort.
      følge_v-k := predsort.
      følgje_v-k := predsort.
      foliere_v-k := predsort.
      folke_v-k := predsort.
      folle_v-k := predsort.
      følle_v-k := predsort.
      folne_v-k := predsort.
      fomle_v-k := predsort.
      føne_v-k := predsort.
      fonne_v-k := predsort.
      forære_v-k := predsort.
      forakte_v-k := predsort.
      forandre_v-k := predsort.
      forankre_v-k := predsort.
      foranledige_v-k := predsort.
      foranstalte_v-k := predsort.
      forarbeide_v-k := predsort.
      forarge_v-k := predsort.
      forarme_v-k := predsort.
      forårsake_v-k := predsort.
      forbanne_v-k := predsort.
      forbarme_v-k := predsort.
      forbause_v-k := predsort.
      forbedre_v-k := predsort.
      forbeholde_v-k := predsort.
      forbeine_v-k := predsort.
      forbene_v-k := predsort.
      forberede_v-k := predsort.
      forbigå_v-k := predsort.
      forbinde_v-k := predsort.
      forbitre_v-k := predsort.
      forblende_v-k := predsort.
      forbli_v-k := predsort.
      forblinde_v-k := predsort.
      forbløffe_v-k := predsort.
      forblø_v-k := predsort.
      forbrenne_v-k := predsort.
      forbruke_v-k := predsort.
      forbryte_v-k := predsort.
      forby_v-k := predsort.
      forbytte_v-k := predsort.
      fordampe_v-k := predsort.
      fordanske_v-k := predsort.
      fordele_v-k := predsort.
      forderve_v-k := predsort.
      fordjupe_v-k := predsort.
      fordoble_v-k := predsort.
      fordømme_v-k := predsort.
      fordøye_v-k := predsort.
      fordra_v-k := predsort.
      fordreie_v-k := predsort.
      fordre_v-k := predsort.
      fordriste_v-k := predsort.
      fordrive_v-k := predsort.
      fordufte_v-k := predsort.
      fordumme_v-k := predsort.
      fordundre_v-k := predsort.
      fordunkle_v-k := predsort.
      fordunste_v-k := predsort.
      fordype_v-k := predsort.
      fordyre_v-k := predsort.
      forebringe_v-k := predsort.
      førebu_v-k := predsort.
      forebygge_v-k := predsort.
      foredle_v-k := predsort.
      foredra_v-k := predsort.
      forefalle_v-k := predsort.
      forefinnes_v-k := predsort.
      foregå_v-k := predsort.
      foregi_v-k := predsort.
      foregripe_v-k := predsort.
      foreholde_v-k := predsort.
      forekomme_v-k := predsort.
      fóre_v-k := predsort.
      fôre_v-k := predsort.
      føre_v-k := predsort.
      foreldes_v-k := predsort.
      forelegge_v-k := predsort.
      forelese_v-k := predsort.
      foreligge_v-k := predsort.
      forelske_v-k := predsort.
      forene_v-k := predsort.
      forenkle_v-k := predsort.
      foresette_v-k := predsort.
      foresi_v-k := predsort.
      foreskrive_v-k := predsort.
      foreslå_v-k := predsort.
      forespeile_v-k := predsort.
      forestå_v-k := predsort.
      forestille_v-k := predsort.
      foresveve_v-k := predsort.
      foreta_v-k := predsort.
      foretrekke_v-k := predsort.
      forevige_v-k := predsort.
      forevise_v-k := predsort.
      forfalle_v-k := predsort.
      forfalske_v-k := predsort.
      forfatte_v-k := predsort.
      forfeile_v-k := predsort.
      forfekte_v-k := predsort.
      forferde_v-k := predsort.
      forferdige_v-k := predsort.
      forfine_v-k := predsort.
      forfjamse_v-k := predsort.
      forflate_v-k := predsort.
      forflere_v-k := predsort.
      forflytte_v-k := predsort.
      forfølge_v-k := predsort.
      forfordele_v-k := predsort.
      forføre_v-k := predsort.
      forføye_v-k := predsort.
      forfremme_v-k := predsort.
      forfriske_v-k := predsort.
      forfryse_v-k := predsort.
      forgå_v-k := predsort.
      forgape_v-k := predsort.
      forgasse_v-k := predsort.
      forgifte_v-k := predsort.
      forgi_v-k := predsort.
      forgjelde_v-k := predsort.
      forgjøre_v-k := predsort.
      forglemme_v-k := predsort.
      forglømme_v-k := predsort.
      forgreine_v-k := predsort.
      forgrene_v-k := predsort.
      forgripe_v-k := predsort.
      forgrove_v-k := predsort.
      forgubbe_v-k := predsort.
      forgude_v-k := predsort.
      forgylle_v-k := predsort.
      forhale_v-k := predsort.
      forhandle_v-k := predsort.
      forhandsbestille_v-k := predsort.
      forhåndsbestille_v-k := predsort.
      forhandsomtale_v-k := predsort.
      forhåndsomtale_v-k := predsort.
      forhandsstemme_v-k := predsort.
      forhåndsstemme_v-k := predsort.
      forhåne_v-k := predsort.
      forhaste_v-k := predsort.
      forhekse_v-k := predsort.
      forherde_v-k := predsort.
      forherlige_v-k := predsort.
      forhindre_v-k := predsort.
      forholde_v-k := predsort.
      forhøre_v-k := predsort.
      forhøye_v-k := predsort.
      forhyre_v-k := predsort.
      forivre_v-k := predsort.
      forjette_v-k := predsort.
      forkalke_v-k := predsort.
      forkaste_v-k := predsort.
      forkjæle_v-k := predsort.
      forkjetre_v-k := predsort.
      forkjøle_v-k := predsort.
      forkjøpe_v-k := predsort.
      forklare_v-k := predsort.
      forkleine_v-k := predsort.
      forkle_v-k := predsort.
      forkludre_v-k := predsort.
      forkobre_v-k := predsort.
      forkomme_v-k := predsort.
      forkopre_v-k := predsort.
      forkorte_v-k := predsort.
      forkromme_v-k := predsort.
      forkue_v-k := predsort.
      forkulle_v-k := predsort.
      forkvakle_v-k := predsort.
      forkynne_v-k := predsort.
      forlange_v-k := predsort.
      forlate_v-k := predsort.
      forlede_v-k := predsort.
      forlegge_v-k := predsort.
      forlenge_v-k := predsort.
      forlese_v-k := predsort.
      forlibe_v-k := predsort.
      forlike_v-k := predsort.
      forlise_v-k := predsort.
      forløfte_v-k := predsort.
      forlokke_v-k := predsort.
      forløpe_v-k := predsort.
      forløse_v-k := predsort.
      forlove_v-k := predsort.
      forløyse_v-k := predsort.
      forlyde_v-k := predsort.
      forlyste_v-k := predsort.
      formæle_v-k := predsort.
      formå_v-k := predsort.
      formale_v-k := predsort.
      formalisere_v-k := predsort.
      formane_v-k := predsort.
      formaste_v-k := predsort.
      formatere_v-k := predsort.
      forme_v-k := predsort.
      formene_v-k := predsort.
      formere_v-k := predsort.
      formgi_v-k := predsort.
      formidle_v-k := predsort.
      formilde_v-k := predsort.
      forminske_v-k := predsort.
      formode_v-k := predsort.
      formolde_v-k := predsort.
      formørke_v-k := predsort.
      formulde_v-k := predsort.
      formulere_v-k := predsort.
      formumme_v-k := predsort.
      fornærme_v-k := predsort.
      fornedre_v-k := predsort.
      fornekte_v-k := predsort.
      fornemme_v-k := predsort.
      fornikle_v-k := predsort.
      fornorske_v-k := predsort.
      fornye_v-k := predsort.
      forøde_v-k := predsort.
      forøke_v-k := predsort.
      forordne_v-k := predsort.
      forøve_v-k := predsort.
      forpakte_v-k := predsort.
      forpeste_v-k := predsort.
      forplante_v-k := predsort.
      forpleie_v-k := predsort.
      forplikte_v-k := predsort.
      forprosjektere_v-k := predsort.
      forpuppe_v-k := predsort.
      forpurre_v-k := predsort.
      forråde_v-k := predsort.
      forråe_v-k := predsort.
      forregne_v-k := predsort.
      forrekne_v-k := predsort.
      forrente_v-k := predsort.
      forrette_v-k := predsort.
      forringe_v-k := predsort.
      forrykke_v-k := predsort.
      forsage_v-k := predsort.
      forsake_v-k := predsort.
      forsegle_v-k := predsort.
      forse_v-k := predsort.
      forsenke_v-k := predsort.
      forsere_v-k := predsort.
      forsikre_v-k := predsort.
      forsimple_v-k := predsort.
      forsinke_v-k := predsort.
      forsire_v-k := predsort.
      forsjofle_v-k := predsort.
      forskale_v-k := predsort.
      forskåne_v-k := predsort.
      forskanse_v-k := predsort.
      forske_v-k := predsort.
      forskjellsbehandle_v-k := predsort.
      forskjønne_v-k := predsort.
      forskottere_v-k := predsort.
      forskrekke_v-k := predsort.
      forskreve_v-k := predsort.
      forskrive_v-k := predsort.
      forskusle_v-k := predsort.
      forskuttere_v-k := predsort.
      forskylde_v-k := predsort.
      forskyve_v-k := predsort.
      forslå_v-k := predsort.
      forsluke_v-k := predsort.
      forslumme_v-k := predsort.
      forsmå_v-k := predsort.
      forsnakke_v-k := predsort.
      forsnevre_v-k := predsort.
      forsøke_v-k := predsort.
      forsølve_v-k := predsort.
      forsømme_v-k := predsort.
      forsone_v-k := predsort.
      forsøple_v-k := predsort.
      forsørge_v-k := predsort.
      forsøte_v-k := predsort.
      forsove_v-k := predsort.
      forspenne_v-k := predsort.
      forspille_v-k := predsort.
      forspise_v-k := predsort.
      forspørre_v-k := predsort.
      forstå_v-k := predsort.
      forsterke_v-k := predsort.
      forstikke_v-k := predsort.
      forstille_v-k := predsort.
      forstørre_v-k := predsort.
      forstøte_v-k := predsort.
      forstøtte_v-k := predsort.
      forstøve_v-k := predsort.
      forstrekke_v-k := predsort.
      forstue_v-k := predsort.
      forstumme_v-k := predsort.
      forstuve_v-k := predsort.
      forstyrre_v-k := predsort.
      forsumpe_v-k := predsort.
      forsure_v-k := predsort.
      forsvare_v-k := predsort.
      forsvenske_v-k := predsort.
      forsverge_v-k := predsort.
      forsverje_v-k := predsort.
      forsvinne_v-k := predsort.
      forsynde_v-k := predsort.
      forsyne_v-k := predsort.
      fortære_v-k := predsort.
      forta_v-k := predsort.
      fortape_v-k := predsort.
      fortegne_v-k := predsort.
      forte_v-k := predsort.
      fortelle_v-k := predsort.
      fortenke_v-k := predsort.
      fortette_v-k := predsort.
      førtidspensjonere_v-k := predsort.
      fortie_v-k := predsort.
      fortinne_v-k := predsort.
      fortjene_v-k := predsort.
      fortne_v-k := predsort.
      fortolke_v-k := predsort.
      fortolle_v-k := predsort.
      fortone_v-k := predsort.
      fortørne_v-k := predsort.
      fortorve_v-k := predsort.
      fortøye_v-k := predsort.
      fortrekke_v-k := predsort.
      fortrenge_v-k := predsort.
      fortrolle_v-k := predsort.
      fortrøste_v-k := predsort.
      fortrylle_v-k := predsort.
      fortryte_v-k := predsort.
      fortsette_v-k := predsort.
      fortvile_v-k := predsort.
      fortykke_v-k := predsort.
      fortynne_v-k := predsort.
      foruleilige_v-k := predsort.
      forulempe_v-k := predsort.
      forulykke_v-k := predsort.
      forundre_v-k := predsort.
      forunne_v-k := predsort.
      forurense_v-k := predsort.
      forurette_v-k := predsort.
      forurolige_v-k := predsort.
      forutberegne_v-k := predsort.
      forutberekne_v-k := predsort.
      forutbestemme_v-k := predsort.
      forutse_v-k := predsort.
      forutsette_v-k := predsort.
      forutsi_v-k := predsort.
      forutskikke_v-k := predsort.
      forvalte_v-k := predsort.
      forvandle_v-k := predsort.
      forvanske_v-k := predsort.
      forvare_v-k := predsort.
      forvede_v-k := predsort.
      forveksle_v-k := predsort.
      forvelle_v-k := predsort.
      forvenne_v-k := predsort.
      forvente_v-k := predsort.
      forverre_v-k := predsort.
      forville_v-k := predsort.
      forvirre_v-k := predsort.
      forvise_v-k := predsort.
      forvisse_v-k := predsort.
      forvitre_v-k := predsort.
      forvolde_v-k := predsort.
      forvrenge_v-k := predsort.
      forvri_v-k := predsort.
      forwarde_v-k := predsort.
      forynge_v-k := predsort.
      fosforescere_v-k := predsort.
      fosse_v-k := predsort.
      fosskoke_v-k := predsort.
      fossregne_v-k := predsort.
      fossro_v-k := predsort.
      fostre_v-k := predsort.
      fote_v-k := predsort.
      fotografere_v-k := predsort.
      fotokopiere_v-k := predsort.
      føye_v-k := predsort.
      føyke_v-k := predsort.
      føyse_v-k := predsort.
      frabe_v-k := predsort.
      frabeordre_v-k := predsort.
      fråde_v-k := predsort.
      fradømme_v-k := predsort.
      frafalle_v-k := predsort.
      fraflytte_v-k := predsort.
      fragå_v-k := predsort.
      fragmentere_v-k := predsort.
      frakjenne_v-k := predsort.
      frakoble_v-k := predsort.
      frakople_v-k := predsort.
      fraksjonere_v-k := predsort.
      frakte_v-k := predsort.
      fralegge_v-k := predsort.
      fralokke_v-k := predsort.
      frambringe_v-k := predsort.
      framby_v-k := predsort.
      framdatere_v-k := predsort.
      framelske_v-k := predsort.
      framføre_v-k := predsort.
      framgå_v-k := predsort.
      framheve_v-k := predsort.
      framholde_v-k := predsort.
      framkalle_v-k := predsort.
      framkaste_v-k := predsort.
      framkomme_v-k := predsort.
      framlegge_v-k := predsort.
      framleie_v-k := predsort.
      framsette_v-k := predsort.
      framsi_v-k := predsort.
      framskaffe_v-k := predsort.
      framskrive_v-k := predsort.
      framskynde_v-k := predsort.
      framskyte_v-k := predsort.
      framsnakke_v-k := predsort.
      framstå_v-k := predsort.
      framstille_v-k := predsort.
      framtre_v-k := predsort.
      framtvinge_v-k := predsort.
      framvise_v-k := predsort.
      franarre_v-k := predsort.
      frankere_v-k := predsort.
      frappere_v-k := predsort.
      fraråde_v-k := predsort.
      frarå_v-k := predsort.
      frarane_v-k := predsort.
      frarøve_v-k := predsort.
      fraselge_v-k := predsort.
      frasere_v-k := predsort.
      frasi_v-k := predsort.
      fraskille_v-k := predsort.
      fraskjøte_v-k := predsort.
      fraskrive_v-k := predsort.
      fråsse_v-k := predsort.
      frastå_v-k := predsort.
      frastjele_v-k := predsort.
      frastøte_v-k := predsort.
      frata_v-k := predsort.
      fraternisere_v-k := predsort.
      fratre_v-k := predsort.
      fråtse_v-k := predsort.
      fravike_v-k := predsort.
      fravriste_v-k := predsort.
      frede_v-k := predsort.
      frege_v-k := predsort.
      frekventere_v-k := predsort.
      frelse_v-k := predsort.
      frembringe_v-k := predsort.
      fremby_v-k := predsort.
      fremdatere_v-k := predsort.
      fremelske_v-k := predsort.
      fremføre_v-k := predsort.
      fremforhandle_v-k := predsort.
      fremgå_v-k := predsort.
      fremheve_v-k := predsort.
      fremholde_v-k := predsort.
      fremkalle_v-k := predsort.
      fremkaste_v-k := predsort.
      fremkomme_v-k := predsort.
      fremlegge_v-k := predsort.
      fremleie_v-k := predsort.
      fremmedgjøre_v-k := predsort.
      fremme_v-k := predsort.
      fremmendgjøre_v-k := predsort.
      fremsette_v-k := predsort.
      fremsi_v-k := predsort.
      fremskaffe_v-k := predsort.
      fremskrive_v-k := predsort.
      fremskynde_v-k := predsort.
      fremskyte_v-k := predsort.
      fremsnakke_v-k := predsort.
      fremstå_v-k := predsort.
      fremstille_v-k := predsort.
      fremtre_v-k := predsort.
      fremtvinge_v-k := predsort.
      fremvise_v-k := predsort.
      frese_v-k := predsort.
      freste_v-k := predsort.
      frette_v-k := predsort.
      frifinne_v-k := predsort.
      frigi_v-k := predsort.
      frigjøre_v-k := predsort.
      frikjenne_v-k := predsort.
      fri_v-k := predsort.
      frilanse_v-k := predsort.
      friliste_v-k := predsort.
      frisere_v-k := predsort.
      friske_v-k := predsort.
      friskmelde_v-k := predsort.
      friskne_v-k := predsort.
      friste_v-k := predsort.
      frita_v-k := predsort.
      fritere_v-k := predsort.
      fritte_v-k := predsort.
      frø_v-k := predsort.
      frondere_v-k := predsort.
      frøse_v-k := predsort.
      frottere_v-k := predsort.
      frukte_v-k := predsort.
      frustrere_v-k := predsort.
      fryde_v-k := predsort.
      frykte_v-k := predsort.
      frynse_v-k := predsort.
      fryse_v-k := predsort.
      fuge_v-k := predsort.
      fukte_v-k := predsort.
      fullbringe_v-k := predsort.
      fullbyrde_v-k := predsort.
      fullende_v-k := predsort.
      fullføre_v-k := predsort.
      fullrose_v-k := predsort.
      fulltakke_v-k := predsort.
      fundamentere_v-k := predsort.
      fundere_v-k := predsort.
      fungere_v-k := predsort.
      funke_v-k := predsort.
      funkle_v-k := predsort.
      funksjonere_v-k := predsort.
      furasjere_v-k := predsort.
      fure_v-k := predsort.
      furnere_v-k := predsort.
      furte_v-k := predsort.
      fusjonere_v-k := predsort.
      fuske_v-k := predsort.
      fyke_v-k := predsort.
      fylke_v-k := predsort.
      fyllekjøre_v-k := predsort.
      fylle_v-k := predsort.
      fyre_v-k := predsort.
      fyse_v-k := predsort.
      gafle_v-k := predsort.
      gagne_v-k := predsort.
      gakke_v-k := predsort.
      gå_v-k := predsort.
      galdre_v-k := predsort.
      gale_v-k := predsort.
      galoppere_v-k := predsort.
      galvanisere_v-k := predsort.
      gamble_v-k := predsort.
      gande_v-k := predsort.
      gane_v-k := predsort.
      gange_v-k := predsort.
      gante_v-k := predsort.
      gantes_v-k := predsort.
      gape_v-k := predsort.
      gaple_v-k := predsort.
      gapskratte_v-k := predsort.
      garantere_v-k := predsort.
      gardere_v-k := predsort.
      garne_v-k := predsort.
      garnere_v-k := predsort.
      garnisonere_v-k := predsort.
      garpe_v-k := predsort.
      garve_v-k := predsort.
      gasjere_v-k := predsort.
      gasse_v-k := predsort.
      gauke_v-k := predsort.
      gaule_v-k := predsort.
      gaume_v-k := predsort.
      geberde_v-k := predsort.
      geipe_v-k := predsort.
      geleide_v-k := predsort.
      geminere_v-k := predsort.
      generalisere_v-k := predsort.
      generere_v-k := predsort.
      genierklære_v-k := predsort.
      geråde_v-k := predsort.
      gerere_v-k := predsort.
      germanisere_v-k := predsort.
      gestalte_v-k := predsort.
      gestikulere_v-k := predsort.
      gidde_v-k := predsort.
      gifte_v-k := predsort.
      gigge_v-k := predsort.
      gi_v-k := predsort.
      gildre_v-k := predsort.
      giljotinere_v-k := predsort.
      gimpe_v-k := predsort.
      gipse_v-k := predsort.
      gire_v-k := predsort.
      girere_v-k := predsort.
      gisne_v-k := predsort.
      gispe_v-k := predsort.
      gisse_v-k := predsort.
      gitre_v-k := predsort.
      gjære_v-k := predsort.
      gjalle_v-k := predsort.
      gjekke_v-k := predsort.
      gjelde_v-k := predsort.
      gjelle_v-k := predsort.
      gjemme_v-k := predsort.
      gjenåpne_v-k := predsort.
      gjenbruke_v-k := predsort.
      gjendrive_v-k := predsort.
      gjenerobre_v-k := predsort.
      gjenfinne_v-k := predsort.
      gjenføde_v-k := predsort.
      gjenforene_v-k := predsort.
      gjenforsikre_v-k := predsort.
      gjenfortelle_v-k := predsort.
      gjenge_v-k := predsort.
      gjengifte_v-k := predsort.
      gjengi_v-k := predsort.
      gjengjelde_v-k := predsort.
      gjeninnføre_v-k := predsort.
      gjeninnsette_v-k := predsort.
      gjeninnta_v-k := predsort.
      gjenkalle_v-k := predsort.
      gjenkjenne_v-k := predsort.
      gjenlegge_v-k := predsort.
      gjenløse_v-k := predsort.
      gjenlyde_v-k := predsort.
      gjenmæle_v-k := predsort.
      gjennomarbeide_v-k := predsort.
      gjennombore_v-k := predsort.
      gjennomdrøfte_v-k := predsort.
      gjennomføre_v-k := predsort.
      gjennomgå_v-k := predsort.
      gjennomhulle_v-k := predsort.
      gjennomillustrere_v-k := predsort.
      gjennomlese_v-k := predsort.
      gjennomleve_v-k := predsort.
      gjennomlufte_v-k := predsort.
      gjennomlyse_v-k := predsort.
      gjennomprøve_v-k := predsort.
      gjennomse_v-k := predsort.
      gjennomskue_v-k := predsort.
      gjennomsøke_v-k := predsort.
      gjennomsteike_v-k := predsort.
      gjennomsteke_v-k := predsort.
      gjennomstrømme_v-k := predsort.
      gjennomsyre_v-k := predsort.
      gjennomtenke_v-k := predsort.
      gjennomtråle_v-k := predsort.
      gjennomtrenge_v-k := predsort.
      gjenoppblusse_v-k := predsort.
      gjenoppbygge_v-k := predsort.
      gjenoppdage_v-k := predsort.
      gjenoppfriske_v-k := predsort.
      gjenoppleve_v-k := predsort.
      gjenopplive_v-k := predsort.
      gjenopprette_v-k := predsort.
      gjenoppstå_v-k := predsort.
      gjenoppta_v-k := predsort.
      gjenopptelle_v-k := predsort.
      gjenopptrykke_v-k := predsort.
      gjenoppvekke_v-k := predsort.
      gjenreise_v-k := predsort.
      gjenskape_v-k := predsort.
      gjenspeile_v-k := predsort.
      gjenstå_v-k := predsort.
      gjenta_v-k := predsort.
      gjenvinne_v-k := predsort.
      gjerde_v-k := predsort.
      gjespe_v-k := predsort.
      gjeste_v-k := predsort.
      gjete_v-k := predsort.
      gjette_v-k := predsort.
      gjøde_v-k := predsort.
      gjødsle_v-k := predsort.
      gjøgle_v-k := predsort.
      gjø_v-k := predsort.
      gjøle_v-k := predsort.
      gjømme_v-k := predsort.
      gjorde_v-k := predsort.
      gjøre_v-k := predsort.
      gjørme_v-k := predsort.
      gjørs_v-k := predsort.
      gjure_v-k := predsort.
      glafse_v-k := predsort.
      gla_v-k := predsort.
      glamme_v-k := predsort.
      glamorisere_v-k := predsort.
      glane_v-k := predsort.
      glanse_v-k := predsort.
      glasere_v-k := predsort.
      glassere_v-k := predsort.
      glattbarbere_v-k := predsort.
      glatte_v-k := predsort.
      glattkjemme_v-k := predsort.
      glattstryke_v-k := predsort.
      glede_v-k := predsort.
      glefse_v-k := predsort.
      gle_v-k := predsort.
      glemme_v-k := predsort.
      gleppe_v-k := predsort.
      glideflyge_v-k := predsort.
      glidefly_v-k := predsort.
      glide_v-k := predsort.
      gli_v-k := predsort.
      glime_v-k := predsort.
      glimre_v-k := predsort.
      glimte_v-k := predsort.
      glinse_v-k := predsort.
      glipe_v-k := predsort.
      glippe_v-k := predsort.
      glire_v-k := predsort.
      glise_v-k := predsort.
      glitre_v-k := predsort.
      glitte_v-k := predsort.
      gløde_v-k := predsort.
      glo_v-k := predsort.
      glø_v-k := predsort.
      glømme_v-k := predsort.
      glore_v-k := predsort.
      glorifisere_v-k := predsort.
      gløtte_v-k := predsort.
      gnafse_v-k := predsort.
      gnage_v-k := predsort.
      gnaldre_v-k := predsort.
      gnåle_v-k := predsort.
      gneldre_v-k := predsort.
      gnelle_v-k := predsort.
      gnikke_v-k := predsort.
      gni_v-k := predsort.
      gnisle_v-k := predsort.
      gnisse_v-k := predsort.
      gniste_v-k := predsort.
      gnistre_v-k := predsort.
      gnu_v-k := predsort.
      gny_v-k := predsort.
      godgjøre_v-k := predsort.
      godkjenne_v-k := predsort.
      godskrive_v-k := predsort.
      godsnakke_v-k := predsort.
      godta_v-k := predsort.
      godte_v-k := predsort.
      godtgjøre_v-k := predsort.
      gomle_v-k := predsort.
      gove_v-k := predsort.
      gøyse_v-k := predsort.
      grabbe_v-k := predsort.
      gradbøye_v-k := predsort.
      grade_v-k := predsort.
      gradere_v-k := predsort.
      graduere_v-k := predsort.
      grafse_v-k := predsort.
      gråle_v-k := predsort.
      gramse_v-k := predsort.
      gråne_v-k := predsort.
      granske_v-k := predsort.
      granulere_v-k := predsort.
      grase_v-k := predsort.
      grassere_v-k := predsort.
      gråte_v-k := predsort.
      gratinere_v-k := predsort.
      gratulere_v-k := predsort.
      grave_v-k := predsort.
      gravere_v-k := predsort.
      gravitere_v-k := predsort.
      gravlegge_v-k := predsort.
      greie_v-k := predsort.
      greine_v-k := predsort.
      gre_v-k := predsort.
      gremme_v-k := predsort.
      grene_v-k := predsort.
      grense_v-k := predsort.
      gresse_v-k := predsort.
      griljere_v-k := predsort.
      grille_v-k := predsort.
      grime_v-k := predsort.
      grine_v-k := predsort.
      gripe_v-k := predsort.
      grise_v-k := predsort.
      grisle_v-k := predsort.
      grisne_v-k := predsort.
      grøde_v-k := predsort.
      grøfte_v-k := predsort.
      gro_v-k := predsort.
      grø_v-k := predsort.
      grønke_v-k := predsort.
      grønkes_v-k := predsort.
      grønnes_v-k := predsort.
      grønske_v-k := predsort.
      grope_v-k := predsort.
      grøppe_v-k := predsort.
      grøsse_v-k := predsort.
      grovmale_v-k := predsort.
      grovsikte_v-k := predsort.
      grovsortere_v-k := predsort.
      grovtelle_v-k := predsort.
      gruble_v-k := predsort.
      grue_v-k := predsort.
      grumse_v-k := predsort.
      grundere_v-k := predsort.
      grunne_v-k := predsort.
      grunnfeste_v-k := predsort.
      grunngi_v-k := predsort.
      grunnlegge_v-k := predsort.
      grunnsette_v-k := predsort.
      grunnstøte_v-k := predsort.
      gruppere_v-k := predsort.
      gruse_v-k := predsort.
      gruslegge_v-k := predsort.
      gry_v-k := predsort.
      gryne_v-k := predsort.
      grynte_v-k := predsort.
      gufse_v-k := predsort.
      guide_v-k := predsort.
      gule_v-k := predsort.
      gulke_v-k := predsort.
      gulne_v-k := predsort.
      gulpe_v-k := predsort.
      gummiere_v-k := predsort.
      gurgle_v-k := predsort.
      gurpe_v-k := predsort.
      guste_v-k := predsort.
      gutere_v-k := predsort.
      gylle_v-k := predsort.
      gymnastisere_v-k := predsort.
      gynge_v-k := predsort.
      gyse_v-k := predsort.
      gyte_v-k := predsort.
      gyve_v-k := predsort.
      habilitere_v-k := predsort.
      hacke_v-k := predsort.
      hæle_v-k := predsort.
      hælsparke_v-k := predsort.
      hærta_v-k := predsort.
      hagle_v-k := predsort.
      haike_v-k := predsort.
      hake_v-k := predsort.
      hakke_v-k := predsort.
      ha_v-k := predsort.
      hale_v-k := predsort.
      halle_v-k := predsort.
      hallusinere_v-k := predsort.
      halse_v-k := predsort.
      halshogge_v-k := predsort.
      halshugge_v-k := predsort.
      halte_v-k := predsort.
      halve_v-k := predsort.
      halvere_v-k := predsort.
      halvgardere_v-k := predsort.
      halvklare_v-k := predsort.
      halvsåle_v-k := predsort.
      halvsove_v-k := predsort.
      hamle_v-k := predsort.
      hamne_v-k := predsort.
      hamre_v-k := predsort.
      hamse_v-k := predsort.
      hamstre_v-k := predsort.
      handfare_v-k := predsort.
      handgi_v-k := predsort.
      håndgi_v-k := predsort.
      handhelse_v-k := predsort.
      handheve_v-k := predsort.
      håndheve_v-k := predsort.
      handhilse_v-k := predsort.
      håndhilse_v-k := predsort.
      handikappe_v-k := predsort.
      handle_v-k := predsort.
      handplukke_v-k := predsort.
      håndplukke_v-k := predsort.
      handsame_v-k := predsort.
      handse_v-k := predsort.
      handtere_v-k := predsort.
      håndtere_v-k := predsort.
      håne_v-k := predsort.
      hånflire_v-k := predsort.
      hangle_v-k := predsort.
      hånle_v-k := predsort.
      hanskes_v-k := predsort.
      håpe_v-k := predsort.
      hardkoke_v-k := predsort.
      hardne_v-k := predsort.
      hardtrampe_v-k := predsort.
      hardtrene_v-k := predsort.
      harke_v-k := predsort.
      harme_v-k := predsort.
      harmonere_v-k := predsort.
      harmonisere_v-k := predsort.
      harpe_v-k := predsort.
      harpesere_v-k := predsort.
      harpunere_v-k := predsort.
      harselere_v-k := predsort.
      harske_v-k := predsort.
      harskne_v-k := predsort.
      harve_v-k := predsort.
      haspe_v-k := predsort.
      hasple_v-k := predsort.
      haste_v-k := predsort.
      hastevedta_v-k := predsort.
      hate_v-k := predsort.
      hauge_v-k := predsort.
      hauglegge_v-k := predsort.
      haugta_v-k := predsort.
      hauke_v-k := predsort.
      hausse_v-k := predsort.
      havarere_v-k := predsort.
      håve_v-k := predsort.
      havne_v-k := predsort.
      heade_v-k := predsort.
      hedre_v-k := predsort.
      hefte_v-k := predsort.
      hegne_v-k := predsort.
      heie_v-k := predsort.
      heilgardere_v-k := predsort.
      heilsteike_v-k := predsort.
      heilsteke_v-k := predsort.
      heilstøpe_v-k := predsort.
      heilstøype_v-k := predsort.
      heilsynkronisere_v-k := predsort.
      heimfalle_v-k := predsort.
      heimfeste_v-k := predsort.
      heimføre_v-k := predsort.
      heimkalle_v-k := predsort.
      heimle_v-k := predsort.
      heimsende_v-k := predsort.
      heimsøke_v-k := predsort.
      heine_v-k := predsort.
      heise_v-k := predsort.
      heitne_v-k := predsort.
      hekke_v-k := predsort.
      hekle_v-k := predsort.
      hekse_v-k := predsort.
      hekte_v-k := predsort.
      hektografere_v-k := predsort.
      helbrede_v-k := predsort.
      helde_v-k := predsort.
      hele_v-k := predsort.
      helgardere_v-k := predsort.
      helgestenge_v-k := predsort.
      helle_v-k := predsort.
      hellelegge_v-k := predsort.
      hellige_v-k := predsort.
      helligholde_v-k := predsort.
      helseslå_v-k := predsort.
      helsteike_v-k := predsort.
      helsteke_v-k := predsort.
      helstøpe_v-k := predsort.
      helstøype_v-k := predsort.
      helsynkronisere_v-k := predsort.
      hemme_v-k := predsort.
      hemmeligholde_v-k := predsort.
      hemmeligstemple_v-k := predsort.
      hende_v-k := predsort.
      henfalle_v-k := predsort.
      henføre_v-k := predsort.
      henge_v-k := predsort.
      hengi_v-k := predsort.
      hengsle_v-k := predsort.
      henholde_v-k := predsort.
      henhøre_v-k := predsort.
      henkaste_v-k := predsort.
      henlede_v-k := predsort.
      henlegge_v-k := predsort.
      henløpe_v-k := predsort.
      henregne_v-k := predsort.
      henrekne_v-k := predsort.
      henrette_v-k := predsort.
      henrive_v-k := predsort.
      henrykke_v-k := predsort.
      hensette_v-k := predsort.
      henskyte_v-k := predsort.
      henslepe_v-k := predsort.
      henspille_v-k := predsort.
      henstå_v-k := predsort.
      henstille_v-k := predsort.
      hensvinne_v-k := predsort.
      hente_v-k := predsort.
      hentyde_v-k := predsort.
      henvende_v-k := predsort.
      henvise_v-k := predsort.
      heppe_v-k := predsort.
      herde_v-k := predsort.
      herdne_v-k := predsort.
      herje_v-k := predsort.
      herliggjøre_v-k := predsort.
      herme_v-k := predsort.
      hermetisere_v-k := predsort.
      heroisere_v-k := predsort.
      herpesere_v-k := predsort.
      herse_v-k := predsort.
      herske_v-k := predsort.
      hesje_v-k := predsort.
      hespe_v-k := predsort.
      hesple_v-k := predsort.
      hestehandle_v-k := predsort.
      hete_v-k := predsort.
      hetne_v-k := predsort.
      hetse_v-k := predsort.
      hevde_v-k := predsort.
      heve_v-k := predsort.
      hevle_v-k := predsort.
      hevne_v-k := predsort.
      hie_v-k := predsort.
      hige_v-k := predsort.
      hike_v-k := predsort.
      hikke_v-k := predsort.
      hikstegråte_v-k := predsort.
      hikste_v-k := predsort.
      hilde_v-k := predsort.
      hildre_v-k := predsort.
      hilse_v-k := predsort.
      himle_v-k := predsort.
      hindre_v-k := predsort.
      hinke_v-k := predsort.
      hisse_v-k := predsort.
      hitsette_v-k := predsort.
      hitte_v-k := predsort.
      hive_v-k := predsort.
      hivsmitte_v-k := predsort.
      hjelpe_v-k := predsort.
      hjemfalle_v-k := predsort.
      hjemfeste_v-k := predsort.
      hjemføre_v-k := predsort.
      hjemkalle_v-k := predsort.
      hjemle_v-k := predsort.
      hjemsende_v-k := predsort.
      hjemsøke_v-k := predsort.
      hjernevaske_v-k := predsort.
      hofferde_v-k := predsort.
      hogge_v-k := predsort.
      hoie_v-k := predsort.
      høkre_v-k := predsort.
      holde_v-k := predsort.
      hølje_v-k := predsort.
      høljeregne_v-k := predsort.
      høljregne_v-k := predsort.
      holke_v-k := predsort.
      holne_v-k := predsort.
      homme_v-k := predsort.
      homogenisere_v-k := predsort.
      honorere_v-k := predsort.
      hope_v-k := predsort.
      hoppe_v-k := predsort.
      hore_v-k := predsort.
      høre_v-k := predsort.
      høres_v-k := predsort.
      horse_v-k := predsort.
      horve_v-k := predsort.
      hospitalisere_v-k := predsort.
      hospitere_v-k := predsort.
      hoste_v-k := predsort.
      høste_v-k := predsort.
      hote_v-k := predsort.
      høve_v-k := predsort.
      hovere_v-k := predsort.
      høvle_v-k := predsort.
      hovmesterere_v-k := predsort.
      hovmode_v-k := predsort.
      hovne_v-k := predsort.
      høyne_v-k := predsort.
      hudflette_v-k := predsort.
      hudstryke_v-k := predsort.
      hue_v-k := predsort.
      hufse_v-k := predsort.
      huge_v-k := predsort.
      hugge_v-k := predsort.
      hugse_v-k := predsort.
      hugsvale_v-k := predsort.
      hugta_v-k := predsort.
      huie_v-k := predsort.
      huke_v-k := predsort.
      hule_v-k := predsort.
      hulke_v-k := predsort.
      hulle_v-k := predsort.
      humanisere_v-k := predsort.
      humpe_v-k := predsort.
      humre_v-k := predsort.
      hundjage_v-k := predsort.
      hundse_v-k := predsort.
      hungre_v-k := predsort.
      hurre_v-k := predsort.
      husbankfinansiere_v-k := predsort.
      huse_v-k := predsort.
      husere_v-k := predsort.
      husholde_v-k := predsort.
      husholdere_v-k := predsort.
      huske_v-k := predsort.
      hustre_v-k := predsort.
      husvale_v-k := predsort.
      hute_v-k := predsort.
      hutre_v-k := predsort.
      hvelve_v-k := predsort.
      hvese_v-k := predsort.
      hvesse_v-k := predsort.
      hvile_v-k := predsort.
      hvine_v-k := predsort.
      hvirvle_v-k := predsort.
      hviske_v-k := predsort.
      hvitne_v-k := predsort.
      hvitte_v-k := predsort.
      hvitvaske_v-k := predsort.
      hygge_v-k := predsort.
      hykle_v-k := predsort.
      hyle_v-k := predsort.
      hylle_v-k := predsort.
      hypnotisere_v-k := predsort.
      hyppe_v-k := predsort.
      hyre_v-k := predsort.
      hysje_v-k := predsort.
      hytte_v-k := predsort.
      iaktta_v-k := predsort.
      iberegne_v-k := predsort.
      iberekne_v-k := predsort.
      iblande_v-k := predsort.
      idealisere_v-k := predsort.
      identifisere_v-k := predsort.
      ideologisere_v-k := predsort.
      idest_v-k := predsort.
      idke_v-k := predsort.
      idømme_v-k := predsort.
      idyllisere_v-k := predsort.
      iføre_v-k := predsort.
      igangsette_v-k := predsort.
      ignorere_v-k := predsort.
      ihukomme_v-k := predsort.
      ikle_v-k := predsort.
      ilandføre_v-k := predsort.
      ildne_v-k := predsort.
      ilegge_v-k := predsort.
      ile_v-k := predsort.
      iligne_v-k := predsort.
      ilikne_v-k := predsort.
      illgjø_v-k := predsort.
      illhuie_v-k := predsort.
      illskrike_v-k := predsort.
      illudere_v-k := predsort.
      illuminere_v-k := predsort.
      illustrere_v-k := predsort.
      ilske_v-k := predsort.
      ilskne_v-k := predsort.
      imaginere_v-k := predsort.
      imitere_v-k := predsort.
      immatrikulere_v-k := predsort.
      immigrere_v-k := predsort.
      immortalisere_v-k := predsort.
      immunisere_v-k := predsort.
      imøtegå_v-k := predsort.
      imøtekomme_v-k := predsort.
      imøtese_v-k := predsort.
      implantere_v-k := predsort.
      implementere_v-k := predsort.
      implisere_v-k := predsort.
      implodere_v-k := predsort.
      imponere_v-k := predsort.
      importere_v-k := predsort.
      impregnere_v-k := predsort.
      improvisere_v-k := predsort.
      inaktivere_v-k := predsort.
      inaugurere_v-k := predsort.
      incitere_v-k := predsort.
      indikere_v-k := predsort.
      individualisere_v-k := predsort.
      indoktrinere_v-k := predsort.
      indusere_v-k := predsort.
      industrialisere_v-k := predsort.
      infamere_v-k := predsort.
      infantilisere_v-k := predsort.
      infiltrere_v-k := predsort.
      infisere_v-k := predsort.
      inflammere_v-k := predsort.
      inflatere_v-k := predsort.
      influere_v-k := predsort.
      informere_v-k := predsort.
      inhalere_v-k := predsort.
      inherere_v-k := predsort.
      inhibere_v-k := predsort.
      initiere_v-k := predsort.
      injisere_v-k := predsort.
      injuriere_v-k := predsort.
      inkarnere_v-k := predsort.
      inklinere_v-k := predsort.
      inkludere_v-k := predsort.
      inkorporere_v-k := predsort.
      innånde_v-k := predsort.
      innanke_v-k := predsort.
      innarbeide_v-k := predsort.
      innbefatte_v-k := predsort.
      innberette_v-k := predsort.
      innbetale_v-k := predsort.
      innbille_v-k := predsort.
      innbinde_v-k := predsort.
      innblande_v-k := predsort.
      innbringe_v-k := predsort.
      innby_v-k := predsort.
      inndampe_v-k := predsort.
      inndele_v-k := predsort.
      inndra_v-k := predsort.
      inndrive_v-k := predsort.
      innebære_v-k := predsort.
      inneha_v-k := predsort.
      inneholde_v-k := predsort.
      inneksersere_v-k := predsort.
      innelåse_v-k := predsort.
      innestå_v-k := predsort.
      innfatte_v-k := predsort.
      innfelle_v-k := predsort.
      innfiltre_v-k := predsort.
      innfinne_v-k := predsort.
      innflytte_v-k := predsort.
      innføre_v-k := predsort.
      innforlive_v-k := predsort.
      innføye_v-k := predsort.
      innfri_v-k := predsort.
      inngå_v-k := predsort.
      inngi_v-k := predsort.
      inngjerde_v-k := predsort.
      inngravere_v-k := predsort.
      inngyte_v-k := predsort.
      innhegne_v-k := predsort.
      innhente_v-k := predsort.
      innhøste_v-k := predsort.
      innhylle_v-k := predsort.
      innkalkulere_v-k := predsort.
      innkalle_v-k := predsort.
      innkapsle_v-k := predsort.
      innkassere_v-k := predsort.
      innkaste_v-k := predsort.
      innkjøre_v-k := predsort.
      innklage_v-k := predsort.
      innklarere_v-k := predsort.
      innkoble_v-k := predsort.
      innkommandere_v-k := predsort.
      innkomme_v-k := predsort.
      innkople_v-k := predsort.
      innkorte_v-k := predsort.
      innkreve_v-k := predsort.
      innkvartere_v-k := predsort.
      innlåne_v-k := predsort.
      innlate_v-k := predsort.
      innlede_v-k := predsort.
      innlegge_v-k := predsort.
      innleie_v-k := predsort.
      innlemme_v-k := predsort.
      innlese_v-k := predsort.
      innlevere_v-k := predsort.
      innløpe_v-k := predsort.
      innløse_v-k := predsort.
      innlosjere_v-k := predsort.
      innløyse_v-k := predsort.
      innmure_v-k := predsort.
      innordne_v-k := predsort.
      innøve_v-k := predsort.
      innovere_v-k := predsort.
      innpakke_v-k := predsort.
      innpasse_v-k := predsort.
      innpassere_v-k := predsort.
      innpiske_v-k := predsort.
      innplante_v-k := predsort.
      innpode_v-k := predsort.
      innpote_v-k := predsort.
      innprente_v-k := predsort.
      innramme_v-k := predsort.
      innrapportere_v-k := predsort.
      innrede_v-k := predsort.
      innreie_v-k := predsort.
      innrette_v-k := predsort.
      innringe_v-k := predsort.
      innrømme_v-k := predsort.
      innrullere_v-k := predsort.
      innrykke_v-k := predsort.
      innsamle_v-k := predsort.
      innsegle_v-k := predsort.
      innseile_v-k := predsort.
      innse_v-k := predsort.
      innsende_v-k := predsort.
      innsette_v-k := predsort.
      innsikte_v-k := predsort.
      innsirkle_v-k := predsort.
      innskipe_v-k := predsort.
      innskjære_v-k := predsort.
      innskjerpe_v-k := predsort.
      innskrenke_v-k := predsort.
      innskrive_v-k := predsort.
      innskyte_v-k := predsort.
      innsmigre_v-k := predsort.
      innsmøre_v-k := predsort.
      innsmugle_v-k := predsort.
      innsnevre_v-k := predsort.
      innsprøyte_v-k := predsort.
      innstemne_v-k := predsort.
      innstevne_v-k := predsort.
      innstifte_v-k := predsort.
      innstille_v-k := predsort.
      innstøpe_v-k := predsort.
      innstøype_v-k := predsort.
      innstramme_v-k := predsort.
      innstudere_v-k := predsort.
      innsuge_v-k := predsort.
      innsynge_v-k := predsort.
      innta_v-k := predsort.
      inntaste_v-k := predsort.
      inntegne_v-k := predsort.
      inntreffe_v-k := predsort.
      inntre_v-k := predsort.
      innvandre_v-k := predsort.
      innvarsle_v-k := predsort.
      innveie_v-k := predsort.
      innvelge_v-k := predsort.
      innvende_v-k := predsort.
      innvie_v-k := predsort.
      innvikle_v-k := predsort.
      innvilge_v-k := predsort.
      innvinne_v-k := predsort.
      innvirke_v-k := predsort.
      innvotere_v-k := predsort.
      innynde_v-k := predsort.
      inokulere_v-k := predsort.
      inseminere_v-k := predsort.
      insinuere_v-k := predsort.
      insistere_v-k := predsort.
      inspirere_v-k := predsort.
      inspisere_v-k := predsort.
      installere_v-k := predsort.
      instituere_v-k := predsort.
      institusjonalisere_v-k := predsort.
      instruere_v-k := predsort.
      instrumentere_v-k := predsort.
      integrere_v-k := predsort.
      intellektualisere_v-k := predsort.
      intendere_v-k := predsort.
      intensivere_v-k := predsort.
      interessere_v-k := predsort.
      interferere_v-k := predsort.
      interfoliere_v-k := predsort.
      internasjonalisere_v-k := predsort.
      internere_v-k := predsort.
      interpellere_v-k := predsort.
      interpolere_v-k := predsort.
      interpretere_v-k := predsort.
      intervenere_v-k := predsort.
      intervjue_v-k := predsort.
      intimidere_v-k := predsort.
      intonere_v-k := predsort.
      intrigere_v-k := predsort.
      introdusere_v-k := predsort.
      invadere_v-k := predsort.
      invalidisere_v-k := predsort.
      inventere_v-k := predsort.
      invertere_v-k := predsort.
      investere_v-k := predsort.
      invitere_v-k := predsort.
      involvere_v-k := predsort.
      ionisere_v-k := predsort.
      irettesette_v-k := predsort.
      irisere_v-k := predsort.
      ironisere_v-k := predsort.
      irre_v-k := predsort.
      irrigere_v-k := predsort.
      irritere_v-k := predsort.
      iscenesette_v-k := predsort.
      ise_v-k := predsort.
      islamisere_v-k := predsort.
      isne_v-k := predsort.
      isolere_v-k := predsort.
      ispe_v-k := predsort.
      istemme_v-k := predsort.
      iterere_v-k := predsort.
      ivareta_v-k := predsort.
      iverksette_v-k := predsort.
      ivre_v-k := predsort.
      jabbe_v-k := predsort.
      jafse_v-k := predsort.
      jage_v-k := predsort.
      jakte_v-k := predsort.
      jåle_v-k := predsort.
      jalme_v-k := predsort.
      jamføre_v-k := predsort.
      jamme_v-k := predsort.
      jamne_v-k := predsort.
      jamre_v-k := predsort.
      jamstille_v-k := predsort.
      jare_v-k := predsort.
      jaske_v-k := predsort.
      jasse_v-k := predsort.
      jassifisere_v-k := predsort.
      jatte_v-k := predsort.
      jazze_v-k := predsort.
      jazzifisere_v-k := predsort.
      jekke_v-k := predsort.
      jelke_v-k := predsort.
      jenke_v-k := predsort.
      jevne_v-k := predsort.
      jevnføre_v-k := predsort.
      jibbe_v-k := predsort.
      jobbe_v-k := predsort.
      jodere_v-k := predsort.
      jodisere_v-k := predsort.
      jodle_v-k := predsort.
      jogge_v-k := predsort.
      joike_v-k := predsort.
      jorde_v-k := predsort.
      jordfeste_v-k := predsort.
      jordslå_v-k := predsort.
      jorte_v-k := predsort.
      journalføre_v-k := predsort.
      journalisere_v-k := predsort.
      jøye_v-k := predsort.
      jubilere_v-k := predsort.
      juble_v-k := predsort.
      jugle_v-k := predsort.
      jukse_v-k := predsort.
      jule_v-k := predsort.
      jumpe_v-k := predsort.
      junke_v-k := predsort.
      juryere_v-k := predsort.
      justere_v-k := predsort.
      kabbe_v-k := predsort.
      kadmiere_v-k := predsort.
      kadreie_v-k := predsort.
      kake_v-k := predsort.
      kakke_v-k := predsort.
      kakle_v-k := predsort.
      kakstryke_v-k := predsort.
      kakuminalisere_v-k := predsort.
      kalandrere_v-k := predsort.
      kaldflire_v-k := predsort.
      kaldrøyke_v-k := predsort.
      kaldskratte_v-k := predsort.
      kaldsmi_v-k := predsort.
      kaldsvette_v-k := predsort.
      kalfatre_v-k := predsort.
      kalibrere_v-k := predsort.
      kalke_v-k := predsort.
      kalkere_v-k := predsort.
      kalkulere_v-k := predsort.
      kalle_v-k := predsort.
      kalligrafere_v-k := predsort.
      kalsinere_v-k := predsort.
      kalve_v-k := predsort.
      kamme_v-k := predsort.
      kampere_v-k := predsort.
      kamse_v-k := predsort.
      kamuflere_v-k := predsort.
      kanalisere_v-k := predsort.
      kandidere_v-k := predsort.
      kandisere_v-k := predsort.
      kannelere_v-k := predsort.
      kanonisere_v-k := predsort.
      kansellere_v-k := predsort.
      kante_v-k := predsort.
      kantre_v-k := predsort.
      kapitalisere_v-k := predsort.
      kapitulere_v-k := predsort.
      kappdrikke_v-k := predsort.
      kappe_v-k := predsort.
      kappes_v-k := predsort.
      kappkjøre_v-k := predsort.
      kapre_v-k := predsort.
      kapseise_v-k := predsort.
      kapsle_v-k := predsort.
      kapunere_v-k := predsort.
      karakterbasere_v-k := predsort.
      karakterisere_v-k := predsort.
      karde_v-k := predsort.
      kare_v-k := predsort.
      kåre_v-k := predsort.
      karikere_v-k := predsort.
      karnøfle_v-k := predsort.
      karre_v-k := predsort.
      karte_v-k := predsort.
      kartere_v-k := predsort.
      kartlegge_v-k := predsort.
      kartonere_v-k := predsort.
      karve_v-k := predsort.
      kase_v-k := predsort.
      kåsere_v-k := predsort.
      kassere_v-k := predsort.
      kastebytte_v-k := predsort.
      kaste_v-k := predsort.
      kastrere_v-k := predsort.
      katalogisere_v-k := predsort.
      katalysere_v-k := predsort.
      kategorisere_v-k := predsort.
      katekisere_v-k := predsort.
      katolisere_v-k := predsort.
      kaue_v-k := predsort.
      kauke_v-k := predsort.
      kausjonere_v-k := predsort.
      kave_v-k := predsort.
      kavere_v-k := predsort.
      kavle_v-k := predsort.
      keie_v-k := predsort.
      keike_v-k := predsort.
      keivle_v-k := predsort.
      kibbe_v-k := predsort.
      kidnappe_v-k := predsort.
      kike_v-k := predsort.
      kikke_v-k := predsort.
      kikne_v-k := predsort.
      kikre_v-k := predsort.
      kikse_v-k := predsort.
      kildre_v-k := predsort.
      kile_v-k := predsort.
      kime_v-k := predsort.
      kimse_v-k := predsort.
      kingse_v-k := predsort.
      kinke_v-k := predsort.
      kinkse_v-k := predsort.
      kinne_v-k := predsort.
      kippe_v-k := predsort.
      kisle_v-k := predsort.
      kitle_v-k := predsort.
      kitte_v-k := predsort.
      kive_v-k := predsort.
      kives_v-k := predsort.
      kjæle_v-k := predsort.
      kjære_v-k := predsort.
      kjærtegne_v-k := predsort.
      kjæte_v-k := predsort.
      kjakse_v-k := predsort.
      kjangse_v-k := predsort.
      kjappe_v-k := predsort.
      kjase_v-k := predsort.
      kjave_v-k := predsort.
      kjede_v-k := predsort.
      kjederøyke_v-k := predsort.
      kjee_v-k := predsort.
      kjefte_v-k := predsort.
      kjekke_v-k := predsort.
      kjekle_v-k := predsort.
      kjekse_v-k := predsort.
      kjemme_v-k := predsort.
      kjempe_v-k := predsort.
      kjenne_v-k := predsort.
      kjennetegne_v-k := predsort.
      kjeppjage_v-k := predsort.
      kjerne_v-k := predsort.
      kjerve_v-k := predsort.
      kjese_v-k := predsort.
      kjeske_v-k := predsort.
      kjevle_v-k := predsort.
      kjøle_v-k := predsort.
      kjølhale_v-k := predsort.
      kjølne_v-k := predsort.
      kjøpe_v-k := predsort.
      kjøpslå_v-k := predsort.
      kjøre_v-k := predsort.
      klabbe_v-k := predsort.
      kladde_v-k := predsort.
      klaffe_v-k := predsort.
      klage_v-k := predsort.
      klake_v-k := predsort.
      klakke_v-k := predsort.
      klå_v-k := predsort.
      klampe_v-k := predsort.
      klamre_v-k := predsort.
      klandre_v-k := predsort.
      klappe_v-k := predsort.
      klapre_v-k := predsort.
      klapse_v-k := predsort.
      klare_v-k := predsort.
      klarere_v-k := predsort.
      klargjøre_v-k := predsort.
      klarlegge_v-k := predsort.
      klarne_v-k := predsort.
      klase_v-k := predsort.
      klaske_v-k := predsort.
      klassifisere_v-k := predsort.
      klatre_v-k := predsort.
      klatte_v-k := predsort.
      klattemale_v-k := predsort.
      klausulere_v-k := predsort.
      klavebinde_v-k := predsort.
      klave_v-k := predsort.
      klebe_v-k := predsort.
      klekke_v-k := predsort.
      kle_v-k := predsort.
      klemme_v-k := predsort.
      klemte_v-k := predsort.
      klenge_v-k := predsort.
      kleppe_v-k := predsort.
      klikke_v-k := predsort.
      klimatisere_v-k := predsort.
      klimpre_v-k := predsort.
      kline_v-k := predsort.
      klinge_v-k := predsort.
      klingre_v-k := predsort.
      klinke_v-k := predsort.
      klipe_v-k := predsort.
      klippe_v-k := predsort.
      klirre_v-k := predsort.
      klisjere_v-k := predsort.
      klisse_v-k := predsort.
      klistre_v-k := predsort.
      klive_v-k := predsort.
      kloakkere_v-k := predsort.
      kløfte_v-k := predsort.
      klokke_v-k := predsort.
      kløkke_v-k := predsort.
      klø_v-k := predsort.
      klone_v-k := predsort.
      kløne_v-k := predsort.
      klore_v-k := predsort.
      klorere_v-k := predsort.
      kloroformere_v-k := predsort.
      kløtsje_v-k := predsort.
      kløve_v-k := predsort.
      kløvje_v-k := predsort.
      klovne_v-k := predsort.
      kløvne_v-k := predsort.
      kløyve_v-k := predsort.
      klubbe_v-k := predsort.
      kludre_v-k := predsort.
      klukke_v-k := predsort.
      klukkle_v-k := predsort.
      klumpe_v-k := predsort.
      klumse_v-k := predsort.
      klundre_v-k := predsort.
      klunke_v-k := predsort.
      klusse_v-k := predsort.
      klynge_v-k := predsort.
      klynke_v-k := predsort.
      klype_v-k := predsort.
      klyve_v-k := predsort.
      knabbe_v-k := predsort.
      knake_v-k := predsort.
      knakke_v-k := predsort.
      kna_v-k := predsort.
      knalle_v-k := predsort.
      knappe_v-k := predsort.
      knarke_v-k := predsort.
      knarte_v-k := predsort.
      knase_v-k := predsort.
      knaske_v-k := predsort.
      knatre_v-k := predsort.
      knatte_v-k := predsort.
      kneble_v-k := predsort.
      knegå_v-k := predsort.
      knege_v-k := predsort.
      knegge_v-k := predsort.
      kneise_v-k := predsort.
      knekke_v-k := predsort.
      knekte_v-k := predsort.
      knele_v-k := predsort.
      kneppe_v-k := predsort.
      knepre_v-k := predsort.
      knerte_v-k := predsort.
      knesette_v-k := predsort.
      kneste_v-k := predsort.
      knette_v-k := predsort.
      knikse_v-k := predsort.
      knipe_v-k := predsort.
      kniple_v-k := predsort.
      knippe_v-k := predsort.
      knipse_v-k := predsort.
      knirke_v-k := predsort.
      knise_v-k := predsort.
      knisle_v-k := predsort.
      knistre_v-k := predsort.
      knitre_v-k := predsort.
      knives_v-k := predsort.
      knivstikke_v-k := predsort.
      knocke_v-k := predsort.
      knockoute_v-k := predsort.
      knoge_v-k := predsort.
      knope_v-k := predsort.
      knoppe_v-k := predsort.
      knote_v-k := predsort.
      knuffe_v-k := predsort.
      knuge_v-k := predsort.
      knulle_v-k := predsort.
      knupse_v-k := predsort.
      knurke_v-k := predsort.
      knurpe_v-k := predsort.
      knurre_v-k := predsort.
      knurve_v-k := predsort.
      knuse_v-k := predsort.
      knuselske_v-k := predsort.
      knusle_v-k := predsort.
      knuspe_v-k := predsort.
      knuspre_v-k := predsort.
      knustre_v-k := predsort.
      knuvle_v-k := predsort.
      kny_v-k := predsort.
      knyste_v-k := predsort.
      knyte_v-k := predsort.
      knytte_v-k := predsort.
      koagulere_v-k := predsort.
      koble_v-k := predsort.
      kødde_v-k := predsort.
      kode_v-k := predsort.
      kodifisere_v-k := predsort.
      køe_v-k := predsort.
      kogle_v-k := predsort.
      kokaindømme_v-k := predsort.
      koke_v-k := predsort.
      kokettere_v-k := predsort.
      kokkelere_v-k := predsort.
      kokkerere_v-k := predsort.
      kokre_v-k := predsort.
      kole_v-k := predsort.
      kollabere_v-k := predsort.
      kollaborere_v-k := predsort.
      kollapse_v-k := predsort.
      kollasjonere_v-k := predsort.
      kolle_v-k := predsort.
      kollektivisere_v-k := predsort.
      kollidere_v-k := predsort.
      kollokvere_v-k := predsort.
      kollsegle_v-k := predsort.
      kollseile_v-k := predsort.
      kolonisere_v-k := predsort.
      kolorere_v-k := predsort.
      kolportere_v-k := predsort.
      kombinere_v-k := predsort.
      komitebehandle_v-k := predsort.
      komitébehandle_v-k := predsort.
      kommandere_v-k := predsort.
      komme_v-k := predsort.
      kommentere_v-k := predsort.
      kommersialisere_v-k := predsort.
      kommunalisere_v-k := predsort.
      kommunisere_v-k := predsort.
      kommutere_v-k := predsort.
      komparere_v-k := predsort.
      kompe_v-k := predsort.
      kompensere_v-k := predsort.
      kompilere_v-k := predsort.
      komplettere_v-k := predsort.
      komplimentere_v-k := predsort.
      komplisere_v-k := predsort.
      komponere_v-k := predsort.
      kompostere_v-k := predsort.
      komprimere_v-k := predsort.
      kompromisse_v-k := predsort.
      kompromittere_v-k := predsort.
      kondemnere_v-k := predsort.
      kondensere_v-k := predsort.
      kondisjonere_v-k := predsort.
      kondolere_v-k := predsort.
      konferere_v-k := predsort.
      konfirmere_v-k := predsort.
      konfiskere_v-k := predsort.
      konfrontere_v-k := predsort.
      kongruere_v-k := predsort.
      konjugere_v-k := predsort.
      konke_v-k := predsort.
      konkludere_v-k := predsort.
      konkretisere_v-k := predsort.
      konkurrere_v-k := predsort.
      konnotere_v-k := predsort.
      konsentrere_v-k := predsort.
      konsertere_v-k := predsort.
      konservere_v-k := predsort.
      konsignere_v-k := predsort.
      konsipere_v-k := predsort.
      konskribere_v-k := predsort.
      konsolidere_v-k := predsort.
      konspirere_v-k := predsort.
      konstatere_v-k := predsort.
      konstituere_v-k := predsort.
      konstruere_v-k := predsort.
      konsultere_v-k := predsort.
      konsumere_v-k := predsort.
      kontakte_v-k := predsort.
      kontere_v-k := predsort.
      kontingentere_v-k := predsort.
      kontinuere_v-k := predsort.
      kontorisere_v-k := predsort.
      kontrahere_v-k := predsort.
      kontrasignere_v-k := predsort.
      kontrastere_v-k := predsort.
      kontre_v-k := predsort.
      kontribuere_v-k := predsort.
      kontrollere_v-k := predsort.
      konvergere_v-k := predsort.
      konversere_v-k := predsort.
      konvertere_v-k := predsort.
      konvoiere_v-k := predsort.
      konvoluttere_v-k := predsort.
      kooperere_v-k := predsort.
      koordinere_v-k := predsort.
      kope_v-k := predsort.
      kopiere_v-k := predsort.
      kople_v-k := predsort.
      koppe_v-k := predsort.
      kopulere_v-k := predsort.
      kore_v-k := predsort.
      koreografere_v-k := predsort.
      korke_v-k := predsort.
      korne_v-k := predsort.
      korporativisere_v-k := predsort.
      korrespondere_v-k := predsort.
      korrigere_v-k := predsort.
      korrodere_v-k := predsort.
      korrugere_v-k := predsort.
      korrumpere_v-k := predsort.
      korrupsjonsmistenke_v-k := predsort.
      korse_v-k := predsort.
      korsfeste_v-k := predsort.
      korslegge_v-k := predsort.
      korte_v-k := predsort.
      kortne_v-k := predsort.
      kortslutte_v-k := predsort.
      kose_v-k := predsort.
      koste_v-k := predsort.
      kostnadsberegne_v-k := predsort.
      kostnadsberekne_v-k := predsort.
      kostymere_v-k := predsort.
      kovne_v-k := predsort.
      køye_v-k := predsort.
      krabbe_v-k := predsort.
      krafse_v-k := predsort.
      krake_v-k := predsort.
      krakelere_v-k := predsort.
      krakke_v-k := predsort.
      kråle_v-k := predsort.
      kramme_v-k := predsort.
      kramse_v-k := predsort.
      krangle_v-k := predsort.
      kranse_v-k := predsort.
      krase_v-k := predsort.
      krasje_v-k := predsort.
      krasjlande_v-k := predsort.
      krasle_v-k := predsort.
      krasse_v-k := predsort.
      kraume_v-k := predsort.
      krave_v-k := predsort.
      kravle_v-k := predsort.
      kreditere_v-k := predsort.
      kreere_v-k := predsort.
      kreftfremkalle_v-k := predsort.
      kreftrelatere_v-k := predsort.
      kreiste_v-k := predsort.
      kreke_v-k := predsort.
      kremere_v-k := predsort.
      kremte_v-k := predsort.
      krenelere_v-k := predsort.
      krenge_v-k := predsort.
      krenke_v-k := predsort.
      krepere_v-k := predsort.
      kreppe_v-k := predsort.
      krepse_v-k := predsort.
      kretse_v-k := predsort.
      kreve_v-k := predsort.
      krible_v-k := predsort.
      krige_v-k := predsort.
      krigsherje_v-k := predsort.
      krikle_v-k := predsort.
      krille_v-k := predsort.
      kriminalisere_v-k := predsort.
      kringgå_v-k := predsort.
      kringkaste_v-k := predsort.
      kringsette_v-k := predsort.
      krinse_v-k := predsort.
      krisle_v-k := predsort.
      kristne_v-k := predsort.
      krite_v-k := predsort.
      kritisere_v-k := predsort.
      kritle_v-k := predsort.
      kritte_v-k := predsort.
      kroke_v-k := predsort.
      krøke_v-k := predsort.
      krokere_v-k := predsort.
      krokkere_v-k := predsort.
      krokne_v-k := predsort.
      krøkne_v-k := predsort.
      kro_v-k := predsort.
      krølle_v-k := predsort.
      kromgarve_v-k := predsort.
      krone_v-k := predsort.
      krongle_v-k := predsort.
      kronikkere_v-k := predsort.
      krøple_v-k := predsort.
      kroppsvisitere_v-k := predsort.
      krøsse_v-k := predsort.
      krote_v-k := predsort.
      krulle_v-k := predsort.
      krumme_v-k := predsort.
      krungle_v-k := predsort.
      kruse_v-k := predsort.
      krusle_v-k := predsort.
      krydre_v-k := predsort.
      kry_v-k := predsort.
      krympe_v-k := predsort.
      kryne_v-k := predsort.
      krype_v-k := predsort.
      kryptere_v-k := predsort.
      kryptifisere_v-k := predsort.
      krysse_v-k := predsort.
      kryssforhøre_v-k := predsort.
      kryssklippe_v-k := predsort.
      krystallisere_v-k := predsort.
      kryste_v-k := predsort.
      kubbe_v-k := predsort.
      kube_v-k := predsort.
      kubere_v-k := predsort.
      kue_v-k := predsort.
      kujonere_v-k := predsort.
      kukelure_v-k := predsort.
      kuldslå_v-k := predsort.
      kule_v-k := predsort.
      kulle_v-k := predsort.
      kullkaste_v-k := predsort.
      kull-lyse_v-k := predsort.
      kulllyse_v-k := predsort.
      kullsegle_v-k := predsort.
      kullseile_v-k := predsort.
      kullyse_v-k := predsort.
      kulminere_v-k := predsort.
      kulse_v-k := predsort.
      kulske_v-k := predsort.
      kulte_v-k := predsort.
      kultivere_v-k := predsort.
      kumle_v-k := predsort.
      kumulere_v-k := predsort.
      kunne_v-k := predsort.
      kunngjøre_v-k := predsort.
      kupere_v-k := predsort.
      kuple_v-k := predsort.
      kuppe_v-k := predsort.
      kure_v-k := predsort.
      kurere_v-k := predsort.
      kurle_v-k := predsort.
      kurre_v-k := predsort.
      kurse_v-k := predsort.
      kursere_v-k := predsort.
      kursivere_v-k := predsort.
      kurtisere_v-k := predsort.
      kurve_v-k := predsort.
      kuske_v-k := predsort.
      kuste_v-k := predsort.
      kute_v-k := predsort.
      kutte_v-k := predsort.
      kuve_v-k := predsort.
      kuvende_v-k := predsort.
      kvadrere_v-k := predsort.
      kvæve_v-k := predsort.
      kvakke_v-k := predsort.
      kvalifisere_v-k := predsort.
      kvamne_v-k := predsort.
      kvantifisere_v-k := predsort.
      kvantitere_v-k := predsort.
      kvare_v-k := predsort.
      kvarte_v-k := predsort.
      kvaste_v-k := predsort.
      kvede_v-k := predsort.
      kvee_v-k := predsort.
      kvege_v-k := predsort.
      kveike_v-k := predsort.
      kveile_v-k := predsort.
      kvekke_v-k := predsort.
      kvelde_v-k := predsort.
      kvele_v-k := predsort.
      kvelve_v-k := predsort.
      kveppe_v-k := predsort.
      kverke_v-k := predsort.
      kverne_v-k := predsort.
      kverrsette_v-k := predsort.
      kverulere_v-k := predsort.
      kverve_v-k := predsort.
      kvese_v-k := predsort.
      kvesse_v-k := predsort.
      kveste_v-k := predsort.
      kvie_v-k := predsort.
      kvikke_v-k := predsort.
      kvikne_v-k := predsort.
      kvi_v-k := predsort.
      kvile_v-k := predsort.
      kvine_v-k := predsort.
      kvinke_v-k := predsort.
      kviste_v-k := predsort.
      kvite_v-k := predsort.
      kvitne_v-k := predsort.
      kvitre_v-k := predsort.
      kvitte_v-k := predsort.
      kvittere_v-k := predsort.
      kvitvaske_v-k := predsort.
      kvotebestemme_v-k := predsort.
      kvotere_v-k := predsort.
      kyle_v-k := predsort.
      kylle_v-k := predsort.
      kysse_v-k := predsort.
      kyte_v-k := predsort.
      labbe_v-k := predsort.
      labialisere_v-k := predsort.
      lade_v-k := predsort.
      lægre_v-k := predsort.
      lære_v-k := predsort.
      lafte_v-k := predsort.
      lage_v-k := predsort.
      lagerføre_v-k := predsort.
      lagge_v-k := predsort.
      lagre_v-k := predsort.
      lake_v-k := predsort.
      lakke_v-k := predsort.
      lakkere_v-k := predsort.
      la_v-k := predsort.
      laksere_v-k := predsort.
      lale_v-k := predsort.
      lalle_v-k := predsort.
      laminere_v-k := predsort.
      lamme_v-k := predsort.
      lamslå_v-k := predsort.
      lande_v-k := predsort.
      landsette_v-k := predsort.
      landsforvise_v-k := predsort.
      låne_v-k := predsort.
      lange_v-k := predsort.
      lansere_v-k := predsort.
      lånta_v-k := predsort.
      lappe_v-k := predsort.
      låre_v-k := predsort.
      larme_v-k := predsort.
      låse_v-k := predsort.
      lasere_v-k := predsort.
      laske_v-k := predsort.
      laste_v-k := predsort.
      late_v-k := predsort.
      låte_v-k := predsort.
      latinisere_v-k := predsort.
      latterliggjøre_v-k := predsort.
      lauge_v-k := predsort.
      lausgi_v-k := predsort.
      lausgjøre_v-k := predsort.
      lauslate_v-k := predsort.
      lausrive_v-k := predsort.
      lauve_v-k := predsort.
      lave_v-k := predsort.
      layoute_v-k := predsort.
      lease_v-k := predsort.
      ledde_v-k := predsort.
      lede_v-k := predsort.
      ledes_v-k := predsort.
      ledsage_v-k := predsort.
      lee_v-k := predsort.
      lefle_v-k := predsort.
      legalisere_v-k := predsort.
      lege_v-k := predsort.
      legemliggjøre_v-k := predsort.
      legere_v-k := predsort.
      legge_v-k := predsort.
      legitimere_v-k := predsort.
      leie_v-k := predsort.
      leies_v-k := predsort.
      leike_v-k := predsort.
      leire_v-k := predsort.
      leite_v-k := predsort.
      leive_v-k := predsort.
      leke_v-k := predsort.
      lekke_v-k := predsort.
      le_v-k := predsort.
      lekre_v-k := predsort.
      lekse_v-k := predsort.
      lemleste_v-k := predsort.
      lemmatisere_v-k := predsort.
      lemme_v-k := predsort.
      lempe_v-k := predsort.
      lemstre_v-k := predsort.
      lene_v-k := predsort.
      lenges_v-k := predsort.
      lengte_v-k := predsort.
      lenke_v-k := predsort.
      lense_v-k := predsort.
      lepe_v-k := predsort.
      lepje_v-k := predsort.
      lese_v-k := predsort.
      leske_v-k := predsort.
      lespe_v-k := predsort.
      lesse_v-k := predsort.
      lete_v-k := predsort.
      letne_v-k := predsort.
      lette_v-k := predsort.
      leve_v-k := predsort.
      levendegjøre_v-k := predsort.
      levere_v-k := predsort.
      levne_v-k := predsort.
      levre_v-k := predsort.
      liberalisere_v-k := predsort.
      lide_v-k := predsort.
      ligge_v-k := predsort.
      ligne_v-k := predsort.
      lignifisere_v-k := predsort.
      like_v-k := predsort.
      likerette_v-k := predsort.
      likestille_v-k := predsort.
      likne_v-k := predsort.
      li_v-k := predsort.
      likvidere_v-k := predsort.
      lime_v-k := predsort.
      lindre_v-k := predsort.
      linjere_v-k := predsort.
      linne_v-k := predsort.
      linnkoke_v-k := predsort.
      linnsalte_v-k := predsort.
      lire_v-k := predsort.
      lirke_v-k := predsort.
      lisensgodkjenne_v-k := predsort.
      lisensiere_v-k := predsort.
      lisitere_v-k := predsort.
      lisse_v-k := predsort.
      liste_v-k := predsort.
      lite_v-k := predsort.
      litografere_v-k := predsort.
      livberge_v-k := predsort.
      live_v-k := predsort.
      livnære_v-k := predsort.
      livne_v-k := predsort.
      livsforsikre_v-k := predsort.
      ljome_v-k := predsort.
      ljuge_v-k := predsort.
      lobbe_v-k := predsort.
      lodde_v-k := predsort.
      loe_v-k := predsort.
      løe_v-k := predsort.
      loffe_v-k := predsort.
      løfte_v-k := predsort.
      logge_v-k := predsort.
      logre_v-k := predsort.
      lokalbedøve_v-k := predsort.
      lokalisere_v-k := predsort.
      lokke_v-k := predsort.
      lombardere_v-k := predsort.
      longere_v-k := predsort.
      lønne_v-k := predsort.
      loope_v-k := predsort.
      løpe_v-k := predsort.
      loppe_v-k := predsort.
      lorgnettere_v-k := predsort.
      lose_v-k := predsort.
      løse_v-k := predsort.
      løsgi_v-k := predsort.
      løsgjøre_v-k := predsort.
      losjere_v-k := predsort.
      løslate_v-k := predsort.
      løsne_v-k := predsort.
      løsningsorientere_v-k := predsort.
      løsrive_v-k := predsort.
      losse_v-k := predsort.
      love_v-k := predsort.
      lovfeste_v-k := predsort.
      lovheimle_v-k := predsort.
      lovhjemle_v-k := predsort.
      lovkvede_v-k := predsort.
      lovprise_v-k := predsort.
      lovsynge_v-k := predsort.
      løye_v-k := predsort.
      løyne_v-k := predsort.
      løype_v-k := predsort.
      løyse_v-k := predsort.
      løyve_v-k := predsort.
      lue_v-k := predsort.
      luffe_v-k := predsort.
      lufte_v-k := predsort.
      lugge_v-k := predsort.
      lugne_v-k := predsort.
      luke_v-k := predsort.
      lukke_v-k := predsort.
      lukte_v-k := predsort.
      lulle_v-k := predsort.
      lumpe_v-k := predsort.
      lune_v-k := predsort.
      lunke_v-k := predsort.
      lunne_v-k := predsort.
      lunsje_v-k := predsort.
      lunte_v-k := predsort.
      lure_v-k := predsort.
      lurve_v-k := predsort.
      luse_v-k := predsort.
      luske_v-k := predsort.
      lute_v-k := predsort.
      lutre_v-k := predsort.
      lyde_v-k := predsort.
      lydisolere_v-k := predsort.
      lye_v-k := predsort.
      lyge_v-k := predsort.
      lykkes_v-k := predsort.
      lykkønske_v-k := predsort.
      lyne_v-k := predsort.
      lynsje_v-k := predsort.
      lyse_v-k := predsort.
      lyske_v-k := predsort.
      lysne_v-k := predsort.
      lyste_v-k := predsort.
      lystre_v-k := predsort.
      lytte_v-k := predsort.
      lyve_v-k := predsort.
      mæle_v-k := predsort.
      mæte_v-k := predsort.
      magasinere_v-k := predsort.
      mage_v-k := predsort.
      magnetisere_v-k := predsort.
      magre_v-k := predsort.
      maie_v-k := predsort.
      makadamisere_v-k := predsort.
      make_v-k := predsort.
      måke_v-k := predsort.
      maksimalisere_v-k := predsort.
      maksimere_v-k := predsort.
      makte_v-k := predsort.
      maktstele_v-k := predsort.
      maktstjele_v-k := predsort.
      makulere_v-k := predsort.
      målbære_v-k := predsort.
      målbinde_v-k := predsort.
      male_v-k := predsort.
      måle_v-k := predsort.
      malte_v-k := predsort.
      maltraktere_v-k := predsort.
      mane_v-k := predsort.
      mangedoble_v-k := predsort.
      mangfoldiggjøre_v-k := predsort.
      mangle_v-k := predsort.
      manifestere_v-k := predsort.
      manikyrere_v-k := predsort.
      manipulere_v-k := predsort.
      mankere_v-k := predsort.
      manne_v-k := predsort.
      mannsoppdekke_v-k := predsort.
      manøvrere_v-k := predsort.
      manudusere_v-k := predsort.
      måpe_v-k := predsort.
      margstele_v-k := predsort.
      margstjele_v-k := predsort.
      marinere_v-k := predsort.
      markedsføre_v-k := predsort.
      markere_v-k := predsort.
      marknadsføre_v-k := predsort.
      marme_v-k := predsort.
      marmorere_v-k := predsort.
      marsjere_v-k := predsort.
      martre_v-k := predsort.
      mase_v-k := predsort.
      maskere_v-k := predsort.
      maskinere_v-k := predsort.
      maskinklippe_v-k := predsort.
      maskinklyppe_v-k := predsort.
      maskinskrive_v-k := predsort.
      maskinstrikke_v-k := predsort.
      maskulinisere_v-k := predsort.
      massakrere_v-k := predsort.
      masseprodusere_v-k := predsort.
      massere_v-k := predsort.
      maste_v-k := predsort.
      masturbere_v-k := predsort.
      matberge_v-k := predsort.
      matche_v-k := predsort.
      mate_v-k := predsort.
      materialisere_v-k := predsort.
      matrikulere_v-k := predsort.
      matte_v-k := predsort.
      måtte_v-k := predsort.
      mattere_v-k := predsort.
      maule_v-k := predsort.
      medbringe_v-k := predsort.
      meddele_v-k := predsort.
      medføre_v-k := predsort.
      medgi_v-k := predsort.
      medisinere_v-k := predsort.
      meditere_v-k := predsort.
      medunderskrive_v-k := predsort.
      medvirke_v-k := predsort.
      megle_v-k := predsort.
      meie_v-k := predsort.
      meine_v-k := predsort.
      meisle_v-k := predsort.
      meite_v-k := predsort.
      mekanisere_v-k := predsort.
      mekke_v-k := predsort.
      mekle_v-k := predsort.
      mekre_v-k := predsort.
      melde_v-k := predsort.
      mele_v-k := predsort.
      melere_v-k := predsort.
      melke_v-k := predsort.
      mellomlande_v-k := predsort.
      melte_v-k := predsort.
      memorere_v-k := predsort.
      mene_v-k := predsort.
      menge_v-k := predsort.
      menstruere_v-k := predsort.
      mentalundersøke_v-k := predsort.
      mergle_v-k := predsort.
      mergstele_v-k := predsort.
      mergstjele_v-k := predsort.
      meritere_v-k := predsort.
      merittere_v-k := predsort.
      merke_v-k := predsort.
      merle_v-k := predsort.
      meske_v-k := predsort.
      messe_v-k := predsort.
      mestre_v-k := predsort.
      metallisere_v-k := predsort.
      mette_v-k := predsort.
      midle_v-k := predsort.
      mige_v-k := predsort.
      migrere_v-k := predsort.
      mikroskopere_v-k := predsort.
      mikse_v-k := predsort.
      mildne_v-k := predsort.
      militarisere_v-k := predsort.
      mime_v-k := predsort.
      mimre_v-k := predsort.
      minelegge_v-k := predsort.
      minere_v-k := predsort.
      miniatyrisere_v-k := predsort.
      minimalisere_v-k := predsort.
      minimere_v-k := predsort.
      minke_v-k := predsort.
      minne_v-k := predsort.
      minnes_v-k := predsort.
      minske_v-k := predsort.
      misbillige_v-k := predsort.
      misbruke_v-k := predsort.
      misfare_v-k := predsort.
      misforstå_v-k := predsort.
      mishage_v-k := predsort.
      mishandle_v-k := predsort.
      mishøre_v-k := predsort.
      misjonere_v-k := predsort.
      miskjenne_v-k := predsort.
      miskle_v-k := predsort.
      miskunne_v-k := predsort.
      misligholde_v-k := predsort.
      mislike_v-k := predsort.
      mislykkes_v-k := predsort.
      misoppfatte_v-k := predsort.
      misregne_v-k := predsort.
      misrekne_v-k := predsort.
      misse_v-k := predsort.
      missnakke_v-k := predsort.
      mista_v-k := predsort.
      miste_v-k := predsort.
      mistenke_v-k := predsort.
      mistenkeliggjøre_v-k := predsort.
      mistolke_v-k := predsort.
      mistrives_v-k := predsort.
      mistro_v-k := predsort.
      mistru_v-k := predsort.
      misunne_v-k := predsort.
      mjaue_v-k := predsort.
      mjøle_v-k := predsort.
      mjølke_v-k := predsort.
      mjuke_v-k := predsort.
      mjuklande_v-k := predsort.
      mjukne_v-k := predsort.
      mobbe_v-k := predsort.
      mobilisere_v-k := predsort.
      møblere_v-k := predsort.
      modellere_v-k := predsort.
      moderere_v-k := predsort.
      modernisere_v-k := predsort.
      modifisere_v-k := predsort.
      modne_v-k := predsort.
      modnes_v-k := predsort.
      modulere_v-k := predsort.
      møkke_v-k := predsort.
      molde_v-k := predsort.
      molestere_v-k := predsort.
      mompe_v-k := predsort.
      mønje_v-k := predsort.
      monne_v-k := predsort.
      monoftongere_v-k := predsort.
      monopolisere_v-k := predsort.
      mønstre_v-k := predsort.
      montere_v-k := predsort.
      moppe_v-k := predsort.
      moralisere_v-k := predsort.
      mørbanke_v-k := predsort.
      more_v-k := predsort.
      morgne_v-k := predsort.
      mørke_v-k := predsort.
      mørklegge_v-k := predsort.
      morkne_v-k := predsort.
      mørkne_v-k := predsort.
      mørne_v-k := predsort.
      morse_v-k := predsort.
      morske_v-k := predsort.
      mortifisere_v-k := predsort.
      mose_v-k := predsort.
      mosjonere_v-k := predsort.
      motarbeide_v-k := predsort.
      motbevise_v-k := predsort.
      møte_v-k := predsort.
      møtes_v-k := predsort.
      motivere_v-k := predsort.
      motorisere_v-k := predsort.
      motsette_v-k := predsort.
      motsi_v-k := predsort.
      motstå_v-k := predsort.
      motsvare_v-k := predsort.
      motta_v-k := predsort.
      motvirke_v-k := predsort.
      movere_v-k := predsort.
      mudre_v-k := predsort.
      mugne_v-k := predsort.
      mukke_v-k := predsort.
      mulde_v-k := predsort.
      mulebinde_v-k := predsort.
      mule_v-k := predsort.
      muliggjøre_v-k := predsort.
      muljere_v-k := predsort.
      mulkte_v-k := predsort.
      mulktere_v-k := predsort.
      mulle_v-k := predsort.
      mulne_v-k := predsort.
      mulre_v-k := predsort.
      multiplisere_v-k := predsort.
      mumifisere_v-k := predsort.
      mumisere_v-k := predsort.
      mumle_v-k := predsort.
      mumse_v-k := predsort.
      munne_v-k := predsort.
      munnhogges_v-k := predsort.
      munnhugges_v-k := predsort.
      muntre_v-k := predsort.
      mure_v-k := predsort.
      murre_v-k := predsort.
      musisere_v-k := predsort.
      mussere_v-k := predsort.
      mute_v-k := predsort.
      mutere_v-k := predsort.
      mygle_v-k := predsort.
      myke_v-k := predsort.
      myklande_v-k := predsort.
      mykne_v-k := predsort.
      myldre_v-k := predsort.
      mynte_v-k := predsort.
      myrde_v-k := predsort.
      myse_v-k := predsort.
      mystifisere_v-k := predsort.
      myte_v-k := predsort.
      nåde_v-k := predsort.
      nære_v-k := predsort.
      nærlese_v-k := predsort.
      nærme_v-k := predsort.
      nage_v-k := predsort.
      nagle_v-k := predsort.
      nå_v-k := predsort.
      nappe_v-k := predsort.
      narkotikarelatere_v-k := predsort.
      narkotisere_v-k := predsort.
      narre_v-k := predsort.
      nasalere_v-k := predsort.
      nasjonalisere_v-k := predsort.
      naske_v-k := predsort.
      nåtle_v-k := predsort.
      natte_v-k := predsort.
      naturalisere_v-k := predsort.
      naube_v-k := predsort.
      naue_v-k := predsort.
      navigere_v-k := predsort.
      navle_v-k := predsort.
      navne_v-k := predsort.
      navngi_v-k := predsort.
      nazifisere_v-k := predsort.
      nebbes_v-k := predsort.
      nedarves_v-k := predsort.
      nedbetale_v-k := predsort.
      nedbrenne_v-k := predsort.
      nedbryte_v-k := predsort.
      nedbygge_v-k := predsort.
      neddemme_v-k := predsort.
      nedfelle_v-k := predsort.
      nedgradere_v-k := predsort.
      nedkalle_v-k := predsort.
      nedkjempe_v-k := predsort.
      nedkjøle_v-k := predsort.
      nedkjøre_v-k := predsort.
      nedkomme_v-k := predsort.
      nedlate_v-k := predsort.
      nedlegge_v-k := predsort.
      nedmolde_v-k := predsort.
      nedmulde_v-k := predsort.
      nedprioritere_v-k := predsort.
      nedringe_v-k := predsort.
      nedsable_v-k := predsort.
      nedsette_v-k := predsort.
      nedskrive_v-k := predsort.
      nedstamme_v-k := predsort.
      nedstemme_v-k := predsort.
      nedtegne_v-k := predsort.
      nedtrappe_v-k := predsort.
      nedverdige_v-k := predsort.
      nedvurdere_v-k := predsort.
      negere_v-k := predsort.
      neglisjere_v-k := predsort.
      neie_v-k := predsort.
      nekte_v-k := predsort.
      nenne_v-k := predsort.
      nette_v-k := predsort.
      nevehelse_v-k := predsort.
      nevehilse_v-k := predsort.
      nevetakes_v-k := predsort.
      nevne_v-k := predsort.
      nidoble_v-k := predsort.
      niholde_v-k := predsort.
      nikke_v-k := predsort.
      nippe_v-k := predsort.
      nisse_v-k := predsort.
      niste_v-k := predsort.
      nistirre_v-k := predsort.
      nitrere_v-k := predsort.
      nitrifisere_v-k := predsort.
      nitte_v-k := predsort.
      nivellere_v-k := predsort.
      nøde_v-k := predsort.
      nødlande_v-k := predsort.
      nødslakte_v-k := predsort.
      nødvendiggjøre_v-k := predsort.
      nøffe_v-k := predsort.
      nø_v-k := predsort.
      nøle_v-k := predsort.
      nomadisere_v-k := predsort.
      nominere_v-k := predsort.
      nonchalere_v-k := predsort.
      nøre_v-k := predsort.
      normalisere_v-k := predsort.
      normere_v-k := predsort.
      nøste_v-k := predsort.
      notere_v-k := predsort.
      notifisere_v-k := predsort.
      nøye_v-k := predsort.
      nøyte_v-k := predsort.
      nøytralisere_v-k := predsort.
      nubbe_v-k := predsort.
      nudde_v-k := predsort.
      nufse_v-k := predsort.
      nugge_v-k := predsort.
      nulle_v-k := predsort.
      nummerere_v-k := predsort.
      nuppe_v-k := predsort.
      nupre_v-k := predsort.
      nuske_v-k := predsort.
      nusle_v-k := predsort.
      nusse_v-k := predsort.
      nyansere_v-k := predsort.
      nyansette_v-k := predsort.
      nyåpne_v-k := predsort.
      nydanne_v-k := predsort.
      nydyrke_v-k := predsort.
      nyetablere_v-k := predsort.
      nykomponere_v-k := predsort.
      nynne_v-k := predsort.
      nype_v-k := predsort.
      nyrydde_v-k := predsort.
      nyse_v-k := predsort.
      nyskape_v-k := predsort.
      nystarte_v-k := predsort.
      nyte_v-k := predsort.
      nytte_v-k := predsort.
      nyttiggjøre_v-k := predsort.
      nyvåkne_v-k := predsort.
      nyve_v-k := predsort.
      obdusere_v-k := predsort.
      objektivere_v-k := predsort.
      observere_v-k := predsort.
      obstruere_v-k := predsort.
      øde_v-k := predsort.
      ødelegge_v-k := predsort.
      odle_v-k := predsort.
      ødsle_v-k := predsort.
      offentliggjøre_v-k := predsort.
      ofre_v-k := predsort.
      oie_v-k := predsort.
      øke_v-k := predsort.
      okke_v-k := predsort.
      okkultere_v-k := predsort.
      okkupere_v-k := predsort.
      økonomisere_v-k := predsort.
      økse_v-k := predsort.
      oksidere_v-k := predsort.
      øksle_v-k := predsort.
      økte_v-k := predsort.
      okulere_v-k := predsort.
      øle_v-k := predsort.
      olje_v-k := predsort.
      olme_v-k := predsort.
      omadressere_v-k := predsort.
      omarbeide_v-k := predsort.
      ombære_v-k := predsort.
      ombestemme_v-k := predsort.
      ombrekke_v-k := predsort.
      ombringe_v-k := predsort.
      omdanne_v-k := predsort.
      omdefinere_v-k := predsort.
      omfamne_v-k := predsort.
      omfatte_v-k := predsort.
      omfavne_v-k := predsort.
      omforme_v-k := predsort.
      omgåes_v-k := predsort.
      omgå_v-k := predsort.
      omgås_v-k := predsort.
      omgi_v-k := predsort.
      omgjøre_v-k := predsort.
      omhandle_v-k := predsort.
      ominnrede_v-k := predsort.
      omkalfatre_v-k := predsort.
      omkomme_v-k := predsort.
      omkranse_v-k := predsort.
      ømme_v-k := predsort.
      ommøblere_v-k := predsort.
      omorganisere_v-k := predsort.
      omplante_v-k := predsort.
      ompotte_v-k := predsort.
      omprikle_v-k := predsort.
      omprioritere_v-k := predsort.
      omprøve_v-k := predsort.
      områ_v-k := predsort.
      omramme_v-k := predsort.
      omredigere_v-k := predsort.
      omregne_v-k := predsort.
      omrekne_v-k := predsort.
      omringe_v-k := predsort.
      omrømme_v-k := predsort.
      omsette_v-k := predsort.
      omskape_v-k := predsort.
      omskifte_v-k := predsort.
      omskjære_v-k := predsort.
      omskole_v-k := predsort.
      omskolere_v-k := predsort.
      omskrive_v-k := predsort.
      omslutte_v-k := predsort.
      omslynge_v-k := predsort.
      omstarte_v-k := predsort.
      omstille_v-k := predsort.
      omstøte_v-k := predsort.
      omstrukturere_v-k := predsort.
      omstyrte_v-k := predsort.
      omtale_v-k := predsort.
      omvandle_v-k := predsort.
      omvelte_v-k := predsort.
      omvende_v-k := predsort.
      omvøle_v-k := predsort.
      omvurdere_v-k := predsort.
      onanere_v-k := predsort.
      ondulere_v-k := predsort.
      onne_v-k := predsort.
      ønske_v-k := predsort.
      ønsketenke_v-k := predsort.
      opalescere_v-k := predsort.
      opalisere_v-k := predsort.
      operasjonalisere_v-k := predsort.
      operere_v-k := predsort.
      oppbevare_v-k := predsort.
      oppbringe_v-k := predsort.
      oppbruke_v-k := predsort.
      oppbygge_v-k := predsort.
      oppby_v-k := predsort.
      oppdage_v-k := predsort.
      oppdatere_v-k := predsort.
      oppdra_v-k := predsort.
      oppdrette_v-k := predsort.
      oppdrive_v-k := predsort.
      oppdyrke_v-k := predsort.
      oppebære_v-k := predsort.
      oppebie_v-k := predsort.
      oppe_v-k := predsort.
      oppelske_v-k := predsort.
      oppfange_v-k := predsort.
      oppfatte_v-k := predsort.
      oppfinne_v-k := predsort.
      oppflaske_v-k := predsort.
      oppfordre_v-k := predsort.
      oppføre_v-k := predsort.
      oppfostre_v-k := predsort.
      oppfylle_v-k := predsort.
      oppgi_v-k := predsort.
      oppgradere_v-k := predsort.
      oppheve_v-k := predsort.
      opphisse_v-k := predsort.
      oppholde_v-k := predsort.
      opphøre_v-k := predsort.
      opphøye_v-k := predsort.
      oppildne_v-k := predsort.
      oppirre_v-k := predsort.
      oppjustere_v-k := predsort.
      oppkalle_v-k := predsort.
      oppkaste_v-k := predsort.
      oppklare_v-k := predsort.
      oppkonstruere_v-k := predsort.
      opplate_v-k := predsort.
      oppleve_v-k := predsort.
      opplive_v-k := predsort.
      oppløfte_v-k := predsort.
      oppløse_v-k := predsort.
      oppløyse_v-k := predsort.
      opplyse_v-k := predsort.
      oppmagasinere_v-k := predsort.
      oppmuntre_v-k := predsort.
      oppnå_v-k := predsort.
      oppnevne_v-k := predsort.
      oppnorske_v-k := predsort.
      opponere_v-k := predsort.
      oppøve_v-k := predsort.
      opprede_v-k := predsort.
      oppreise_v-k := predsort.
      oppreklamere_v-k := predsort.
      opprette_v-k := predsort.
      opprettholde_v-k := predsort.
      oppringe_v-k := predsort.
      opprinne_v-k := predsort.
      opprøre_v-k := predsort.
      oppruste_v-k := predsort.
      oppsette_v-k := predsort.
      oppskrive_v-k := predsort.
      oppsluke_v-k := predsort.
      oppslutte_v-k := predsort.
      oppsøke_v-k := predsort.
      oppsparke_v-k := predsort.
      oppspore_v-k := predsort.
      oppstå_v-k := predsort.
      oppstykke_v-k := predsort.
      oppsummere_v-k := predsort.
      oppta_v-k := predsort.
      opptegne_v-k := predsort.
      opptelle_v-k := predsort.
      opptenne_v-k := predsort.
      opptjene_v-k := predsort.
      opptrappe_v-k := predsort.
      opptre_v-k := predsort.
      oppvarte_v-k := predsort.
      oppveie_v-k := predsort.
      oppvekke_v-k := predsort.
      oppvigle_v-k := predsort.
      oppvise_v-k := predsort.
      oppvurdere_v-k := predsort.
      optere_v-k := predsort.
      optimalisere_v-k := predsort.
      optimere_v-k := predsort.
      ordinere_v-k := predsort.
      ordlegge_v-k := predsort.
      ordne_v-k := predsort.
      oreigne_v-k := predsort.
      øre_v-k := predsort.
      øremerke_v-k := predsort.
      organisere_v-k := predsort.
      orge_v-k := predsort.
      orientere_v-k := predsort.
      orke_v-k := predsort.
      orkestrere_v-k := predsort.
      orme_v-k := predsort.
      ornamentere_v-k := predsort.
      ørske_v-k := predsort.
      ørte_v-k := predsort.
      oscillere_v-k := predsort.
      ose_v-k := predsort.
      øse_v-k := predsort.
      øsregne_v-k := predsort.
      oste_v-k := predsort.
      otre_v-k := predsort.
      øve_v-k := predsort.
      overanstrenge_v-k := predsort.
      overbelaste_v-k := predsort.
      overbeskatte_v-k := predsort.
      overbeskytte_v-k := predsort.
      overbetone_v-k := predsort.
      overbevise_v-k := predsort.
      overbringe_v-k := predsort.
      overbrodere_v-k := predsort.
      overby_v-k := predsort.
      overdimensjonere_v-k := predsort.
      overdosere_v-k := predsort.
      overdøve_v-k := predsort.
      overdra_v-k := predsort.
      overdrive_v-k := predsort.
      overdynge_v-k := predsort.
      overeksponere_v-k := predsort.
      overfalle_v-k := predsort.
      overflødiggjøre_v-k := predsort.
      overfløye_v-k := predsort.
      overføre_v-k := predsort.
      overforsikre_v-k := predsort.
      overfuse_v-k := predsort.
      overgå_v-k := predsort.
      overgi_v-k := predsort.
      overgrepstiltale_v-k := predsort.
      overhale_v-k := predsort.
      overholde_v-k := predsort.
      overhøre_v-k := predsort.
      overhøvle_v-k := predsort.
      overkjøre_v-k := predsort.
      overkomme_v-k := predsort.
      overkompensere_v-k := predsort.
      overlaste_v-k := predsort.
      overlate_v-k := predsort.
      overlegge_v-k := predsort.
      overlesse_v-k := predsort.
      overleve_v-k := predsort.
      overlevere_v-k := predsort.
      overliste_v-k := predsort.
      overmanne_v-k := predsort.
      overmette_v-k := predsort.
      overnatte_v-k := predsort.
      overopphete_v-k := predsort.
      overøse_v-k := predsort.
      overprøve_v-k := predsort.
      overraske_v-k := predsort.
      overrekke_v-k := predsort.
      overrenne_v-k := predsort.
      overrisle_v-k := predsort.
      overrumple_v-k := predsort.
      oversende_v-k := predsort.
      oversette_v-k := predsort.
      oversitte_v-k := predsort.
      overskride_v-k := predsort.
      overskue_v-k := predsort.
      overskygge_v-k := predsort.
      overspille_v-k := predsort.
      overstige_v-k := predsort.
      oversvømme_v-k := predsort.
      overta_v-k := predsort.
      overtale_v-k := predsort.
      overtegne_v-k := predsort.
      overtreffe_v-k := predsort.
      overtrekke_v-k := predsort.
      overtre_v-k := predsort.
      overtyde_v-k := predsort.
      overutvikle_v-k := predsort.
      overvære_v-k := predsort.
      overvåke_v-k := predsort.
      overveie_v-k := predsort.
      overvelde_v-k := predsort.
      overvinne_v-k := predsort.
      overvintre_v-k := predsort.
      overvurdere_v-k := predsort.
      ovre_v-k := predsort.
      øyde_v-k := predsort.
      øydelegge_v-k := predsort.
      øye_v-k := predsort.
      øyne_v-k := predsort.
      påakte_v-k := predsort.
      påanke_v-k := predsort.
      påberope_v-k := predsort.
      påby_v-k := predsort.
      pace_v-k := predsort.
      padle_v-k := predsort.
      pådømme_v-k := predsort.
      pådra_v-k := predsort.
      pæle_v-k := predsort.
      pælme_v-k := predsort.
      paffe_v-k := predsort.
      påføre_v-k := predsort.
      pågå_v-k := predsort.
      paginere_v-k := predsort.
      pågripe_v-k := predsort.
      påhvile_v-k := predsort.
      påkalle_v-k := predsort.
      påkjære_v-k := predsort.
      påkjøre_v-k := predsort.
      pakke_v-k := predsort.
      påklage_v-k := predsort.
      pakte_v-k := predsort.
      palatalisere_v-k := predsort.
      pålegge_v-k := predsort.
      pale_v-k := predsort.
      påligge_v-k := predsort.
      palletere_v-k := predsort.
      palme_v-k := predsort.
      påløpe_v-k := predsort.
      palpere_v-k := predsort.
      panegyrere_v-k := predsort.
      panele_v-k := predsort.
      panere_v-k := predsort.
      panorere_v-k := predsort.
      pansre_v-k := predsort.
      pante_v-k := predsort.
      pantsette_v-k := predsort.
      pantstille_v-k := predsort.
      påpeke_v-k := predsort.
      pappe_v-k := predsort.
      paradere_v-k := predsort.
      parafere_v-k := predsort.
      parafinere_v-k := predsort.
      parafrasere_v-k := predsort.
      paragrafere_v-k := predsort.
      parallellføre_v-k := predsort.
      parallellforskyve_v-k := predsort.
      parallellisere_v-k := predsort.
      paralysere_v-k := predsort.
      parasittere_v-k := predsort.
      påregne_v-k := predsort.
      pårekne_v-k := predsort.
      pare_v-k := predsort.
      parere_v-k := predsort.
      parfymere_v-k := predsort.
      parkere_v-k := predsort.
      parlamentere_v-k := predsort.
      parlere_v-k := predsort.
      parodiere_v-k := predsort.
      parsellere_v-k := predsort.
      partere_v-k := predsort.
      partisipere_v-k := predsort.
      påse_v-k := predsort.
      pasifisere_v-k := predsort.
      påskjønne_v-k := predsort.
      påskynde_v-k := predsort.
      passe_v-k := predsort.
      passere_v-k := predsort.
      passiare_v-k := predsort.
      passivere_v-k := predsort.
      passivisere_v-k := predsort.
      påstå_v-k := predsort.
      pasteurisere_v-k := predsort.
      påsy_v-k := predsort.
      påta_v-k := predsort.
      påtale_v-k := predsort.
      påtegne_v-k := predsort.
      påtenne_v-k := predsort.
      patentere_v-k := predsort.
      patentsøke_v-k := predsort.
      patinere_v-k := predsort.
      påtreffe_v-k := predsort.
      patronisere_v-k := predsort.
      patruljere_v-k := predsort.
      patte_v-k := predsort.
      påtvinge_v-k := predsort.
      pausere_v-k := predsort.
      påvirke_v-k := predsort.
      påvise_v-k := predsort.
      peile_v-k := predsort.
      peise_v-k := predsort.
      peke_v-k := predsort.
      pelle_v-k := predsort.
      pelletere_v-k := predsort.
      pelme_v-k := predsort.
      pelse_v-k := predsort.
      pendle_v-k := predsort.
      penetrere_v-k := predsort.
      penne_v-k := predsort.
      pense_v-k := predsort.
      pensjonere_v-k := predsort.
      pensle_v-k := predsort.
      pepre_v-k := predsort.
      perennere_v-k := predsort.
      perfeksjonere_v-k := predsort.
      perforere_v-k := predsort.
      pergamentere_v-k := predsort.
      perkolere_v-k := predsort.
      perkutere_v-k := predsort.
      perle_v-k := predsort.
      permittere_v-k := predsort.
      permutere_v-k := predsort.
      perse_v-k := predsort.
      persiflere_v-k := predsort.
      persipere_v-k := predsort.
      persistere_v-k := predsort.
      personifisere_v-k := predsort.
      personliggjøre_v-k := predsort.
      perspirere_v-k := predsort.
      perturbere_v-k := predsort.
      pervertere_v-k := predsort.
      pese_v-k := predsort.
      piffe_v-k := predsort.
      pigge_v-k := predsort.
      pigmentere_v-k := predsort.
      pikere_v-k := predsort.
      pikke_v-k := predsort.
      pile_v-k := predsort.
      pilke_v-k := predsort.
      pille_v-k := predsort.
      pilte_v-k := predsort.
      pimpe_v-k := predsort.
      pine_v-k := predsort.
      pinke_v-k := predsort.
      pinne_v-k := predsort.
      pinse_v-k := predsort.
      pipe_v-k := predsort.
      piple_v-k := predsort.
      pippe_v-k := predsort.
      pirke_v-k := predsort.
      pirre_v-k := predsort.
      piruettere_v-k := predsort.
      piske_v-k := predsort.
      pisse_v-k := predsort.
      piste_v-k := predsort.
      pistre_v-k := predsort.
      pitle_v-k := predsort.
      pjalle_v-k := predsort.
      pjatte_v-k := predsort.
      pjoltre_v-k := predsort.
      pjuske_v-k := predsort.
      pjutre_v-k := predsort.
      plaffe_v-k := predsort.
      plage_v-k := predsort.
      plagiere_v-k := predsort.
      plane_v-k := predsort.
      planere_v-k := predsort.
      planke_v-k := predsort.
      planlegge_v-k := predsort.
      plante_v-k := predsort.
      plapre_v-k := predsort.
      plaske_v-k := predsort.
      plassere_v-k := predsort.
      plastre_v-k := predsort.
      platinere_v-k := predsort.
      pledere_v-k := predsort.
      pleie_v-k := predsort.
      plette_v-k := predsort.
      plikte_v-k := predsort.
      plinge_v-k := predsort.
      plire_v-k := predsort.
      plissere_v-k := predsort.
      ploge_v-k := predsort.
      plombere_v-k := predsort.
      pløse_v-k := predsort.
      plotte_v-k := predsort.
      pløye_v-k := predsort.
      pludre_v-k := predsort.
      plugge_v-k := predsort.
      plukke_v-k := predsort.
      plumpe_v-k := predsort.
      plundre_v-k := predsort.
      plusse_v-k := predsort.
      plyndre_v-k := predsort.
      plystre_v-k := predsort.
      pøble_v-k := predsort.
      pochere_v-k := predsort.
      pode_v-k := predsort.
      poengtere_v-k := predsort.
      poetisere_v-k := predsort.
      pokre_v-k := predsort.
      pokulere_v-k := predsort.
      polarisere_v-k := predsort.
      pole_v-k := predsort.
      polemisere_v-k := predsort.
      polere_v-k := predsort.
      politianmelde_v-k := predsort.
      politisere_v-k := predsort.
      pollinere_v-k := predsort.
      polstre_v-k := predsort.
      polymerisere_v-k := predsort.
      pomadisere_v-k := predsort.
      poncere_v-k := predsort.
      ponere_v-k := predsort.
      pønse_v-k := predsort.
      pønske_v-k := predsort.
      poppe_v-k := predsort.
      popularisere_v-k := predsort.
      porsjonere_v-k := predsort.
      portrettere_v-k := predsort.
      pøse_v-k := predsort.
      posere_v-k := predsort.
      posisjonere_v-k := predsort.
      posjere_v-k := predsort.
      postdatere_v-k := predsort.
      poste_v-k := predsort.
      postere_v-k := predsort.
      postlegge_v-k := predsort.
      poststemple_v-k := predsort.
      postulere_v-k := predsort.
      potensere_v-k := predsort.
      potte_v-k := predsort.
      praie_v-k := predsort.
      prakke_v-k := predsort.
      praktisere_v-k := predsort.
      prale_v-k := predsort.
      prange_v-k := predsort.
      prasle_v-k := predsort.
      prate_v-k := predsort.
      predestinere_v-k := predsort.
      predeterminere_v-k := predsort.
      predike_v-k := predsort.
      predikere_v-k := predsort.
      predisponere_v-k := predsort.
      prefabrikkere_v-k := predsort.
      preferere_v-k := predsort.
      prege_v-k := predsort.
      prekavere_v-k := predsort.
      preke_v-k := predsort.
      prelle_v-k := predsort.
      preludere_v-k := predsort.
      premiere_v-k := predsort.
      prente_v-k := predsort.
      prenumerere_v-k := predsort.
      preparere_v-k := predsort.
      preppe_v-k := predsort.
      presentere_v-k := predsort.
      preservere_v-k := predsort.
      presidere_v-k := predsort.
      presisere_v-k := predsort.
      preskribere_v-k := predsort.
      presse_v-k := predsort.
      prestere_v-k := predsort.
      prestevie_v-k := predsort.
      presumere_v-k := predsort.
      presupponere_v-k := predsort.
      pretendere_v-k := predsort.
      prette_v-k := predsort.
      prikke_v-k := predsort.
      prikle_v-k := predsort.
      prime_v-k := predsort.
      primsigne_v-k := predsort.
      prioritere_v-k := predsort.
      prisbelønne_v-k := predsort.
      prise_v-k := predsort.
      prisgi_v-k := predsort.
      privatdrive_v-k := predsort.
      privatisere_v-k := predsort.
      privilegere_v-k := predsort.
      probere_v-k := predsort.
      problematisere_v-k := predsort.
      produsere_v-k := predsort.
      proe_v-k := predsort.
      profanere_v-k := predsort.
      profesjonalisere_v-k := predsort.
      profetere_v-k := predsort.
      profilere_v-k := predsort.
      profittere_v-k := predsort.
      prognosere_v-k := predsort.
      prognostisere_v-k := predsort.
      programfeste_v-k := predsort.
      programmere_v-k := predsort.
      projisere_v-k := predsort.
      proklamere_v-k := predsort.
      proletarisere_v-k := predsort.
      prolongere_v-k := predsort.
      promenere_v-k := predsort.
      promovere_v-k := predsort.
      prompe_v-k := predsort.
      promulgere_v-k := predsort.
      pronere_v-k := predsort.
      pronominalisere_v-k := predsort.
      propagandere_v-k := predsort.
      propagere_v-k := predsort.
      proponere_v-k := predsort.
      proporsjonere_v-k := predsort.
      proppe_v-k := predsort.
      prosedere_v-k := predsort.
      prosentlønne_v-k := predsort.
      prosessere_v-k := predsort.
      prosjektere_v-k := predsort.
      proskribere_v-k := predsort.
      prospektere_v-k := predsort.
      prostituere_v-k := predsort.
      protesjere_v-k := predsort.
      protestere_v-k := predsort.
      protokollere_v-k := predsort.
      protte_v-k := predsort.
      prøvebore_v-k := predsort.
      prøvefilme_v-k := predsort.
      prøvekjøre_v-k := predsort.
      prove_v-k := predsort.
      prøve_v-k := predsort.
      prøveskyte_v-k := predsort.
      proviantere_v-k := predsort.
      providere_v-k := predsort.
      provosere_v-k := predsort.
      pruste_v-k := predsort.
      prute_v-k := predsort.
      pryde_v-k := predsort.
      pryle_v-k := predsort.
      psyke_v-k := predsort.
      psykoanalysere_v-k := predsort.
      ptroe_v-k := predsort.
      ptrotte_v-k := predsort.
      publisere_v-k := predsort.
      pudre_v-k := predsort.
      puffe_v-k := predsort.
      puge_v-k := predsort.
      pugge_v-k := predsort.
      pukke_v-k := predsort.
      puldre_v-k := predsort.
      pule_v-k := predsort.
      pulse_v-k := predsort.
      pulsere_v-k := predsort.
      pulverisere_v-k := predsort.
      pumpebremse_v-k := predsort.
      pumpe_v-k := predsort.
      punche_v-k := predsort.
      punge_v-k := predsort.
      punktere_v-k := predsort.
      purke_v-k := predsort.
      purle_v-k := predsort.
      purre_v-k := predsort.
      pushe_v-k := predsort.
      pusle_v-k := predsort.
      pusse_v-k := predsort.
      puste_v-k := predsort.
      putle_v-k := predsort.
      putre_v-k := predsort.
      putte_v-k := predsort.
      pynte_v-k := predsort.
      rabattere_v-k := predsort.
      rable_v-k := predsort.
      radbrekke_v-k := predsort.
      råde_v-k := predsort.
      radere_v-k := predsort.
      rådføre_v-k := predsort.
      radiere_v-k := predsort.
      radikalisere_v-k := predsort.
      rådlegge_v-k := predsort.
      rådslå_v-k := predsort.
      rådspørre_v-k := predsort.
      raffinere_v-k := predsort.
      rafse_v-k := predsort.
      rafte_v-k := predsort.
      rage_v-k := predsort.
      raide_v-k := predsort.
      rake_v-k := predsort.
      råke_v-k := predsort.
      råkjøre_v-k := predsort.
      rakke_v-k := predsort.
      rakle_v-k := predsort.
      rakne_v-k := predsort.
      rå_v-k := predsort.
      råle_v-k := predsort.
      raljere_v-k := predsort.
      ralle_v-k := predsort.
      ramle_v-k := predsort.
      ramme_v-k := predsort.
      ramponere_v-k := predsort.
      ramsalte_v-k := predsort.
      ramse_v-k := predsort.
      rana_v-k := predsort.
      rande_v-k := predsort.
      randsy_v-k := predsort.
      rane_v-k := predsort.
      råne_v-k := predsort.
      range_v-k := predsort.
      rangere_v-k := predsort.
      rangle_v-k := predsort.
      ranke_v-k := predsort.
      ransake_v-k := predsort.
      rape_v-k := predsort.
      rappe_v-k := predsort.
      rappellere_v-k := predsort.
      rapportere_v-k := predsort.
      rapse_v-k := predsort.
      rase_v-k := predsort.
      rasere_v-k := predsort.
      rasjonalisere_v-k := predsort.
      rasjonere_v-k := predsort.
      raske_v-k := predsort.
      rasle_v-k := predsort.
      raspe_v-k := predsort.
       raste_v-k := predsort.
      ratifisere_v-k := predsort.
      ratle_v-k := predsort.
      råtne_v-k := predsort.
      ratte_v-k := predsort.
      rauke_v-k := predsort.
      rause_v-k := predsort.
      raute_v-k := predsort.
      rave_v-k := predsort.
      reagere_v-k := predsort.
      reaktivere_v-k := predsort.
      realisere_v-k := predsort.
      realitetsbehandle_v-k := predsort.
      reassurere_v-k := predsort.
      rebellere_v-k := predsort.
      recensere_v-k := predsort.
      redde_v-k := predsort.
      redefinere_v-k := predsort.
      redegjøre_v-k := predsort.
      rede_v-k := predsort.
      redigere_v-k := predsort.
      redoble_v-k := predsort.
      reduplisere_v-k := predsort.
      redusere_v-k := predsort.
      referere_v-k := predsort.
      reflektere_v-k := predsort.
      reformere_v-k := predsort.
      refse_v-k := predsort.
      refundere_v-k := predsort.
      refusere_v-k := predsort.
      regenerere_v-k := predsort.
      regissere_v-k := predsort.
      registrere_v-k := predsort.
      regjere_v-k := predsort.
      reglementere_v-k := predsort.
      regne_v-k := predsort.
      regularisere_v-k := predsort.
      regulere_v-k := predsort.
      regummiere_v-k := predsort.
      rehabilitere_v-k := predsort.
      reie_v-k := predsort.
      reike_v-k := predsort.
      reindyrke_v-k := predsort.
      reingjøre_v-k := predsort.
      reinske_v-k := predsort.
      reinskrive_v-k := predsort.
      reinvaske_v-k := predsort.
      reinvestere_v-k := predsort.
      reipe_v-k := predsort.
      reise-v_v-k := predsort.
      reive_v-k := predsort.
      rejisere_v-k := predsort.
      rekapitulere_v-k := predsort.
      reke_v-k := predsort.
      rekke_v-k := predsort.
      reklamere_v-k := predsort.
      rekne_v-k := predsort.
      rekognosere_v-k := predsort.
      rekommandere_v-k := predsort.
      rekonstruere_v-k := predsort.
      rekordforbedre_v-k := predsort.
      re_v-k := predsort.
      rekreere_v-k := predsort.
      rekruttere_v-k := predsort.
      rektifisere_v-k := predsort.
      rekvirere_v-k := predsort.
      relatere_v-k := predsort.
      relativisere_v-k := predsort.
      relegere_v-k := predsort.
      relokere_v-k := predsort.
      rembursere_v-k := predsort.
      reminisere_v-k := predsort.
      remisere_v-k := predsort.
      remittere_v-k := predsort.
      remixe_v-k := predsort.
      remje_v-k := predsort.
      remontere_v-k := predsort.
      remplassere_v-k := predsort.
      remse_v-k := predsort.
      rendyrke_v-k := predsort.
      rengjøre_v-k := predsort.
      renne_v-k := predsort.
      renonsere_v-k := predsort.
      renovere_v-k := predsort.
      rense_v-k := predsort.
      renske_v-k := predsort.
      renskrive_v-k := predsort.
      renvaske_v-k := predsort.
      reorganisere_v-k := predsort.
      reparere_v-k := predsort.
      repatriere_v-k := predsort.
      repetere_v-k := predsort.
      replisere_v-k := predsort.
      representere_v-k := predsort.
      reprodusere_v-k := predsort.
      reseptbelegge_v-k := predsort.
      reservere_v-k := predsort.
      residere_v-k := predsort.
      residivere_v-k := predsort.
      resignere_v-k := predsort.
      resipere_v-k := predsort.
      resirkulere_v-k := predsort.
      resistere_v-k := predsort.
      resitere_v-k := predsort.
      resolvere_v-k := predsort.
      resonnere_v-k := predsort.
      resorbere_v-k := predsort.
      resosialisere_v-k := predsort.
      respektere_v-k := predsort.
      respirere_v-k := predsort.
      respondere_v-k := predsort.
      restarte_v-k := predsort.
      restaurere_v-k := predsort.
      restere_v-k := predsort.
      restituere_v-k := predsort.
      resultere_v-k := predsort.
      resymere_v-k := predsort.
      retardere_v-k := predsort.
      reteste_v-k := predsort.
      retinere_v-k := predsort.
      retirere_v-k := predsort.
      rette_v-k := predsort.
      rettferdiggjøre_v-k := predsort.
      rettkjenne_v-k := predsort.
      rettlede_v-k := predsort.
      rettleie_v-k := predsort.
      returnere_v-k := predsort.
      retusjere_v-k := predsort.
      revaluere_v-k := predsort.
      revansjere_v-k := predsort.
      reve_v-k := predsort.
      reversere_v-k := predsort.
      revidere_v-k := predsort.
      revne_v-k := predsort.
      revoltere_v-k := predsort.
      revolusjonere_v-k := predsort.
      ribbe_v-k := predsort.
      ride_v-k := predsort.
      rifle_v-k := predsort.
      rigge_v-k := predsort.
      rikke_v-k := predsort.
      rikosjettere_v-k := predsort.
      ri_v-k := predsort.
      rikse_v-k := predsort.
      rikte_v-k := predsort.
      rime_v-k := predsort.
      rine_v-k := predsort.
      ringeakte_v-k := predsort.
      ringe_v-k := predsort.
      ringle_v-k := predsort.
      rinne_v-k := predsort.
      ripe_v-k := predsort.
      ripostere_v-k := predsort.
      rippe_v-k := predsort.
      rise_v-k := predsort.
      risikere_v-k := predsort.
      risle_v-k := predsort.
      rispe_v-k := predsort.
      risse_v-k := predsort.
      riste_v-k := predsort.
      ristornere_v-k := predsort.
      rite_v-k := predsort.
      ritualisere_v-k := predsort.
      rivalisere_v-k := predsort.
      rive_v-k := predsort.
      rivne_v-k := predsort.
      robbe_v-k := predsort.
      robotisere_v-k := predsort.
      rocke_v-k := predsort.
      røde_v-k := predsort.
      rødme_v-k := predsort.
      rødne_v-k := predsort.
      roe_v-k := predsort.
      røfle_v-k := predsort.
      røke_v-k := predsort.
      rokere_v-k := predsort.
      rokke_v-k := predsort.
      røklegge_v-k := predsort.
      ro_v-k := predsort.
      røkte_v-k := predsort.
      røle_v-k := predsort.
      rolle_v-k := predsort.
      romanisere_v-k := predsort.
      romantisere_v-k := predsort.
      rømme_v-k := predsort.
      romstere_v-k := predsort.
      røntgenfotografere_v-k := predsort.
      rope_v-k := predsort.
      røpe_v-k := predsort.
      røre_v-k := predsort.
      rose_v-k := predsort.
      rosemale_v-k := predsort.
      røske_v-k := predsort.
      roste_v-k := predsort.
      røste_v-k := predsort.
      rote_v-k := predsort.
      rotere_v-k := predsort.
      rotfeste_v-k := predsort.
      rotfylle_v-k := predsort.
      rotte_v-k := predsort.
      røve_v-k := predsort.
      røyke_v-k := predsort.
      røyklegge_v-k := predsort.
      røyne_v-k := predsort.
      røyse_v-k := predsort.
      røyte_v-k := predsort.
      rubbe_v-k := predsort.
      rubrisere_v-k := predsort.
      rufse_v-k := predsort.
      ruge_v-k := predsort.
      rugge_v-k := predsort.
      rugle_v-k := predsort.
      ruinere_v-k := predsort.
      rukke_v-k := predsort.
      rulle_v-k := predsort.
      rullere_v-k := predsort.
      rulte_v-k := predsort.
      rumle_v-k := predsort.
      runde_v-k := predsort.
      rundjule_v-k := predsort.
      rundstele_v-k := predsort.
      rundstjele_v-k := predsort.
      rundvaske_v-k := predsort.
      rune_v-k := predsort.
      runge_v-k := predsort.
      runke_v-k := predsort.
      ruse_v-k := predsort.
      rushe_v-k := predsort.
      ruske_v-k := predsort.
      rusle_v-k := predsort.
      russifisere_v-k := predsort.
      rustbehandle_v-k := predsort.
      ruste_v-k := predsort.
      rustifisere_v-k := predsort.
      rustne_v-k := predsort.
      rute-v_v-k := predsort.
      rutle_v-k := predsort.
      rutsje_v-k := predsort.
      rutte_v-k := predsort.
      ruve_v-k := predsort.
      rydde_v-k := predsort.
      rygge_v-k := predsort.
      ryke_v-k := predsort.
      rykke_v-k := predsort.
      ry_v-k := predsort.
      ryktes_v-k := predsort.
      rynke_v-k := predsort.
      ryste_v-k := predsort.
      ryte_v-k := predsort.
      sabbe_v-k := predsort.
      sable_v-k := predsort.
      sabotere_v-k := predsort.
      sadle_v-k := predsort.
      sælde_v-k := predsort.
      særmerke_v-k := predsort.
      særprege_v-k := predsort.
      sæte_v-k := predsort.
      sætre_v-k := predsort.
      safe_v-k := predsort.
      safte_v-k := predsort.
      sage_v-k := predsort.
      sakfelle_v-k := predsort.
      sakke_v-k := predsort.
      sakne_v-k := predsort.
      så-v_v-k := predsort.
      sakse_v-k := predsort.
      saksøke_v-k := predsort.
      saktne_v-k := predsort.
      sålde_v-k := predsort.
      saldere_v-k := predsort.
      sale_v-k := predsort.
      såle_v-k := predsort.
      salte_v-k := predsort.
      saluttere_v-k := predsort.
      salve_v-k := predsort.
      samarbeide_v-k := predsort.
      sameksistere_v-k := predsort.
      samkjøre_v-k := predsort.
      samle_v-k := predsort.
      sammenfatte_v-k := predsort.
      sammenholde_v-k := predsort.
      sammenkalle_v-k := predsort.
      sammenligne_v-k := predsort.
      sammenlikne_v-k := predsort.
      sammenrulle_v-k := predsort.
      sammenslå_v-k := predsort.
      sammenstille_v-k := predsort.
      sammensverge_v-k := predsort.
      sammensverje_v-k := predsort.
      samordne_v-k := predsort.
      sample_v-k := predsort.
      samrå_v-k := predsort.
      samse_v-k := predsort.
      samstave_v-k := predsort.
      samstemme_v-k := predsort.
      samsvare_v-k := predsort.
      samtale_v-k := predsort.
      samvirke_v-k := predsort.
      sanere_v-k := predsort.
      sanforisere_v-k := predsort.
      sangre_v-k := predsort.
      sanke_v-k := predsort.
      sanksjonere_v-k := predsort.
      sanne_v-k := predsort.
      sannsynliggjøre_v-k := predsort.
      sanse_v-k := predsort.
      såpe_v-k := predsort.
      såre_v-k := predsort.
      såte_v-k := predsort.
      satinere_v-k := predsort.
      satirisere_v-k := predsort.
      satisfisere_v-k := predsort.
      satse_v-k := predsort.
      saumfare_v-k := predsort.
      sause_v-k := predsort.
      savle_v-k := predsort.
      savne_v-k := predsort.
      score_v-k := predsort.
      scripte_v-k := predsort.
      sedimentere_v-k := predsort.
      seede_v-k := predsort.
      sees_v-k := predsort.
      segle_v-k := predsort.
      segmentere_v-k := predsort.
      segne_v-k := predsort.
      segregere_v-k := predsort.
      seide_v-k := predsort.
      seigpine_v-k := predsort.
      seile_v-k := predsort.
      seinke_v-k := predsort.
      seipe_v-k := predsort.
      seire_v-k := predsort.
      seise_v-k := predsort.
      se_v-k := predsort.
      seksdoble_v-k := predsort.
      seksjonere_v-k := predsort.
      sekularisere_v-k := predsort.
      sekundere_v-k := predsort.
      sele_v-k := predsort.
      selektere_v-k := predsort.
      selge_v-k := predsort.
      selvdrive_v-k := predsort.
      selvdyrke_v-k := predsort.
      selvseile_v-k := predsort.
      selvstendiggjøre_v-k := predsort.
      semaforere_v-k := predsort.
      sementere_v-k := predsort.
      seminere_v-k := predsort.
      semsgarve_v-k := predsort.
      semske_v-k := predsort.
      semskgarve_v-k := predsort.
      sende_v-k := predsort.
      senke_v-k := predsort.
      sensurere_v-k := predsort.
      sentimentalisere_v-k := predsort.
      sentralisere_v-k := predsort.
      sentre_v-k := predsort.
      sentrere_v-k := predsort.
      sentrifugere_v-k := predsort.
      separere_v-k := predsort.
      seriekoble_v-k := predsort.
      seriekople_v-k := predsort.
      serratere_v-k := predsort.
      sertifisere_v-k := predsort.
      serve_v-k := predsort.
      servere_v-k := predsort.
      ses_v-k := predsort.
      sesongdebutere_v-k := predsort.
      sesse_v-k := predsort.
      setre_v-k := predsort.
      sette_v-k := predsort.
      settle_v-k := predsort.
      sexdebutere_v-k := predsort.
      shake_v-k := predsort.
      shoppe_v-k := predsort.
      showe_v-k := predsort.
      shunte_v-k := predsort.
      sideordne_v-k := predsort.
      sidestille_v-k := predsort.
      sidrikke_v-k := predsort.
      sifrere_v-k := predsort.
      sige_v-k := predsort.
      signalere_v-k := predsort.
      signalisere_v-k := predsort.
      signe_v-k := predsort.
      signere_v-k := predsort.
      sike_v-k := predsort.
      sikkerhetsklarere_v-k := predsort.
      sikksakke_v-k := predsort.
      sikle_v-k := predsort.
      si_v-k := predsort.
      sikre_v-k := predsort.
      sikte_v-k := predsort.
      silblø_v-k := predsort.
      sildre_v-k := predsort.
      sile_v-k := predsort.
      silregne_v-k := predsort.
      silrenne_v-k := predsort.
      simplifisere_v-k := predsort.
      simulere_v-k := predsort.
      sindre_v-k := predsort.
      single_v-k := predsort.
      sinke_v-k := predsort.
      sintre_v-k := predsort.
      sipe_v-k := predsort.
      sippe_v-k := predsort.
      siregne_v-k := predsort.
      sirkle_v-k := predsort.
      sirkulere_v-k := predsort.
      siselere_v-k := predsort.
      sisle_v-k := predsort.
      sitere_v-k := predsort.
      sitre_v-k := predsort.
      sitte_v-k := predsort.
      sive_v-k := predsort.
      sivilisere_v-k := predsort.
      sjabbe_v-k := predsort.
      sjalte_v-k := predsort.
      sjamponere_v-k := predsort.
      sjanghaie_v-k := predsort.
      sjangle_v-k := predsort.
      sjanse_v-k := predsort.
      sjarmantisere_v-k := predsort.
      sjarmere_v-k := predsort.
      sjaske_v-k := predsort.
      sjattere_v-k := predsort.
      sjaue_v-k := predsort.
      sjefe_v-k := predsort.
      sjeide_v-k := predsort.
      sjekke_v-k := predsort.
      sjenere_v-k := predsort.
      sjevre_v-k := predsort.
      sjikanere_v-k := predsort.
      sjingle_v-k := predsort.
      sjofle_v-k := predsort.
      sjoge_v-k := predsort.
      sjogse_v-k := predsort.
      sjokkåpne_v-k := predsort.
      sjokke_v-k := predsort.
      sjokkere_v-k := predsort.
      sjølstendiggjøre_v-k := predsort.
      sjonglere_v-k := predsort.
      sjøsette_v-k := predsort.
      sjudoble_v-k := predsort.
      sjukemelde_v-k := predsort.
      sjukmelde_v-k := predsort.
      sjukne_v-k := predsort.
      sjuske_v-k := predsort.
      skade_v-k := predsort.
      skadeskyte_v-k := predsort.
      skaffe_v-k := predsort.
      skafte_v-k := predsort.
      skake_v-k := predsort.
      skakke_v-k := predsort.
      ska_v-k := predsort.
      skålde_v-k := predsort.
      skåle_v-k := predsort.
      skalere_v-k := predsort.
      skalke_v-k := predsort.
      skalle_v-k := predsort.
      skalpere_v-k := predsort.
      skalte_v-k := predsort.
      skambite_v-k := predsort.
      skamfere_v-k := predsort.
      skamfryse_v-k := predsort.
      skamhogge_v-k := predsort.
      skamhugge_v-k := predsort.
      skamkjøre_v-k := predsort.
      skamklippe_v-k := predsort.
      skamklyppe_v-k := predsort.
      skamme_v-k := predsort.
      skamrose_v-k := predsort.
      skamslå_v-k := predsort.
      skandalisere_v-k := predsort.
      skandere_v-k := predsort.
      skåne_v-k := predsort.
      skanne_v-k := predsort.
      skante_v-k := predsort.
      skape_v-k := predsort.
      skare_v-k := predsort.
      skåre_v-k := predsort.
      skarifisere_v-k := predsort.
      skårlegge_v-k := predsort.
      skarre_v-k := predsort.
      skarve_v-k := predsort.
      skate_v-k := predsort.
      skåte_v-k := predsort.
      skatte_v-k := predsort.
      skattlegge_v-k := predsort.
      skave_v-k := predsort.
      skavle_v-k := predsort.
      skeie_v-k := predsort.
      skeine_v-k := predsort.
      skeise_v-k := predsort.
      skeive_v-k := predsort.
      skeivle_v-k := predsort.
      skifte_v-k := predsort.
      skikke_v-k := predsort.
      skildre_v-k := predsort.
      skille_v-k := predsort.
      skilte_v-k := predsort.
      skimle_v-k := predsort.
      skimre_v-k := predsort.
      skimte_v-k := predsort.
      skingre_v-k := predsort.
      skinne_v-k := predsort.
      skipe_v-k := predsort.
      skiple_v-k := predsort.
      skissere_v-k := predsort.
      skite_v-k := predsort.
      skitne_v-k := predsort.
      skive_v-k := predsort.
      skjære_v-k := predsort.
      skjærne_v-k := predsort.
      skjefte_v-k := predsort.
      skje_v-k := predsort.
      skjele_v-k := predsort.
      skjelettere_v-k := predsort.
      skjelle_v-k := predsort.
      skjelme_v-k := predsort.
      skjelne_v-k := predsort.
      skjelve_v-k := predsort.
      skjematisere_v-k := predsort.
      skjemme_v-k := predsort.
      skjemmes_v-k := predsort.
      skjemte_v-k := predsort.
      skjende_v-k := predsort.
      skjene_v-k := predsort.
      skjenke_v-k := predsort.
      skjenne_v-k := predsort.
      skjerme_v-k := predsort.
      skjerpe_v-k := predsort.
      skjolde_v-k := predsort.
      skjønne_v-k := predsort.
      skjønnmale_v-k := predsort.
      skjønnsligne_v-k := predsort.
      skjønnslikne_v-k := predsort.
      skjørne_v-k := predsort.
      skjøte_v-k := predsort.
      skjøtte_v-k := predsort.
      skjule_v-k := predsort.
      skli_v-k := predsort.
      skofte_v-k := predsort.
      skoggerle_v-k := predsort.
      skogre_v-k := predsort.
      sko_v-k := predsort.
      skole_v-k := predsort.
      skolere_v-k := predsort.
      skolme_v-k := predsort.
      skore_v-k := predsort.
      skorpe_v-k := predsort.
      skorte_v-k := predsort.
      skotte_v-k := predsort.
      skove_v-k := predsort.
      skovle_v-k := predsort.
      skøye_v-k := predsort.
      skøyte_v-k := predsort.
      skrå_v-k := predsort.
      skråle_v-k := predsort.
      skralle_v-k := predsort.
      skramle_v-k := predsort.
      skråne_v-k := predsort.
      skrangle_v-k := predsort.
      skrante_v-k := predsort.
      skrape_v-k := predsort.
      skråsette_v-k := predsort.
      skråstille_v-k := predsort.
      skratte_v-k := predsort.
      skrattle_v-k := predsort.
      skravere_v-k := predsort.
      skravle_v-k := predsort.
      skredderere_v-k := predsort.
      skreddersy_v-k := predsort.
      skre_v-k := predsort.
      skrelle_v-k := predsort.
      skremme_v-k := predsort.
      skrense_v-k := predsort.
      skreve_v-k := predsort.
      skrible_v-k := predsort.
      skride_v-k := predsort.
      skrifte_v-k := predsort.
      skriftfeste_v-k := predsort.
      skrike_v-k := predsort.
      skrinlegge_v-k := predsort.
      skritte_v-k := predsort.
      skrive_v-k := predsort.
      skrøfte_v-k := predsort.
      skrø_v-k := predsort.
      skrømte_v-k := predsort.
      skrøne_v-k := predsort.
      skrote_v-k := predsort.
      skrubbe_v-k := predsort.
      skrue_v-k := predsort.
      skrukke_v-k := predsort.
      skru_v-k := predsort.
      skrumpe_v-k := predsort.
      skruve_v-k := predsort.
      skryte_v-k := predsort.
      skubbe_v-k := predsort.
      skue_v-k := predsort.
      skuffe_v-k := predsort.
      skufle_v-k := predsort.
      skule_v-k := predsort.
      skulke_v-k := predsort.
      skulle_v-k := predsort.
      skulptere_v-k := predsort.
      skulse_v-k := predsort.
      skumkjøre_v-k := predsort.
      skumle_v-k := predsort.
      skumlese_v-k := predsort.
      skumme_v-k := predsort.
      skumpe_v-k := predsort.
      skumpiske_v-k := predsort.
      skumple_v-k := predsort.
      skumre_v-k := predsort.
      skumtygge_v-k := predsort.
      skure_v-k := predsort.
      skurre_v-k := predsort.
      skusle_v-k := predsort.
      skutle_v-k := predsort.
      skutte_v-k := predsort.
      skuve_v-k := predsort.
      skvadronere_v-k := predsort.
      skvære_v-k := predsort.
      skvaldre_v-k := predsort.
      skvale_v-k := predsort.
      skvalpe_v-k := predsort.
      skvatre_v-k := predsort.
      skvette_v-k := predsort.
      skvise_v-k := predsort.
      skvulpe_v-k := predsort.
      skye_v-k := predsort.
      skyfle_v-k := predsort.
      skygge_v-k := predsort.
      skyggelegge_v-k := predsort.
      sky_v-k := predsort.
      skylde_v-k := predsort.
      skyldes_v-k := predsort.
      skyldsette_v-k := predsort.
      skylle_v-k := predsort.
      skynde_v-k := predsort.
      skysse_v-k := predsort.
      skyte_v-k := predsort.
      skyve_v-k := predsort.
      slabbe_v-k := predsort.
      sladde_v-k := predsort.
      sladre_v-k := predsort.
      slafse_v-k := predsort.
      slage_v-k := predsort.
      slagge_v-k := predsort.
      slakke_v-k := predsort.
      slakne_v-k := predsort.
      slå_v-k := predsort.
      slakte_v-k := predsort.
      slamme_v-k := predsort.
      slampe_v-k := predsort.
      slamre_v-k := predsort.
      slamse_v-k := predsort.
      slange_v-k := predsort.
      slanke_v-k := predsort.
      slapne_v-k := predsort.
      slappe_v-k := predsort.
      slarke_v-k := predsort.
      slarve_v-k := predsort.
      slaske_v-k := predsort.
      slåss_v-k := predsort.
      slavebinde_v-k := predsort.
      slave_v-k := predsort.
      slegge_v-k := predsort.
      sleike_v-k := predsort.
      sleiske_v-k := predsort.
      sleive_v-k := predsort.
      slekte_v-k := predsort.
      slemme_v-k := predsort.
      slenge_v-k := predsort.
      slentre_v-k := predsort.
      slepe_v-k := predsort.
      sleppe_v-k := predsort.
      sleske_v-k := predsort.
      sletne_v-k := predsort.
      slette_v-k := predsort.
      sleve_v-k := predsort.
      slikke_v-k := predsort.
      slime_v-k := predsort.
      slindbarke_v-k := predsort.
      slinde_v-k := predsort.
      slindre_v-k := predsort.
      slingre_v-k := predsort.
      slipe_v-k := predsort.
      slippe_v-k := predsort.
      sliske_v-k := predsort.
      slisse_v-k := predsort.
      slite_v-k := predsort.
      slitne_v-k := predsort.
      slodde_v-k := predsort.
      slokke_v-k := predsort.
      sløkke_v-k := predsort.
      slokne_v-k := predsort.
      sløre_v-k := predsort.
      sløse_v-k := predsort.
      sløve_v-k := predsort.
      sløvne_v-k := predsort.
      sløyde_v-k := predsort.
      sløye_v-k := predsort.
      sløyfe_v-k := predsort.
      slubbe_v-k := predsort.
      slubre_v-k := predsort.
      sludde_v-k := predsort.
      sludre_v-k := predsort.
      sluffe_v-k := predsort.
      slufse_v-k := predsort.
      sluke_v-k := predsort.
      slukke_v-k := predsort.
      slukne_v-k := predsort.
      slumpe_v-k := predsort.
      slumre_v-k := predsort.
      slumse_v-k := predsort.
      sluntre_v-k := predsort.
      slure_v-k := predsort.
      slurke_v-k := predsort.
      slurpe_v-k := predsort.
      slurve_v-k := predsort.
      sluse_v-k := predsort.
      sluske_v-k := predsort.
      slutte_v-k := predsort.
      sluttføre_v-k := predsort.
      sluttforhandle_v-k := predsort.
      slynge_v-k := predsort.
      smadre_v-k := predsort.
      smake_v-k := predsort.
      smaksette_v-k := predsort.
      smalne_v-k := predsort.
      småløpe_v-k := predsort.
      småprate_v-k := predsort.
      smashe_v-k := predsort.
      smaske_v-k := predsort.
      smatre_v-k := predsort.
      smatte_v-k := predsort.
      smede_v-k := predsort.
      smeike_v-k := predsort.
      smeise_v-k := predsort.
      smekke_v-k := predsort.
      smekte_v-k := predsort.
      smelle_v-k := predsort.
      smelte_v-k := predsort.
      smergle_v-k := predsort.
      smerte_v-k := predsort.
      smette_v-k := predsort.
      smigre_v-k := predsort.
      smikke_v-k := predsort.
      smi_v-k := predsort.
      smile_v-k := predsort.
      sminke_v-k := predsort.
      smiske_v-k := predsort.
      smitte_v-k := predsort.
      smøre_v-k := predsort.
      smugle_v-k := predsort.
      smuldre_v-k := predsort.
      smule_v-k := predsort.
      smulne_v-k := predsort.
      smusse_v-k := predsort.
      smyge_v-k := predsort.
      smykke_v-k := predsort.
      snadre_v-k := predsort.
      snafse_v-k := predsort.
      snakke_v-k := predsort.
      snåle_v-k := predsort.
      snappe_v-k := predsort.
      snapse_v-k := predsort.
      snaske_v-k := predsort.
      snaublåse_v-k := predsort.
      snaue_v-k := predsort.
      snaugnage_v-k := predsort.
      snauhogge_v-k := predsort.
      snauhugge_v-k := predsort.
      snauklippe_v-k := predsort.
      snauklyppe_v-k := predsort.
      snauslite_v-k := predsort.
      snåve_v-k := predsort.
      snavle_v-k := predsort.
      snegle_v-k := predsort.
      sneie_v-k := predsort.
      sneise_v-k := predsort.
      sneke_v-k := predsort.
      snekkerere_v-k := predsort.
      sne_v-k := predsort.
      snekre_v-k := predsort.
      snelle_v-k := predsort.
      snerke_v-k := predsort.
      snerpe_v-k := predsort.
      snerre_v-k := predsort.
      snerte_v-k := predsort.
      snevre_v-k := predsort.
      sniffe_v-k := predsort.
      snike_v-k := predsort.
      snikmyrde_v-k := predsort.
      snirkle_v-k := predsort.
      snitte_v-k := predsort.
      snobbe_v-k := predsort.
      snøfle_v-k := predsort.
      snøfte_v-k := predsort.
      snøgge_v-k := predsort.
      snoke_v-k := predsort.
      sno_v-k := predsort.
      snø_v-k := predsort.
      snope_v-k := predsort.
      snørekjøre_v-k := predsort.
      snøre_v-k := predsort.
      snorke_v-k := predsort.
      snorksove_v-k := predsort.
      snøse_v-k := predsort.
      snøvle_v-k := predsort.
      snubbe_v-k := predsort.
      snuble_v-k := predsort.
      snufse_v-k := predsort.
      snu_v-k := predsort.
      snurpe_v-k := predsort.
      snurre_v-k := predsort.
      snuse_v-k := predsort.
      snuske_v-k := predsort.
      snusmale_v-k := predsort.
      snylte_v-k := predsort.
      snyte_v-k := predsort.
      sogne_v-k := predsort.
      søke_v-k := predsort.
      sokke_v-k := predsort.
      søkke_v-k := predsort.
      sokne_v-k := predsort.
      sole_v-k := predsort.
      søle_v-k := predsort.
      solidarisere_v-k := predsort.
      sollisitere_v-k := predsort.
      solmisere_v-k := predsort.
      somle_v-k := predsort.
      sømme_v-k := predsort.
      somres_v-k := predsort.
      sondere_v-k := predsort.
      sønderrive_v-k := predsort.
      sondre_v-k := predsort.
      sone_v-k := predsort.
      sope_v-k := predsort.
      søple_v-k := predsort.
      soppe_v-k := predsort.
      sordinere_v-k := predsort.
      sørge_v-k := predsort.
      sortere_v-k := predsort.
      sose_v-k := predsort.
      sosialisere_v-k := predsort.
      sote_v-k := predsort.
      søte_v-k := predsort.
      sove_v-k := predsort.
      sovne_v-k := predsort.
      spakne_v-k := predsort.
      spa_v-k := predsort.
      spå_v-k := predsort.
      spalte_v-k := predsort.
      spandere_v-k := predsort.
      spane_v-k := predsort.
      spanke_v-k := predsort.
      spankulere_v-k := predsort.
      spanske_v-k := predsort.
      spante_v-k := predsort.
      spare_v-k := predsort.
      sparke_v-k := predsort.
      sparkle_v-k := predsort.
      sparre_v-k := predsort.
      spase_v-k := predsort.
      spasere_v-k := predsort.
      speede_v-k := predsort.
      speide_v-k := predsort.
      speile_v-k := predsort.
      speilvende_v-k := predsort.
      speke_v-k := predsort.
      spekke_v-k := predsort.
      spekkmåle_v-k := predsort.
      spekne_v-k := predsort.
      spe_v-k := predsort.
      spekse_v-k := predsort.
      spekulere_v-k := predsort.
      spenne_v-k := predsort.
      sperre_v-k := predsort.
      spesialbygge_v-k := predsort.
      spesialisere_v-k := predsort.
      spesifisere_v-k := predsort.
      spidde_v-k := predsort.
      spikke_v-k := predsort.
      spikre_v-k := predsort.
      spildre_v-k := predsort.
      spile_v-k := predsort.
      spille_v-k := predsort.
      spinke_v-k := predsort.
      spinne_v-k := predsort.
      spionere_v-k := predsort.
      spiralisere_v-k := predsort.
      spire_v-k := predsort.
      spise_v-k := predsort.
      spisse_v-k := predsort.
      spissformulere_v-k := predsort.
      spjære_v-k := predsort.
      spjåke_v-k := predsort.
      spjelke_v-k := predsort.
      spleise_v-k := predsort.
      splintre_v-k := predsort.
      splitte_v-k := predsort.
      spøke_v-k := predsort.
      spole_v-k := predsort.
      spolere_v-k := predsort.
      sponse_v-k := predsort.
      spore_v-k := predsort.
      spørre_v-k := predsort.
      spørres_v-k := predsort.
      spørs_v-k := predsort.
      sporte_v-k := predsort.
      spotte_v-k := predsort.
      sprade_v-k := predsort.
      sprake_v-k := predsort.
      spraye_v-k := predsort.
      sprede_v-k := predsort.
      spreie_v-k := predsort.
      sprekke_v-k := predsort.
      spre_v-k := predsort.
      sprelle_v-k := predsort.
      sprenge_v-k := predsort.
      sprengkjøre_v-k := predsort.
      sprenglese_v-k := predsort.
      sprette_v-k := predsort.
      sprike_v-k := predsort.
      spri_v-k := predsort.
      springe_v-k := predsort.
      sprinte_v-k := predsort.
      sprite_v-k := predsort.
      sprøsteike_v-k := predsort.
      sprøsteke_v-k := predsort.
      sprøyte_v-k := predsort.
      sprøytelakkere_v-k := predsort.
      sprudle_v-k := predsort.
      sprute_v-k := predsort.
      sprutregne_v-k := predsort.
      spunse_v-k := predsort.
      spurtbeseire_v-k := predsort.
      spurte_v-k := predsort.
      spy_v-k := predsort.
      spyle_v-k := predsort.
      spytte_v-k := predsort.
      stabbe_v-k := predsort.
      stabilisere_v-k := predsort.
      stable_v-k := predsort.
      stadfeste_v-k := predsort.
      staffere_v-k := predsort.
      stage_v-k := predsort.
      stagge_v-k := predsort.
      stagnere_v-k := predsort.
      stagvende_v-k := predsort.
      stake_v-k := predsort.
      ståke_v-k := predsort.
      stakke_v-k := predsort.
      stå_v-k := predsort.
      stalle_v-k := predsort.
      stålsette_v-k := predsort.
      stambokføre_v-k := predsort.
      stamme_v-k := predsort.
      stampe_v-k := predsort.
      standardisere_v-k := predsort.
      stane_v-k := predsort.
      stange_v-k := predsort.
      stanke_v-k := predsort.
      stanse_v-k := predsort.
      staple_v-k := predsort.
      stappe_v-k := predsort.
      starte_v-k := predsort.
      stase_v-k := predsort.
      stasjonere_v-k := predsort.
      statuere_v-k := predsort.
      staure_v-k := predsort.
      stave_v-k := predsort.
      stavre_v-k := predsort.
      stede_v-k := predsort.
      stedfeste_v-k := predsort.
      stege_v-k := predsort.
      steike_v-k := predsort.
      steile_v-k := predsort.
      steine_v-k := predsort.
      steinsette_v-k := predsort.
      steke_v-k := predsort.
      stekke_v-k := predsort.
      ste_v-k := predsort.
      stele_v-k := predsort.
      stelle_v-k := predsort.
      stemme_v-k := predsort.
      stemne_v-k := predsort.
      stemple_v-k := predsort.
      stenge_v-k := predsort.
      stenke_v-k := predsort.
      stenografere_v-k := predsort.
      stensilere_v-k := predsort.
      steppe_v-k := predsort.
      stereotypere_v-k := predsort.
      sterilisere_v-k := predsort.
      sterkne_v-k := predsort.
      sterte_v-k := predsort.
      stette_v-k := predsort.
      stevje_v-k := predsort.
      stevne_v-k := predsort.
      stifte_v-k := predsort.
      stige_v-k := predsort.
      stigmatisere_v-k := predsort.
      stikke_v-k := predsort.
      stikle_v-k := predsort.
      stile_v-k := predsort.
      stilisere_v-k := predsort.
      stille_v-k := predsort.
      stilne_v-k := predsort.
      stiltre_v-k := predsort.
      stime_v-k := predsort.
      stimle_v-k := predsort.
      stimrense_v-k := predsort.
      stimulere_v-k := predsort.
      stinge_v-k := predsort.
      stinke_v-k := predsort.
      stiple_v-k := predsort.
      stipulere_v-k := predsort.
      stirre_v-k := predsort.
      stive_v-k := predsort.
      stivfryse_v-k := predsort.
      stivne_v-k := predsort.
      stivpiske_v-k := predsort.
      stjele_v-k := predsort.
      støe_v-k := predsort.
      stoffe_v-k := predsort.
      stokke_v-k := predsort.
      støkke_v-k := predsort.
      stø_v-k := predsort.
      stole_v-k := predsort.
      støle_v-k := predsort.
      stølne_v-k := predsort.
      stolpe_v-k := predsort.
      stolpre_v-k := predsort.
      stønne_v-k := predsort.
      støpe_v-k := predsort.
      stoppe_v-k := predsort.
      størkne_v-k := predsort.
      storle_v-k := predsort.
      storme_v-k := predsort.
      stormgjø_v-k := predsort.
      storskrike_v-k := predsort.
      storspille_v-k := predsort.
      stortrives_v-k := predsort.
      støte_v-k := predsort.
      stotre_v-k := predsort.
      støtte_v-k := predsort.
      støve_v-k := predsort.
      støvsuge_v-k := predsort.
      støye_v-k := predsort.
      støype_v-k := predsort.
      støyte_v-k := predsort.
      straffedømme_v-k := predsort.
      straffeforfølge_v-k := predsort.
      straffe_v-k := predsort.
      stra_v-k := predsort.
      stråle_v-k := predsort.
      stramme_v-k := predsort.
      strande_v-k := predsort.
      strangulere_v-k := predsort.
      strante_v-k := predsort.
      stratifisere_v-k := predsort.
      streame_v-k := predsort.
      strebe_v-k := predsort.
      streife_v-k := predsort.
      streike_v-k := predsort.
      streite_v-k := predsort.
      streke_v-k := predsort.
      strekke_v-k := predsort.
      strende_v-k := predsort.
      strene_v-k := predsort.
      strenge_v-k := predsort.
      strengle_v-k := predsort.
      stresse_v-k := predsort.
      streve_v-k := predsort.
      stride_v-k := predsort.
      stridgråte_v-k := predsort.
      stridregne_v-k := predsort.
      strigle_v-k := predsort.
      strigråte_v-k := predsort.
      strikke_v-k := predsort.
      stri_v-k := predsort.
      strimle_v-k := predsort.
      stripe_v-k := predsort.
      strippe_v-k := predsort.
      striregne_v-k := predsort.
      stritte_v-k := predsort.
      strø_v-k := predsort.
      strømme_v-k := predsort.
      stroppe_v-k := predsort.
      strukturere_v-k := predsort.
      strupe_v-k := predsort.
      strutte_v-k := predsort.
      stryke_v-k := predsort.
      strype_v-k := predsort.
      studere_v-k := predsort.
      stue_v-k := predsort.
      stuke_v-k := predsort.
      stulle_v-k := predsort.
      stumpe_v-k := predsort.
      stunde_v-k := predsort.
      stupe_v-k := predsort.
      sture_v-k := predsort.
      sturte_v-k := predsort.
      stusse_v-k := predsort.
      stutne_v-k := predsort.
      stutte_v-k := predsort.
      stuve_v-k := predsort.
      stygges_v-k := predsort.
      stykke_v-k := predsort.
      stylte_v-k := predsort.
      styltre_v-k := predsort.
      styre_v-k := predsort.
      styrke_v-k := predsort.
      styrtblø_v-k := predsort.
      styrte_v-k := predsort.
      styrtregne_v-k := predsort.
      subbe_v-k := predsort.
      sublimere_v-k := predsort.
      subordinere_v-k := predsort.
      subsidiere_v-k := predsort.
      subskribere_v-k := predsort.
      substantivere_v-k := predsort.
      substituere_v-k := predsort.
      subsumere_v-k := predsort.
      subtrahere_v-k := predsort.
      subvenere_v-k := predsort.
      suffigere_v-k := predsort.
      sufflere_v-k := predsort.
      suge_v-k := predsort.
      suggerere_v-k := predsort.
      sukke_v-k := predsort.
      sukle_v-k := predsort.
      sukre_v-k := predsort.
      suksedere_v-k := predsort.
      sulke_v-k := predsort.
      sulle_v-k := predsort.
      sultefôre_v-k := predsort.
      sulte_v-k := predsort.
      summe_v-k := predsort.
      summere_v-k := predsort.
      supe_v-k := predsort.
      supere_v-k := predsort.
      supinere_v-k := predsort.
      supplere_v-k := predsort.
      supplikkere_v-k := predsort.
      supponere_v-k := predsort.
      supprimere_v-k := predsort.
      surfe_v-k := predsort.
      surkle_v-k := predsort.
      surle_v-k := predsort.
      surmule_v-k := predsort.
      surne_v-k := predsort.
      surre_v-k := predsort.
      suse_v-k := predsort.
      susle_v-k := predsort.
      suspendere_v-k := predsort.
      susse_v-k := predsort.
      sutenere_v-k := predsort.
      sutre_v-k := predsort.
      sutte_v-k := predsort.
      svabre_v-k := predsort.
      svæve_v-k := predsort.
      svage_v-k := predsort.
      svaie_v-k := predsort.
      svale_v-k := predsort.
      svalne_v-k := predsort.
      svampe_v-k := predsort.
      svanse_v-k := predsort.
      svare_v-k := predsort.
      svarteliste_v-k := predsort.
      svartmale_v-k := predsort.
      svartne_v-k := predsort.
      svarve_v-k := predsort.
      sveipe_v-k := predsort.
      sveise_v-k := predsort.
      sveive_v-k := predsort.
      svekke_v-k := predsort.
      svelge_v-k := predsort.
      svelgje_v-k := predsort.
      svelle_v-k := predsort.
      svelte_v-k := predsort.
      sverge_v-k := predsort.
      sverje_v-k := predsort.
      sverme_v-k := predsort.
      sverte_v-k := predsort.
      svette_v-k := predsort.
      sveve_v-k := predsort.
      svike_v-k := predsort.
      svi_v-k := predsort.
      svikte_v-k := predsort.
      svime_v-k := predsort.
      svimerke_v-k := predsort.
      svimeslå_v-k := predsort.
      svimle_v-k := predsort.
      svimre_v-k := predsort.
      svindle_v-k := predsort.
      svinebinde_v-k := predsort.
      svine_v-k := predsort.
      svinge_v-k := predsort.
      svinne_v-k := predsort.
      svinse_v-k := predsort.
      svinte_v-k := predsort.
      svippe_v-k := predsort.
      svire_v-k := predsort.
      svirre_v-k := predsort.
      svive_v-k := predsort.
      svivørde_v-k := predsort.
      svivøre_v-k := predsort.
      svolke_v-k := predsort.
      svømme_v-k := predsort.
      svøpe_v-k := predsort.
      svovle_v-k := predsort.
      svulme_v-k := predsort.
      svulne_v-k := predsort.
      svuppe_v-k := predsort.
      swinge_v-k := predsort.
      syde_v-k := predsort.
      sykdomsplage_v-k := predsort.
      sykemelde_v-k := predsort.
      sykle_v-k := predsort.
      sykmelde_v-k := predsort.
      sykne_v-k := predsort.
      sy_v-k := predsort.
      sylte_v-k := predsort.
      symbolisere_v-k := predsort.
      sympatisere_v-k := predsort.
      sympatistreike_v-k := predsort.
      synde_v-k := predsort.
      syne_v-k := predsort.
      synes_v-k := predsort.
      synfare_v-k := predsort.
      synge_v-k := predsort.
      synke_v-k := predsort.
      synkopere_v-k := predsort.
      synkronisere_v-k := predsort.
      synkverve_v-k := predsort.
      synliggjøre_v-k := predsort.
      synse_v-k := predsort.
      syntetisere_v-k := predsort.
      syre_v-k := predsort.
      syrne_v-k := predsort.
      sysle_v-k := predsort.
      sysselsette_v-k := predsort.
      systematisere_v-k := predsort.
      syte_v-k := predsort.
      tabbe_v-k := predsort.
      tabellarisere_v-k := predsort.
      tabubelegge_v-k := predsort.
      tabulere_v-k := predsort.
      tære_v-k := predsort.
      tafse_v-k := predsort.
      tagge_v-k := predsort.
      tagne_v-k := predsort.
      tåkelegge_v-k := predsort.
      takke_v-k := predsort.
      takle_v-k := predsort.
      ta_v-k := predsort.
      takse_v-k := predsort.
      taksere_v-k := predsort.
      taktere_v-k := predsort.
      tale_v-k := predsort.
      tåle_v-k := predsort.
      talle_v-k := predsort.
      tallfeste_v-k := predsort.
      tampe_v-k := predsort.
      tamse_v-k := predsort.
      tane_v-k := predsort.
      tangere_v-k := predsort.
      tanke_v-k := predsort.
      tape_v-k := predsort.
      tapetsere_v-k := predsort.
      tappe_v-k := predsort.
      tariffeste_v-k := predsort.
      tariff-feste_v-k := predsort.
      tårne_v-k := predsort.
      tasle_v-k := predsort.
      tasse_v-k := predsort.
      taste_v-k := predsort.
      tatovere_v-k := predsort.
      taue_v-k := predsort.
      taushetsbelegge_v-k := predsort.
      tave_v-k := predsort.
      taxe_v-k := predsort.
      tefte_v-k := predsort.
      tegne_v-k := predsort.
      teipe_v-k := predsort.
      teite_v-k := predsort.
      tekke_v-k := predsort.
      teknifisere_v-k := predsort.
      te_v-k := predsort.
      tekste_v-k := predsort.
      teksturere_v-k := predsort.
      telefonere_v-k := predsort.
      telegrafere_v-k := predsort.
      tele_v-k := predsort.
      teleksere_v-k := predsort.
      telemetrere_v-k := predsort.
      telgje_v-k := predsort.
      telle_v-k := predsort.
      telne_v-k := predsort.
      telte_v-k := predsort.
      tematisere_v-k := predsort.
      temme_v-k := predsort.
      temperere_v-k := predsort.
      tendere_v-k := predsort.
      tenke_v-k := predsort.
      tenne_v-k := predsort.
      teoretisere_v-k := predsort.
      teppe_v-k := predsort.
      teppelegge_v-k := predsort.
      terge_v-k := predsort.
      terminfeste_v-k := predsort.
      terpe_v-k := predsort.
      terrassere_v-k := predsort.
      terrorarrestere_v-k := predsort.
      terrorisere_v-k := predsort.
      terrorsikte_v-k := predsort.
      terske_v-k := predsort.
      testamentere_v-k := predsort.
      teste_v-k := predsort.
      tetne_v-k := predsort.
      tette_v-k := predsort.
      teve_v-k := predsort.
      tevle_v-k := predsort.
      tidfeste_v-k := predsort.
      tidoble_v-k := predsort.
      tidsbestemme_v-k := predsort.
      tidsinnstille_v-k := predsort.
      tie_v-k := predsort.
      tigge_v-k := predsort.
      tikke_v-k := predsort.
      tilbakebetale_v-k := predsort.
      tilbakeføre_v-k := predsort.
      tilbakekalle_v-k := predsort.
      tilbakelegge_v-k := predsort.
      tilbakevise_v-k := predsort.
      tilbe_v-k := predsort.
      tilberede_v-k := predsort.
      tilbringe_v-k := predsort.
      tilby_v-k := predsort.
      tildekke_v-k := predsort.
      tildele_v-k := predsort.
      tildømme_v-k := predsort.
      tilegne_v-k := predsort.
      tilendebringe_v-k := predsort.
      tilfalle_v-k := predsort.
      tilflyte_v-k := predsort.
      tilføre_v-k := predsort.
      tilføye_v-k := predsort.
      tilfredsstille_v-k := predsort.
      tilfryse_v-k := predsort.
      tilfyke_v-k := predsort.
      tilgi_v-k := predsort.
      tilgodese_v-k := predsort.
      tilgrise_v-k := predsort.
      tilgro_v-k := predsort.
      tilhogge_v-k := predsort.
      tilholde_v-k := predsort.
      tilhøre_v-k := predsort.
      tilhugge_v-k := predsort.
      tilhylle_v-k := predsort.
      tilintetgjøre_v-k := predsort.
      tiljamne_v-k := predsort.
      tiljevne_v-k := predsort.
      tiljuble_v-k := predsort.
      tilkalle_v-k := predsort.
      tilkjempe_v-k := predsort.
      tilkjennegi_v-k := predsort.
      tilkjenne_v-k := predsort.
      tilkjøre_v-k := predsort.
      tilkline_v-k := predsort.
      tilknytte_v-k := predsort.
      tilkoble_v-k := predsort.
      tilkomme_v-k := predsort.
      tilkople_v-k := predsort.
      tillage_v-k := predsort.
      tillate_v-k := predsort.
      tillegge_v-k := predsort.
      tillempe_v-k := predsort.
      tilligge_v-k := predsort.
      tillyse_v-k := predsort.
      tilmåle_v-k := predsort.
      tilnærme_v-k := predsort.
      tilpasse_v-k := predsort.
      tilplante_v-k := predsort.
      tilrå_v-k := predsort.
      tilrane_v-k := predsort.
      tilrede_v-k := predsort.
      tilrettelegge_v-k := predsort.
      tilrettevise_v-k := predsort.
      tilrive_v-k := predsort.
      tilså_v-k := predsort.
      tilsende_v-k := predsort.
      tilsette_v-k := predsort.
      tilsidesette_v-k := predsort.
      tilsi_v-k := predsort.
      tilsikte_v-k := predsort.
      tilskjøte_v-k := predsort.
      tilskrive_v-k := predsort.
      tilskye_v-k := predsort.
      tilskynde_v-k := predsort.
      tilsløre_v-k := predsort.
      tilslutte_v-k := predsort.
      tilsmøre_v-k := predsort.
      tilsmusse_v-k := predsort.
      tilsnakke_v-k := predsort.
      tilsnike_v-k := predsort.
      tilsøle_v-k := predsort.
      tilspisse_v-k := predsort.
      tilstå_v-k := predsort.
      tilstede_v-k := predsort.
      tilstille_v-k := predsort.
      tilstoppe_v-k := predsort.
      tilstøte_v-k := predsort.
      tilstøyte_v-k := predsort.
      tilstrebe_v-k := predsort.
      tilsvare_v-k := predsort.
      tilsvine_v-k := predsort.
      tilta_v-k := predsort.
      tiltale_v-k := predsort.
      tiltenke_v-k := predsort.
      tiltrekke_v-k := predsort.
      tiltre_v-k := predsort.
      tiltro_v-k := predsort.
      tiltru_v-k := predsort.
      tiltvinge_v-k := predsort.
      tilvende_v-k := predsort.
      tilvenne_v-k := predsort.
      tilvirke_v-k := predsort.
      tilvise_v-k := predsort.
      times_v-k := predsort.
      tinde_v-k := predsort.
      tindre_v-k := predsort.
      tine_v-k := predsort.
      tinge_v-k := predsort.
      tingliggjøre_v-k := predsort.
      tinglyse_v-k := predsort.
      tinkle_v-k := predsort.
      tiple_v-k := predsort.
      tippe_v-k := predsort.
      tipse_v-k := predsort.
      tirre_v-k := predsort.
      tiske_v-k := predsort.
      tisle_v-k := predsort.
      tisse_v-k := predsort.
      titrere_v-k := predsort.
      titte_v-k := predsort.
      titulere_v-k := predsort.
      tjadre_v-k := predsort.
      tjærebre_v-k := predsort.
      tjærebrenne_v-k := predsort.
      tjære_v-k := predsort.
      tjafse_v-k := predsort.
      tjåke_v-k := predsort.
      tjatre_v-k := predsort.
      tjatte_v-k := predsort.
      tjelde_v-k := predsort.
      tjene_v-k := predsort.
      tjenestegjøre_v-k := predsort.
      tjenstgjøre_v-k := predsort.
      tjone_v-k := predsort.
      tjørebre_v-k := predsort.
      tjore_v-k := predsort.
      tjukne_v-k := predsort.
      tjuve_v-k := predsort.
      tjuvlåne_v-k := predsort.
      tjuvlytte_v-k := predsort.
      tjuvslakte_v-k := predsort.
      tjuvstarte_v-k := predsort.
      tjuvtitte_v-k := predsort.
      tjuvtrene_v-k := predsort.
      todele_v-k := predsort.
      toe_v-k := predsort.
      tøffe_v-k := predsort.
      tøfle_v-k := predsort.
      toge_v-k := predsort.
      tø_v-k := predsort.
      tolerere_v-k := predsort.
      tolke_v-k := predsort.
      tollbehandle_v-k := predsort.
      tømme_v-k := predsort.
      tømre_v-k := predsort.
      tomse_v-k := predsort.
      tone_v-k := predsort.
      toppe_v-k := predsort.
      toppmontere_v-k := predsort.
      tordne_v-k := predsort.
      tore_v-k := predsort.
      tørke_v-k := predsort.
      tørne_v-k := predsort.
      torpedere_v-k := predsort.
      tørre_v-k := predsort.
      tørrkoke_v-k := predsort.
      tørrlegge_v-k := predsort.
      tørrtrene_v-k := predsort.
      tørste_v-k := predsort.
      torturere_v-k := predsort.
      torve_v-k := predsort.
      totalfrede_v-k := predsort.
      totalskade_v-k := predsort.
      tøtsje_v-k := predsort.
      touche_v-k := predsort.
      touchere_v-k := predsort.
      tove_v-k := predsort.
      tøve_v-k := predsort.
      tøye_v-k := predsort.
      tøyle_v-k := predsort.
      tøyse_v-k := predsort.
      trabelere_v-k := predsort.
      trace_v-k := predsort.
      tråde_v-k := predsort.
      tradere_v-k := predsort.
      træ_v-k := predsort.
      trafikkere_v-k := predsort.
      trakassere_v-k := predsort.
      trakke_v-k := predsort.
      tråkke_v-k := predsort.
      tråkle_v-k := predsort.
      trå_v-k := predsort.
      trakte_v-k := predsort.
      traktere_v-k := predsort.
      tralle_v-k := predsort.
      trampe_v-k := predsort.
      tranchere_v-k := predsort.
      transcendere_v-k := predsort.
      transferere_v-k := predsort.
      transfigurere_v-k := predsort.
      transformere_v-k := predsort.
      transfundere_v-k := predsort.
      transistorisere_v-k := predsort.
      transitivere_v-k := predsort.
      transittere_v-k := predsort.
      transjere_v-k := predsort.
      transkribere_v-k := predsort.
      translitterere_v-k := predsort.
      transmittere_v-k := predsort.
      transpirere_v-k := predsort.
      transplantere_v-k := predsort.
      transponere_v-k := predsort.
      transportere_v-k := predsort.
      transskribere_v-k := predsort.
      trappe_v-k := predsort.
      trase_v-k := predsort.
      trasere_v-k := predsort.
      traske_v-k := predsort.
      trasse_v-k := predsort.
      trassere_v-k := predsort.
      trave_v-k := predsort.
      tråve_v-k := predsort.
      traversere_v-k := predsort.
      travestere_v-k := predsort.
      tredele_v-k := predsort.
      tredoble_v-k := predsort.
      treffe_v-k := predsort.
      treffes_v-k := predsort.
      trege_v-k := predsort.
      trekke_v-k := predsort.
      tre_v-k := predsort.
      trekte_v-k := predsort.
      trellbinde_v-k := predsort.
      trelle_v-k := predsort.
      tremulere_v-k := predsort.
      trene_v-k := predsort.
      trenere_v-k := predsort.
      trenge_v-k := predsort.
      trenges_v-k := predsort.
      trepanere_v-k := predsort.
      treske_v-k := predsort.
      tretne_v-k := predsort.
      trette_v-k := predsort.
      trevle_v-k := predsort.
      triangulere_v-k := predsort.
      trigge_v-k := predsort.
      trikke_v-k := predsort.
      trikse_v-k := predsort.
      trille_v-k := predsort.
      trimme_v-k := predsort.
      trine_v-k := predsort.
      trinse_v-k := predsort.
      trippe_v-k := predsort.
      triumfere_v-k := predsort.
      trive_v-k := predsort.
      trives_v-k := predsort.
      trivialisere_v-k := predsort.
      trivle_v-k := predsort.
      tro_v-k := predsort.
      trø_v-k := predsort.
      trollbinde_v-k := predsort.
      trolle_v-k := predsort.
      trollstemme_v-k := predsort.
      trollstille_v-k := predsort.
      trolove_v-k := predsort.
      tromle_v-k := predsort.
      tromme_v-k := predsort.
      trone_v-k := predsort.
      troppe_v-k := predsort.
      trosse_v-k := predsort.
      trøste_v-k := predsort.
      trøstespise_v-k := predsort.
      trøtne_v-k := predsort.
      trøtte_v-k := predsort.
      true_v-k := predsort.
      truge_v-k := predsort.
      tru_v-k := predsort.
      trulove_v-k := predsort.
      trulte_v-k := predsort.
      trumfe_v-k := predsort.
      trutne_v-k := predsort.
      trygde_v-k := predsort.
      trygge_v-k := predsort.
      trygle_v-k := predsort.
      trykke_v-k := predsort.
      trykkimpregnere_v-k := predsort.
      tryllebinde_v-k := predsort.
      trylle_v-k := predsort.
      tryne_v-k := predsort.
      tryte_v-k := predsort.
      tufse_v-k := predsort.
      tufte_v-k := predsort.
      tukle_v-k := predsort.
      tukte_v-k := predsort.
      tulle_v-k := predsort.
      tumle_v-k := predsort.
      tune_v-k := predsort.
      tupere_v-k := predsort.
      tuppe_v-k := predsort.
      ture_v-k := predsort.
      turne_v-k := predsort.
      turnere_v-k := predsort.
      turve_v-k := predsort.
      tusje_v-k := predsort.
      tusjere_v-k := predsort.
      tuske_v-k := predsort.
      tusle_v-k := predsort.
      tusse_v-k := predsort.
      tuste_v-k := predsort.
      tute_v-k := predsort.
      tutle_v-k := predsort.
      tvære_v-k := predsort.
      tvangsdirigere_v-k := predsort.
      tvangsfôre_v-k := predsort.
      tvangsinnlegge_v-k := predsort.
      tvedele_v-k := predsort.
      tverrsnu_v-k := predsort.
      tverrstanse_v-k := predsort.
      tverrstille_v-k := predsort.
      tverrvende_v-k := predsort.
      tvette_v-k := predsort.
      tviholde_v-k := predsort.
      tvile_v-k := predsort.
      tvinge_v-k := predsort.
      tvinne_v-k := predsort.
      tviste_v-k := predsort.
      twiste_v-k := predsort.
      tyde_v-k := predsort.
      tydeliggjøre_v-k := predsort.
      tygge_v-k := predsort.
      tykke_v-k := predsort.
      tykkes_v-k := predsort.
      tykne_v-k := predsort.
      ty_v-k := predsort.
      tylle_v-k := predsort.
      tyne_v-k := predsort.
      tynge_v-k := predsort.
      tynne_v-k := predsort.
      typebestemme_v-k := predsort.
      typegodkjenne_v-k := predsort.
      type_v-k := predsort.
      typifisere_v-k := predsort.
      typisere_v-k := predsort.
      typologisere_v-k := predsort.
      tyrannisere_v-k := predsort.
      tysse_v-k := predsort.
      tyste_v-k := predsort.
      tystne_v-k := predsort.
      tyte_v-k := predsort.
      tyverianmelde_v-k := predsort.
      tyvlåne_v-k := predsort.
      tyvlytte_v-k := predsort.
      tyvslakte_v-k := predsort.
      tyvstarte_v-k := predsort.
      tyvtitte_v-k := predsort.
      tyvtrene_v-k := predsort.
      udødeliggjøre_v-k := predsort.
      ufarliggjøre_v-k := predsort.
      uffe_v-k := predsort.
      uføretrygde_v-k := predsort.
      ugge_v-k := predsort.
      uglese_v-k := predsort.
      ugreie_v-k := predsort.
      ugyldiggjøre_v-k := predsort.
      uleilige_v-k := predsort.
      ule_v-k := predsort.
      ulke_v-k := predsort.
      ulme_v-k := predsort.
      ulne_v-k := predsort.
      ulykkesforsikre_v-k := predsort.
      umake_v-k := predsort.
      umenneskeliggjøre_v-k := predsort.
      umuliggjøre_v-k := predsort.
      umyndiggjøre_v-k := predsort.
      underbetale_v-k := predsort.
      underbinde_v-k := predsort.
      underbringe_v-k := predsort.
      underbygge_v-k := predsort.
      underby_v-k := predsort.
      underdimensjonere_v-k := predsort.
      underdrive_v-k := predsort.
      undereksponere_v-k := predsort.
      underforsikre_v-k := predsort.
      underforstå_v-k := predsort.
      undergå_v-k := predsort.
      undergi_v-k := predsort.
      undergjære_v-k := predsort.
      undergrave_v-k := predsort.
      underhandle_v-k := predsort.
      underholde_v-k := predsort.
      underkaste_v-k := predsort.
      underkjenne_v-k := predsort.
      underkjøle_v-k := predsort.
      underkommunisere_v-k := predsort.
      underkue_v-k := predsort.
      underlegge_v-k := predsort.
      underlette_v-k := predsort.
      underminere_v-k := predsort.
      underordne_v-k := predsort.
      underrapportere_v-k := predsort.
      underrepresentere_v-k := predsort.
      underrette_v-k := predsort.
      underselge_v-k := predsort.
      underskride_v-k := predsort.
      underskrive_v-k := predsort.
      underslå_v-k := predsort.
      undersøke_v-k := predsort.
      underspille_v-k := predsort.
      understå_v-k := predsort.
      understellsbehandle_v-k := predsort.
      understøtte_v-k := predsort.
      understreke_v-k := predsort.
      understryke_v-k := predsort.
      undertegne_v-k := predsort.
      undertrykke_v-k := predsort.
      undertvinge_v-k := predsort.
      undervise_v-k := predsort.
      undervurdere_v-k := predsort.
      undre_v-k := predsort.
      undres_v-k := predsort.
      uniformere_v-k := predsort.
      universalisere_v-k := predsort.
      unnbe_v-k := predsort.
      unndra_v-k := predsort.
      unne_v-k := predsort.
      unnfange_v-k := predsort.
      unnfly_v-k := predsort.
      unngå_v-k := predsort.
      unngjelde_v-k := predsort.
      unnkomme_v-k := predsort.
      unnlate_v-k := predsort.
      unnse_v-k := predsort.
      unnsette_v-k := predsort.
      unnsi_v-k := predsort.
      unnskylde_v-k := predsort.
      unnslå_v-k := predsort.
      unnsleppe_v-k := predsort.
      unnslippe_v-k := predsort.
      unnta_v-k := predsort.
      unnvære_v-k := predsort.
      unnvike_v-k := predsort.
      urbanisere_v-k := predsort.
      urframføre_v-k := predsort.
      urfremføre_v-k := predsort.
      urgere_v-k := predsort.
      urinere_v-k := predsort.
      uroe_v-k := predsort.
      uroppføre_v-k := predsort.
      urydde_v-k := predsort.
      uskadeliggjøre_v-k := predsort.
      usurpere_v-k := predsort.
      usynliggjøre_v-k := predsort.
      utagere_v-k := predsort.
      utånde_v-k := predsort.
      utarbeide_v-k := predsort.
      utarte_v-k := predsort.
      utause_v-k := predsort.
      utbasunere_v-k := predsort.
      utbedre_v-k := predsort.
      utbe_v-k := predsort.
      utbetale_v-k := predsort.
      utbreie_v-k := predsort.
      utbre_v-k := predsort.
      utbringe_v-k := predsort.
      utbrodere_v-k := predsort.
      utbryte_v-k := predsort.
      utbygge_v-k := predsort.
      utby_v-k := predsort.
      utbytte_v-k := predsort.
      utdanne_v-k := predsort.
      utdatere_v-k := predsort.
      utdebattere_v-k := predsort.
      utdele_v-k := predsort.
      utdjupe_v-k := predsort.
      utdø_v-k := predsort.
      utdype_v-k := predsort.
      utebli_v-k := predsort.
      uteglemme_v-k := predsort.
      uteglømme_v-k := predsort.
      uteksaminere_v-k := predsort.
      uteksperimentere_v-k := predsort.
      utelåse_v-k := predsort.
      utelate_v-k := predsort.
      utelukke_v-k := predsort.
      uteske_v-k := predsort.
      utestenge_v-k := predsort.
      utfelle_v-k := predsort.
      utferdige_v-k := predsort.
      utfolde_v-k := predsort.
      utfordre_v-k := predsort.
      utføre_v-k := predsort.
      utforme_v-k := predsort.
      utforske_v-k := predsort.
      utfri_v-k := predsort.
      utfritte_v-k := predsort.
      utfylle_v-k := predsort.
      utgå_v-k := predsort.
      utgiftsføre_v-k := predsort.
      utgi_v-k := predsort.
      utgjøre_v-k := predsort.
      utgrunne_v-k := predsort.
      utgyte_v-k := predsort.
      utheve_v-k := predsort.
      utholde_v-k := predsort.
      uthole_v-k := predsort.
      uthule_v-k := predsort.
      utjamne_v-k := predsort.
      utjevne_v-k := predsort.
      utkalle_v-k := predsort.
      utkåre_v-k := predsort.
      utkaste_v-k := predsort.
      utkjempe_v-k := predsort.
      utklarere_v-k := predsort.
      utklasse_v-k := predsort.
      utklekke_v-k := predsort.
      utkoble_v-k := predsort.
      utkommandere_v-k := predsort.
      utkomme_v-k := predsort.
      utkonkurrere_v-k := predsort.
      utkople_v-k := predsort.
      utkreve_v-k := predsort.
      utkrystallisere_v-k := predsort.
      utlade_v-k := predsort.
      utlåne_v-k := predsort.
      utlede_v-k := predsort.
      utlegge_v-k := predsort.
      utleie_v-k := predsort.
      utlevere_v-k := predsort.
      utligne_v-k := predsort.
      utlikne_v-k := predsort.
      utlodde_v-k := predsort.
      utløpe_v-k := predsort.
      utløse_v-k := predsort.
      utlosse_v-k := predsort.
      utlove_v-k := predsort.
      utløyse_v-k := predsort.
      utlyde_v-k := predsort.
      utlyse_v-k := predsort.
      utmaie_v-k := predsort.
      utmale_v-k := predsort.
      utmåle_v-k := predsort.
      utmanøvrere_v-k := predsort.
      utmatte_v-k := predsort.
      utmeisle_v-k := predsort.
      utmerke_v-k := predsort.
      utmynte_v-k := predsort.
      utnevne_v-k := predsort.
      utnytte_v-k := predsort.
      utøse_v-k := predsort.
      utøve_v-k := predsort.
      utpante_v-k := predsort.
      utparsellere_v-k := predsort.
      utpeke_v-k := predsort.
      utpensle_v-k := predsort.
      utplassere_v-k := predsort.
      utplyndre_v-k := predsort.
      utpønse_v-k := predsort.
      utpønske_v-k := predsort.
      utporsjonere_v-k := predsort.
      utprege_v-k := predsort.
      utprøve_v-k := predsort.
      utradere_v-k := predsort.
      utrangere_v-k := predsort.
      utrede_v-k := predsort.
      utrette_v-k := predsort.
      utringe_v-k := predsort.
      utrope_v-k := predsort.
      utruste_v-k := predsort.
      utrydde_v-k := predsort.
      utrydningstrue_v-k := predsort.
      utse_v-k := predsort.
      utsende_v-k := predsort.
      utsette_v-k := predsort.
      utsi_v-k := predsort.
      utskille_v-k := predsort.
      utskipe_v-k := predsort.
      utskrive_v-k := predsort.
      utskyte_v-k := predsort.
      utslette_v-k := predsort.
      utslynge_v-k := predsort.
      utsmykke_v-k := predsort.
      utsondre_v-k := predsort.
      utsone_v-k := predsort.
      utsortere_v-k := predsort.
      utspare_v-k := predsort.
      utspeide_v-k := predsort.
      utspille_v-k := predsort.
      utspinne_v-k := predsort.
      utspionere_v-k := predsort.
      utspørre_v-k := predsort.
      utsprede_v-k := predsort.
      utspre_v-k := predsort.
      utstaffere_v-k := predsort.
      utstå_v-k := predsort.
      utstede_v-k := predsort.
      utstille_v-k := predsort.
      utstoppe_v-k := predsort.
      utstøte_v-k := predsort.
      utstråle_v-k := predsort.
      utstykke_v-k := predsort.
      utstyre_v-k := predsort.
      utsuge_v-k := predsort.
      utta_v-k := predsort.
      uttale_v-k := predsort.
      uttegne_v-k := predsort.
      uttransportere_v-k := predsort.
      uttrykke_v-k := predsort.
      uttvære_v-k := predsort.
      uttyde_v-k := predsort.
      utvandre_v-k := predsort.
      utvanne_v-k := predsort.
      utvatne_v-k := predsort.
      utveksle_v-k := predsort.
      utvide_v-k := predsort.
      utvikle_v-k := predsort.
      utvinne_v-k := predsort.
      utvirke_v-k := predsort.
      utvise_v-k := predsort.
      vabbe_v-k := predsort.
      vade_v-k := predsort.
      vadre_v-k := predsort.
      væpne_v-k := predsort.
      være_v-k := predsort.
      væske_v-k := predsort.
      væte_v-k := predsort.
      vafle_v-k := predsort.
      vagabondere_v-k := predsort.
      våge_v-k := predsort.
      vagge_v-k := predsort.
      vagle_v-k := predsort.
      vaie_v-k := predsort.
      vake_v-k := predsort.
      våke_v-k := predsort.
      vakle_v-k := predsort.
      vakne_v-k := predsort.
      våkne_v-k := predsort.
      vaksinere_v-k := predsort.
      vakte_v-k := predsort.
      valandere_v-k := predsort.
      våle_v-k := predsort.
      valfarte_v-k := predsort.
      validere_v-k := predsort.
      valke_v-k := predsort.
      valne_v-k := predsort.
      valpe_v-k := predsort.
      valse_v-k := predsort.
      valte_v-k := predsort.
      valutere_v-k := predsort.
      valvere_v-k := predsort.
      vanære_v-k := predsort.
      vanakte_v-k := predsort.
      vandalisere_v-k := predsort.
      vande_v-k := predsort.
      vånde_v-k := predsort.
      vandle_v-k := predsort.
      vandre_v-k := predsort.
      vanhedre_v-k := predsort.
      vanhellige_v-k := predsort.
      vanke_v-k := predsort.
      vannbeise_v-k := predsort.
      vanne_v-k := predsort.
      vannkjemme_v-k := predsort.
      vannslipe_v-k := predsort.
      vanrøkte_v-k := predsort.
      vansire_v-k := predsort.
      vanskeliggjøre_v-k := predsort.
      vanskjøtte_v-k := predsort.
      vansmekte_v-k := predsort.
      vanstelle_v-k := predsort.
      vanstyre_v-k := predsort.
      vante_v-k := predsort.
      vantrives_v-k := predsort.
      vanvørde_v-k := predsort.
      vanvøre_v-k := predsort.
      vare_v-k := predsort.
      våre_v-k := predsort.
      våres_v-k := predsort.
      vareta_v-k := predsort.
      varetektsfengsle_v-k := predsort.
      variere_v-k := predsort.
      varme_v-k := predsort.
      varmhamre_v-k := predsort.
      varmherde_v-k := predsort.
      varmheve_v-k := predsort.
      varmrøke_v-k := predsort.
      varmrøyke_v-k := predsort.
      varpe_v-k := predsort.
      varsku_v-k := predsort.
      varsle_v-k := predsort.
      varte_v-k := predsort.
      vase_v-k := predsort.
      våse_v-k := predsort.
      vaske_v-k := predsort.
      vassbeise_v-k := predsort.
      vasse_v-k := predsort.
      vasskjemme_v-k := predsort.
      vasslipe_v-k := predsort.
      vass-slipe_v-k := predsort.
      vatne_v-k := predsort.
      våtne_v-k := predsort.
      vedbli_v-k := predsort.
      vedde_v-k := predsort.
      vederfares_v-k := predsort.
      vederkvege_v-k := predsort.
      vedgå_v-k := predsort.
      vedkjenne_v-k := predsort.
      vedkomme_v-k := predsort.
      vedlegge_v-k := predsort.
      vedlikeholde_v-k := predsort.
      vedrøre_v-k := predsort.
      vedstå_v-k := predsort.
      vedta_v-k := predsort.
      vedtektsfeste_v-k := predsort.
      vedvare_v-k := predsort.
      vefte_v-k := predsort.
      vege_v-k := predsort.
      vegetere_v-k := predsort.
      vegre_v-k := predsort.
      veide_v-k := predsort.
      veie_v-k := predsort.
      veikne_v-k := predsort.
      veilede_v-k := predsort.
      veite_v-k := predsort.
      veive_v-k := predsort.
      vekke_v-k := predsort.
      vekne_v-k := predsort.
      veksle_v-k := predsort.
      vektlegge_v-k := predsort.
      velbegrunne_v-k := predsort.
      velge_v-k := predsort.
      velle_v-k := predsort.
      velsigne_v-k := predsort.
      velte_v-k := predsort.
      vemmes_v-k := predsort.
      vende_v-k := predsort.
      venne_v-k := predsort.
      vente_v-k := predsort.
      ventilere_v-k := predsort.
      verbalisere_v-k := predsort.
      verdige_v-k := predsort.
      verdsette_v-k := predsort.
      verdsliggjøre_v-k := predsort.
      verge_v-k := predsort.
      verifisere_v-k := predsort.
      verje_v-k := predsort.
      verke_v-k := predsort.
      vernalisere_v-k := predsort.
      verne_v-k := predsort.
      verpe_v-k := predsort.
      versere_v-k := predsort.
      versifisere_v-k := predsort.
      versne_v-k := predsort.
      verve_v-k := predsort.
      vettskremme_v-k := predsort.
      veve_v-k := predsort.
      vibrere_v-k := predsort.
      vide_v-k := predsort.
      videreforedle_v-k := predsort.
      videreføre_v-k := predsort.
      viderefortelle_v-k := predsort.
       videresende_v-k := predsort.
      videreutdanne_v-k := predsort.
      videreutvikle_v-k := predsort.
      vie_v-k := predsort.
      vifte_v-k := predsort.
      vigge_v-k := predsort.
      vigilere_v-k := predsort.
      vigle_v-k := predsort.
      vigsle_v-k := predsort.
      vikariere_v-k := predsort.
      vike_v-k := predsort.
      vikle_v-k := predsort.
      vikte_v-k := predsort.
      vildre_v-k := predsort.
      villede_v-k := predsort.
      villeie_v-k := predsort.
      ville_v-k := predsort.
      vill-lede_v-k := predsort.
      vill-leie_v-k := predsort.
      vime_v-k := predsort.
      vimre_v-k := predsort.
      vimse_v-k := predsort.
      vinde_v-k := predsort.
      vindisere_v-k := predsort.
      vinge_v-k := predsort.
      vingle_v-k := predsort.
      vinke_v-k := predsort.
      vinkle_v-k := predsort.
      vinne_v-k := predsort.
      vinsje_v-k := predsort.
      vinterisolere_v-k := predsort.
      vintre_v-k := predsort.
      vintres_v-k := predsort.
      vippe_v-k := predsort.
      vire_v-k := predsort.
      virilisere_v-k := predsort.
      virke_v-k := predsort.
      virkeliggjøre_v-k := predsort.
      virre_v-k := predsort.
      virvle_v-k := predsort.
      vise_v-k := predsort.
      visere_v-k := predsort.
      visitere_v-k := predsort.
      viske_v-k := predsort.
      visle_v-k := predsort.
      visne_v-k := predsort.
      vispe_v-k := predsort.
      visualisere_v-k := predsort.
      vitalisere_v-k := predsort.
      vitaminisere_v-k := predsort.
      vite_v-k := predsort.
      vitje_v-k := predsort.
      vitne_v-k := predsort.
      vitse_v-k := predsort.
      vivisekere_v-k := predsort.
      voffe_v-k := predsort.
      vogge_v-k := predsort.
      vokalisere_v-k := predsort.
      vokse_v-k := predsort.
      vokte_v-k := predsort.
      volde_v-k := predsort.
      voldsutsette_v-k := predsort.
      voldta_v-k := predsort.
      vøle_v-k := predsort.
      voltere_v-k := predsort.
      voltigere_v-k := predsort.
      vomere_v-k := predsort.
      vone_v-k := predsort.
      vorde_v-k := predsort.
      vørde_v-k := predsort.
      vøre_v-k := predsort.
      votere_v-k := predsort.
      vove_v-k := predsort.
      vræle_v-k := predsort.
      vrake_v-k := predsort.
      vralte_v-k := predsort.
      vredes_v-k := predsort.
      vrenge_v-k := predsort.
      vrikke_v-k := predsort.
      vrikle_v-k := predsort.
      vri_v-k := predsort.
      vrimle_v-k := predsort.
      vringle_v-k := predsort.
      vrinske_v-k := predsort.
      vriste_v-k := predsort.
      vrøvle_v-k := predsort.
      vugge_v-k := predsort.
      vulgarisere_v-k := predsort.
      vulkanisere_v-k := predsort.
      vulke_v-k := predsort.
      vurdere_v-k := predsort.
      webbe_v-k := predsort.
      xerografere_v-k := predsort.
      xylografere_v-k := predsort.
      ydde_v-k := predsort.
      ydmyke_v-k := predsort.
      yle_v-k := predsort.
      ymte_v-k := predsort.
      ynde_v-k := predsort.
      ynges_v-k := predsort.
      yngle_v-k := predsort.
      ynke_v-k := predsort.
      ynkes_v-k := predsort.
      yppe_v-k := predsort.
      yre_v-k := predsort.
      yste_v-k := predsort.
      yte_v-k := predsort.
      ytre_v-k := predsort.
